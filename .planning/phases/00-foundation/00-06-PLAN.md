---
phase: 0
plan: 06
type: execute
wave: 3
depends_on:
  - 00-04
files_modified:
  - docker-compose.yml
  - tests/integration/conftest.py
  - tests/integration/db/__init__.py
  - tests/integration/db/test_alembic_and_hypertables.py
autonomous: false
requirements:
  - FOUND-03
  - OPS-07
  - TEST-01
  - TEST-05
tags:
  - python
  - docker
  - testcontainers
  - timescaledb
  - migrations
  - integration-tests
must_haves:
  truths:
    - "docker-compose.yml defines a single postgres service using timescale/timescaledb:2.18.0-pg16 (D-25)"
    - "tests/integration/conftest.py defines a SESSION-scoped fixture PostgresContainer('timescale/timescaledb:2.18.0-pg16') to amortize ~5-10s container startup across all integration tests (Pitfall 5)"
    - "tests/integration/conftest.py runs `alembic upgrade head` once at session start; per-test fixtures TRUNCATE service_event for isolation"
    - "test_alembic_upgrade_is_idempotent runs `alembic upgrade head` TWICE on the same testcontainer and asserts no error (D-31 — rerun-safe)"
    - "test_service_event_is_hypertable queries timescaledb_information.hypertables and asserts service_event is registered (D-31)"
    - "test_service_event_has_compression_policy queries timescaledb_information.jobs and asserts a compression job exists for service_event (D-31)"
    - "[BLOCKING] schema push: `uv run alembic upgrade head` against the testcontainers Postgres exits 0 and is rerun-safe — this is the runtime gate for FOUND-03"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local TimescaleDB service for developer machines (D-25)"
    - path: "tests/integration/conftest.py"
      provides: "session-scoped PostgresContainer fixture; function-scoped DB cleanup fixture; alembic upgrade head once per session"
    - path: "tests/integration/db/test_alembic_and_hypertables.py"
      provides: "3 integration tests covering rerun-safety + hypertable existence + compression policy existence (D-31)"
  key_links:
    - from: "tests/integration/conftest.py"
      to: "alembic/env.py + alembic/versions/0001..0002"
      via: "PostgresContainer URL injected into DATABASE_URL env var; alembic upgrade head runs once at session start"
      pattern: "container.get_connection_url() rewritten to postgresql+asyncpg://; subprocess.run([uv, run, alembic, upgrade, head])"
    - from: "tests/integration/db/test_alembic_and_hypertables.py"
      to: "src/shortfire/db/timescale.py helpers"
      via: "The helpers' SQL must produce a working hypertable + compression policy on a real Timescale container"
      pattern: "SELECT hypertable_name FROM timescaledb_information.hypertables WHERE hypertable_name = 'service_event'"
---

<objective>
Provide local-dev TimescaleDB via docker-compose (D-25) and the [BLOCKING] integration tests that actually run `alembic upgrade head` against a real `timescale/timescaledb:2.18.0-pg16` container and verify the hypertable + compression policy lands correctly (D-31). This is the runtime gate for FOUND-03 — Plan 00-04 only verified static migration shape; this plan exercises the migrations end-to-end.

Purpose: Phase 0 cannot pass verification without proving the migrations actually run on a real Timescale instance. Build and type checks pass trivially without migrations (no production code touches the DB yet); the testcontainers run is the only signal that the migration files + db/timescale.py helpers + alembic/env.py are mutually consistent. Pitfall 5 (testcontainers cold-start cost) is mitigated by session-scoped fixture.

Output: docker-compose.yml + 2 test files. The integration test suite (`pytest -m integration`) becomes the gate for `/gsd:verify-work` on Phase 0.
</objective>

<execution_context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-VALIDATION.md
</context>

<interfaces>
The integration test suite is THE gate for FOUND-03 (D-31). Verifies that the migration files from Plan 00-04 actually run on a real `timescale/timescaledb:2.18.0-pg16` Postgres.

Test infrastructure pattern (Pitfall 5):
- Session-scoped: `PostgresContainer("timescale/timescaledb:2.18.0-pg16")` — start once
- Session-scoped: `subprocess.run(["uv", "run", "alembic", "upgrade", "head"], env={"DATABASE_URL": container_url, **os.environ})` — migrate once
- Function-scoped: `TRUNCATE service_event` between tests (cheap)

testcontainers-python: `from testcontainers.postgres import PostgresContainer`. Pass custom image as constructor arg. Image must have TimescaleDB extension pre-installed (the `timescale/timescaledb:2.18.0-pg16` image does).
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testcontainers PostgresContainer -> alembic upgrade | DATABASE_URL injected via env; URL rewrite from postgresql:// -> postgresql+asyncpg:// in env.py (already in Plan 00-04) |
| Local dev docker-compose -> developer's psql | docker-compose listens on localhost:5432; not exposed beyond localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Tampering | tests/integration/db/test_alembic_and_hypertables.py | mitigate | Tests query `timescaledb_information` system views (read-only); no dynamic table names passed into create_hypertable/etc. — the helpers from Plan 00-04 receive hardcoded `'service_event'`. |
| T-00-09 | Tampering | Local dev vs CI parity | mitigate | docker-compose uses the SAME image tag (`timescale/timescaledb:2.18.0-pg16`) as testcontainers AND Railway marketplace template — single source of truth. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.yml + tests/integration/conftest.py (session-scoped PostgresContainer + alembic upgrade head once-per-session + function-scoped cleanup)</name>
  <files>
    docker-compose.yml
    tests/integration/conftest.py
    tests/integration/db/__init__.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-25, D-31)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pitfall 5 (testcontainers TimescaleDB cold-start cost — session-scoped pattern)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Environment Availability (Docker availability assumption A4)
    alembic/env.py (Plan 00-04 — env.py reads DATABASE_URL from env)
  </read_first>
  <action>
    Create `docker-compose.yml` per D-25 (CONTEXT.md §D-25 has the full YAML — copy verbatim):
    ```yaml
    services:
      postgres:
        image: timescale/timescaledb:2.18.0-pg16
        environment:
          POSTGRES_USER: shortfire
          POSTGRES_PASSWORD: shortfire
          POSTGRES_DB: shortfire_dev
          TIMESCALEDB_TELEMETRY: 'off'
        ports: ["5432:5432"]
        volumes: ["shortfire-pg:/var/lib/postgresql/data"]
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U shortfire"]
    volumes:
      shortfire-pg: {}
    ```

    Create `tests/integration/__init__.py` (empty package marker — Plan 00-01 likely already created this; verify).

    Create `tests/integration/db/__init__.py` (empty package marker).

    Create `tests/integration/conftest.py`:
    - `import subprocess, sys, os` etc.
    - `from testcontainers.postgres import PostgresContainer`
    - **Session-scoped fixture `timescale_container`** (Pitfall 5): `@pytest.fixture(scope='session') def timescale_container(): with PostgresContainer('timescale/timescaledb:2.18.0-pg16') as c: yield c`
    - **Session-scoped fixture `migrated_db`**: yields the container URL AFTER running `alembic upgrade head`. Implementation: read connection URL from `timescale_container.get_connection_url()` (which returns `postgresql+psycopg2://...` — rewrite to `postgresql://` for alembic, since env.py handles the asyncpg rewrite). Set `DATABASE_URL` in environment via `monkeypatch.setenv` at SESSION scope (use the `pytest_collection_modifyitems` hook or a `module_scoped_monkeypatch` autouse fixture). Then `subprocess.run(["uv", "run", "alembic", "upgrade", "head"], env={"DATABASE_URL": url, **os.environ}, check=True, capture_output=True)`. Yield the URL.
    - **Function-scoped fixture `clean_service_event`**: `@pytest.fixture def clean_service_event(migrated_db): import psycopg or asyncpg via asyncio.run(...): conn = ...; await conn.execute("TRUNCATE service_event"); yield; truncate again on teardown`. Use `asyncpg` (Plan 00-04 standardized on it; D-30).
    - Mark integration tests via `pytest.ini_options.markers = ["integration: requires Docker"]` — already configured in Plan 00-01 `pyproject.toml`.
    - Add a session-scoped autouse fixture that verifies Docker is available by attempting to import `docker`; if unavailable, `pytest.skip("Docker not available — integration tests require Docker")` at the COLLECTION level (use `pytest_collection_modifyitems`).

    IMPLEMENTATION DETAIL: `PostgresContainer.get_connection_url()` returns a URL like `postgresql+psycopg2://test:test@host:port/test`. Alembic env.py from Plan 00-04 rewrites `postgresql://` to `postgresql+asyncpg://` but may not handle the `postgresql+psycopg2://` form. Three options:
      (a) Pass `postgresql://` form to alembic by stripping the `+psycopg2`: `url.replace("postgresql+psycopg2://", "postgresql://")`.
      (b) Extend Plan 00-04's `_rewrite_url` to handle `+psycopg2` (cleaner; revisit if Plan 00-04 needs updating).
      (c) Use testcontainers' lower-level `get_container_host_ip()` + `get_exposed_port(5432)` to construct a clean `postgresql://...` URL ourselves.
    Use option (a) for now — strip `+psycopg2` before setting DATABASE_URL — and document in conftest.

    Document in conftest the resilient pattern for `wait_for_logs` or `wait_container_is_ready` — testcontainers handles this internally with `pg_isready` polling; default is sufficient.
  </action>
  <verify>
    <automated>docker info &gt;/dev/null 2&gt;&amp;1 &amp;&amp; uv run python -c "from testcontainers.postgres import PostgresContainer; print('testcontainers importable')" &amp;&amp; test -f docker-compose.yml &amp;&amp; uv run python -c "import yaml; doc = yaml.safe_load(open('docker-compose.yml')); assert doc['services']['postgres']['image'] == 'timescale/timescaledb:2.18.0-pg16'; assert doc['services']['postgres']['environment']['TIMESCALEDB_TELEMETRY'] == 'off'"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists; YAML parses; `services.postgres.image == "timescale/timescaledb:2.18.0-pg16"`; ports `5432:5432`; volume `shortfire-pg`; TIMESCALEDB_TELEMETRY=off
    - `tests/integration/conftest.py` exists and defines: (a) session-scoped `timescale_container` fixture using PostgresContainer; (b) session-scoped `migrated_db` fixture that runs `alembic upgrade head` ONCE and yields a URL; (c) function-scoped `clean_service_event` fixture that TRUNCATEs the table between tests
    - `uv run python -c "from testcontainers.postgres import PostgresContainer; PostgresContainer('timescale/timescaledb:2.18.0-pg16')"` constructs without error (does not start container)
    - `docker info` succeeds in the executor's environment (Assumption A4 — if Docker absent, the fixtures will skip and the plan can still ship; document this in 00-06-SUMMARY)
  </acceptance_criteria>
  <done>docker-compose.yml ready for local-dev Timescale; tests/integration/conftest.py provides session-scoped container + once-per-session alembic upgrade head + function-scoped service_event truncation, all marked integration; Docker availability checked at collection time.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Schema push — run `uv run alembic upgrade head` against testcontainers + 3 D-31 integration tests (idempotent, hypertable exists, compression policy exists)</name>
  <files>
    tests/integration/db/test_alembic_and_hypertables.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-31 — 3 required Phase 0 integration tests)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Validation Architecture FOUND-03 rows
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pitfall 3 (Alembic async + extension ordering — these tests catch the 3 failure modes that stack)
    tests/integration/conftest.py (Task 1)
    alembic/versions/0001_init_timescaledb.py + alembic/versions/0002_service_event_hypertable.py (Plan 00-04)
    src/shortfire/db/timescale.py (Plan 00-04)
  </read_first>
  <what-built>
    Three integration tests in `tests/integration/db/test_alembic_and_hypertables.py`:

    1. **`test_alembic_upgrade_is_idempotent`** (D-31): runs `subprocess.run(["uv", "run", "alembic", "upgrade", "head"], env={"DATABASE_URL": migrated_db_url, **os.environ}, check=True)` a SECOND time on a container that ALREADY has the migration applied (via the `migrated_db` session fixture). Assert returncode == 0 and stderr/stdout do not contain "ERROR" or "FATAL" patterns. Proves rerun-safety (Pitfall 3 — 0001's `CREATE EXTENSION IF NOT EXISTS` and 0002's helpers' `if_not_exists` flags do their job).

    2. **`test_service_event_is_hypertable`** (D-31): connect to `migrated_db` via asyncpg; execute `SELECT hypertable_name FROM timescaledb_information.hypertables WHERE hypertable_name = 'service_event'`. Assert exactly one row. Asserts the `create_hypertable` helper from Plan 00-04 actually registered service_event as a hypertable.

    3. **`test_service_event_has_compression_policy`** (D-31): connect to `migrated_db` via asyncpg; execute `SELECT job_id, hypertable_name, schedule_interval FROM timescaledb_information.jobs WHERE hypertable_name = 'service_event' AND application_name LIKE 'Compression%'`. Assert at least one row (the compression job created by `add_compression_policy("service_event", after_age="7 days")` from 0002). Additionally execute `SELECT compress_segmentby, compress_orderby FROM timescaledb_information.compression_settings WHERE hypertable_name = 'service_event'` and assert `compress_segmentby == 'service_name'` and `compress_orderby == 'ts DESC'`.
  </what-built>
  <action>
    Create `tests/integration/db/test_alembic_and_hypertables.py` with the 3 tests above. Use `@pytest.mark.integration` on every test. Use `asyncpg.connect(url)` for queries; CONVERT the migrated_db URL to the asyncpg form (`postgresql://...` — asyncpg does NOT accept the `+asyncpg` suffix in its own connect calls).

    The actual `alembic upgrade head` against testcontainers occurred ONCE in the `migrated_db` session fixture (Task 1). This task's `test_alembic_upgrade_is_idempotent` runs `alembic upgrade head` a SECOND time on the same container — verifying idempotency (D-31).

    Run the integration suite locally: `uv run pytest -m integration -v tests/integration/db/test_alembic_and_hypertables.py`. Expected outcome: 3 tests pass; total runtime ~30-60s (cold container ~10s + 2 migrations ~2s + 3 query tests <1s each + idempotent re-run ~2s).
  </action>
  <how-to-verify>
    1. Ensure Docker is running: `docker info` exits 0.
    2. Run: `uv run pytest -m integration -v tests/integration/db/test_alembic_and_hypertables.py`
    3. Expected output:
       ```
       tests/integration/db/test_alembic_and_hypertables.py::test_alembic_upgrade_is_idempotent PASSED
       tests/integration/db/test_alembic_and_hypertables.py::test_service_event_is_hypertable PASSED
       tests/integration/db/test_alembic_and_hypertables.py::test_service_event_has_compression_policy PASSED
       3 passed in ~30s
       ```
    4. Verify the schema with a manual psql connection while the container is up (if running locally via docker-compose):
       ```
       docker compose up -d postgres
       DATABASE_URL=postgresql://shortfire:shortfire@localhost:5432/shortfire_dev uv run alembic upgrade head
       psql postgresql://shortfire:shortfire@localhost:5432/shortfire_dev -c "SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='service_event'"
       # Expect 1 row.
       psql postgresql://shortfire:shortfire@localhost:5432/shortfire_dev -c "SELECT job_id, application_name, schedule_interval FROM timescaledb_information.jobs WHERE hypertable_name='service_event'"
       # Expect at least 1 compression job row.
       ```

    **This is the runtime gate for FOUND-03.** No production code touches the DB in Phase 0 — type checks and unit tests cannot catch a broken migration. ONLY this integration suite proves the migration files + helpers + env.py are mutually consistent.

    **If any test fails:** check Pitfall 3 (URL rewrite, extension ordering, naming convention). The most likely failure modes are:
    - `Could not parse SQLAlchemy URL` → URL rewrite is missing the `+psycopg2` form coming out of testcontainers (see Task 1 conftest option (a) — strip `+psycopg2` before passing to alembic).
    - `function create_hypertable does not exist` → migration 0001 didn't apply before 0002 (down_revision wiring broken).
    - Naming convention not applied → env.py is missing `from shortfire.db.base import Base; target_metadata = Base.metadata`.
  </how-to-verify>
  <verify>
    <automated>docker info &gt;/dev/null 2&gt;&amp;1 &amp;&amp; uv run pytest -m integration -v tests/integration/db/test_alembic_and_hypertables.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -m integration -v tests/integration/db/test_alembic_and_hypertables.py` reports `3 passed`
    - Total integration suite runtime is &lt; 90s (Pitfall 5 mitigation via session-scoped container; VALIDATION.md sampling rate budget)
    - Inside test_alembic_upgrade_is_idempotent: the second alembic upgrade head invocation exits 0 with no ERROR / FATAL in stderr
    - Inside test_service_event_is_hypertable: query returns exactly 1 row for service_event
    - Inside test_service_event_has_compression_policy: query returns at least 1 compression job row AND compress_segmentby='service_name' AND compress_orderby='ts DESC'
    - Manual smoke (docker compose up postgres + alembic upgrade head + psql query) confirms hypertable + compression policy land correctly outside the testcontainers fixture (proves the migration is environment-agnostic)
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 3 D-31 integration tests pass and the manual psql smoke confirms hypertable + compression job in `timescaledb_information.{hypertables, jobs, compression_settings}`. Or describe the failure mode (URL rewrite issue, extension ordering issue, naming convention issue) so the executor can iterate.</resume-signal>
  <done>FOUND-03 runtime gate satisfied: alembic upgrade head is rerun-safe on a real Timescale 2.18.0-pg16 container; service_event is a hypertable; compression policy exists with the locked segment_by/order_by config; Pitfall 3 failure modes all caught by the test trio.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `docker info` exits 0 (Docker available)
2. `uv run pytest -m integration -v tests/integration/db/test_alembic_and_hypertables.py` reports 3 passed within <90s
3. Manual `docker compose up -d postgres && DATABASE_URL=postgresql://shortfire:shortfire@localhost:5432/shortfire_dev uv run alembic upgrade head && uv run alembic upgrade head` (run twice — verify second invocation also exits 0)
4. Verify the `docker-compose.yml` file is parsed clean and uses TimescaleDB 2.18.0-pg16
</verification>

<success_criteria>
- docker-compose.yml provides local TimescaleDB (D-25)
- tests/integration/conftest.py uses session-scoped PostgresContainer (Pitfall 5) and runs alembic upgrade head once per session
- All 3 D-31 integration tests pass: idempotent rerun, hypertable existence, compression policy existence
- Manual psql smoke confirms the schema lands correctly outside of testcontainers
- FOUND-03 RUNTIME gate satisfied (Plan 00-04 was the static-shape gate; this is the runtime gate)
- OPS-07 RUNTIME gate satisfied (migration discipline proven against a real Timescale; Plan 00-07 will wire this into Railway preDeployCommand)
- TEST-01 reinforced (testcontainers + asyncpg used here; respx/aioresponses installed by Plan 00-01)
- TEST-05 partially supported (the InMemoryCandleRepo fakes from Plan 00-08 will use the same Plan 00-04 schema indirectly)
</success_criteria>

<output>
Create `.planning/phases/00-foundation/00-06-SUMMARY.md` documenting:
- Total integration suite runtime (target <90s; flag if exceeded — investigate Pitfall 5 mitigation effectiveness)
- The exact testcontainers image tag actually pulled (`docker images | grep timescaledb` after the test run)
- Output of `SELECT * FROM timescaledb_information.compression_settings WHERE hypertable_name='service_event'` (segmentby + orderby exactly as expected)
- Job ID + schedule_interval of the compression policy job (audit trail)
- Whether Docker was available; if not, document the skip pattern used
</output>
