---
phase: 0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .env.example
  - .pre-commit-config.yaml
  - .gitleaks.toml
  - AGENTS.md
  - README.md
  - src/shortfire/__init__.py
  - src/shortfire/domain/__init__.py
  - src/shortfire/settings/__init__.py
  - src/shortfire/observability/__init__.py
  - src/shortfire/db/__init__.py
  - src/shortfire/clients/__init__.py
  - src/shortfire/ingest/__init__.py
  - src/shortfire/strategy/__init__.py
  - src/shortfire/execution/__init__.py
  - src/shortfire/risk/__init__.py
  - src/shortfire/entrypoints/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/fakes/__init__.py
  - tests/unit/__init__.py
  - tests/integration/__init__.py
  - tests/unit/test_smoke_imports.py
  - tests/unit/repo_hygiene/test_gitignore_covers_env_files.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-06
  - OPS-08
  - TEST-01
  - TEST-02
  - TEST-06
tags:
  - python
  - uv
  - tooling
  - tdd
  - secrets
  - pre-commit
must_haves:
  truths:
    - "`uv sync` against a fresh clone produces a working venv with all production + dev deps locked in uv.lock"
    - "`uv run pytest -m 'not integration' -q` exits 0 against a fresh clone (proves test framework wired and smoke imports green)"
    - "`uv run ruff format --check .` and `uv run ruff check .` exit 0"
    - "`uv run pyright` exits 0 in strict mode against src/ and tests/"
    - "`pre-commit run --all-files` exits 0 (ruff, gitleaks, ban-naive-timestamp, ban-on-delete-cascade, ban-float-in-domain all green)"
    - "Committing a file containing a synthetic AWS key fingerprint is BLOCKED by the local gitleaks pre-commit hook"
    - ".gitignore prevents `.env`, `.env.local`, `.env.*.local`, `*.env.bak` from being tracked"
    - "AGENTS.md documents TDD discipline (failing test before code) and the 'no Railway healthcheck = liveness probe' pitfall (TEST-02)"
  artifacts:
    - path: "pyproject.toml"
      provides: "[project] metadata, [dependency-groups] dev group, [tool.pytest.ini_options], [tool.ruff], [tool.pyright], [tool.coverage.run] (per D-02, D-33, Pattern 9 pyproject skeleton)"
      contains: "shortfire 0.1.0, requires-python >=3.12, asyncio_mode = \"auto\", integration marker, --cov-fail-under not yet (CI flag), typeCheckingMode = \"strict\""
    - path: "uv.lock"
      provides: "Pinned dependency graph (PEP 735 compatible)"
    - path: ".gitignore"
      provides: "Excludes per D-23: .env, .env.local, .env.*.local, *.env.bak, .venv/, __pycache__/, *.pyc, .pytest_cache/, .mypy_cache/, .ruff_cache/, .coverage, htmlcov/, coverage.xml, dist/, build/, *.egg-info/, .DS_Store, .vscode/, .idea/"
    - path: ".env.example"
      provides: "Section-banner-formatted template per UI-SPEC.md §.env.example formatting; placeholders use `your_<purpose>_here`; Phase 5 trade keys commented-out"
    - path: ".pre-commit-config.yaml"
      provides: "Hooks: ruff-format, ruff, gitleaks v8.24.2, ban-naive-timestamp, ban-on-delete-cascade, ban-float-in-domain (per D-22 layer 1, D-32, Pattern 8)"
    - path: ".gitleaks.toml"
      provides: "useDefault=true + allowlist for uv.lock, tests/fixtures/, tests/fakes/, .env.example (per Pattern 8 + Pitfall 6)"
    - path: "AGENTS.md"
      provides: "TDD discipline doc (TEST-02); Railway healthcheck-is-not-liveness-probe note (Pitfall 4); GSD workflow link"
    - path: "src/shortfire/{domain,settings,observability,db,clients,ingest,strategy,execution,risk,entrypoints}/__init__.py"
      provides: "Package skeleton per D-06; empty __init__.py for ingest/strategy/execution/risk (filled in later phases)"
    - path: "tests/conftest.py"
      provides: "Shared Hypothesis strategies `money` and `utc_dt` per RESEARCH.md §Code Examples / Common Operation 1; Hypothesis profile `ci` with deadline=None, max_examples=200"
    - path: "tests/fakes/__init__.py"
      provides: "Package marker for Plan 00-08 fake clients"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lockfile resolution"
      pattern: "uv sync --locked succeeds"
    - from: ".pre-commit-config.yaml"
      to: ".gitleaks.toml"
      via: "gitleaks hook reads allowlist config"
      pattern: "gitleaks --config .gitleaks.toml"
    - from: "tests/conftest.py"
      to: "tests/unit/**/*.py"
      via: "shared fixtures imported by domain/settings/observability tests in later plans"
      pattern: "from tests.conftest import money, utc_dt"
---

<objective>
Establish the repo skeleton with uv-managed Python 3.12 toolchain (ruff, pyright, pytest, Hypothesis, pytest-asyncio, pytest-cov, freezegun, respx, aioresponses, testcontainers), pre-commit hooks (ruff + gitleaks + 3 grep guards), .gitignore + .env.example contracts, AGENTS.md TDD doc, and package-layout `__init__.py` stubs across `src/shortfire/{domain,settings,observability,db,clients,ingest,strategy,execution,risk,entrypoints}/` and `tests/{fakes,unit,integration}/`.

Purpose: This is the Wave 1 foundation that every other plan depends on. Plans 02-08 all start by running `uv sync` and `uv run pytest` against the artifacts this plan produces. Without it, no other Phase 0 work has anywhere to live or any way to verify itself.

Output: A fresh `git clone` followed by `uv sync` produces a green `uv run pytest` (smoke test only, no domain logic yet), `uv run ruff check`, and `uv run pyright`. `pre-commit run --all-files` exits 0. This satisfies FOUND-01, FOUND-06, OPS-08, TEST-01, TEST-02, TEST-06.
</objective>

<execution_context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/PROJECT.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/ROADMAP.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/REQUIREMENTS.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/STATE.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/CLAUDE.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-UI-SPEC.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-VALIDATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → git push | gitleaks pre-commit catches secrets before they reach the remote |
| `.env.local` (filesystem) → tracked files | .gitignore + pre-commit grep + gitleaks layered defense |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | git working tree | mitigate | gitleaks v8.24.2 pre-commit hook (D-22 layer 1) blocks commits containing recognizable secret signatures; .gitleaks.toml allowlists uv.lock + tests/fixtures + tests/fakes + .env.example to prevent false-positive bypass culture (Pitfall 6) |
| T-00-04 | Tampering of historical data | Future Alembic migrations + src/shortfire/domain/ | mitigate | `ban-naive-timestamp` pre-commit grep (D-32) forbids `TIMESTAMP[^(]` under alembic/versions/ and src/ |
| T-00-05 | Tampering / Loss of Data | Future Alembic migrations | mitigate | `ban-on-delete-cascade` pre-commit grep (D-32) forbids `ON DELETE CASCADE` under alembic/versions/ |
| T-00-10 | Tampering | src/shortfire/domain/ | mitigate | `ban-float-in-domain` pre-commit grep (D-09) forbids `: float` annotation under src/shortfire/domain/, forcing Decimal-only money on the hot path |
| T-00-09 | Tampering | Local dev vs CI parity | mitigate | `.pre-commit-config.yaml` invokes the same ruff/gitleaks tools that the CI workflow will invoke in Plan 00-08 (single source of truth) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize repo skeleton (pyproject.toml + uv.lock + package __init__s + .gitignore + .env.example + AGENTS.md + README.md)</name>
  <files>
    pyproject.toml
    uv.lock
    .gitignore
    .env.example
    AGENTS.md
    README.md
    src/shortfire/__init__.py
    src/shortfire/domain/__init__.py
    src/shortfire/settings/__init__.py
    src/shortfire/observability/__init__.py
    src/shortfire/db/__init__.py
    src/shortfire/clients/__init__.py
    src/shortfire/ingest/__init__.py
    src/shortfire/strategy/__init__.py
    src/shortfire/execution/__init__.py
    src/shortfire/risk/__init__.py
    src/shortfire/entrypoints/__init__.py
    tests/__init__.py
    tests/conftest.py
    tests/fakes/__init__.py
    tests/unit/__init__.py
    tests/integration/__init__.py
    tests/unit/test_smoke_imports.py
    tests/unit/repo_hygiene/__init__.py
    tests/unit/repo_hygiene/test_gitignore_covers_env_files.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-02, D-06, D-09, D-20, D-23)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Code Examples / Common Operation 2 (pyproject.toml skeleton)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Recommended Project Structure
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-UI-SPEC.md §`.env.example` formatting (section banner pattern, `your_<purpose>_here` placeholders, Phase 5 commented-out)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/CLAUDE.md (tech stack matrix — every dep version targeted)
  </read_first>
  <behavior>
    - Test 1: `python -c "import shortfire; import shortfire.domain; import shortfire.settings; import shortfire.observability; import shortfire.db; import shortfire.clients; import shortfire.entrypoints"` succeeds
    - Test 2: `.gitignore` contains every pattern from D-23 (assert as a `tests/unit/repo_hygiene/test_gitignore_covers_env_files.py` test reading `.gitignore` and asserting required substrings present)
    - Test 3: `pyproject.toml` declares `requires-python = ">=3.12"`, `[tool.ruff] target-version = "py312"`, `[tool.pyright] typeCheckingMode = "strict"`, `[tool.pytest.ini_options] asyncio_mode = "auto"`, `integration` marker
    - Test 4: `tests/conftest.py` exports `money` and `utc_dt` Hypothesis strategies (smoke import test)
  </behavior>
  <action>
    Run `uv init --package shortfire --python 3.12` to bootstrap, then OVERWRITE `pyproject.toml` with the full schema from RESEARCH.md §Code Examples / Common Operation 2 (pyproject skeleton). Production deps (in `[project] dependencies`): `pydantic>=2.13`, `pydantic-settings>=2.11`, `fastapi>=0.128`, `uvicorn[standard]>=0.39`, `structlog>=25.5`, `asgi-correlation-id>=4.3`, `prometheus-client>=0.25`, `sqlalchemy>=2.0.49`, `asyncpg>=0.31`, `alembic>=1.16`, `orjson>=3.11`, `tenacity>=9.1`, `aiolimiter>=1.2`, `httpx>=0.28`, `python-dotenv>=1.2`. Dev deps (in `[dependency-groups] dev`): `pytest>=8.4`, `pytest-asyncio>=1.2`, `pytest-cov>=7.1`, `hypothesis>=6.141`, `freezegun>=1.5`, `respx>=0.23`, `aioresponses>=0.7`, `testcontainers[postgres]>=4.13`, `ruff>=0.15`, `pyright>=1.1.409`, `pre-commit>=4.0`. Set `[tool.uv] default-groups = ["dev"]`. Set `[build-system] requires = ["hatchling"]`, `build-backend = "hatchling.build"`. Set `[tool.hatch.build.targets.wheel] packages = ["src/shortfire"]`. Set `[tool.pytest.ini_options] asyncio_mode = "auto"`, `markers = ["integration: requires Docker + testcontainers (slow)"]`, `addopts = ["-ra", "--strict-markers"]`, `testpaths = ["tests"]`. Set `[tool.ruff] line-length = 110`, `target-version = "py312"`. Set `[tool.ruff.lint] select = ["E", "F", "I", "B", "UP", "SIM", "ASYNC", "PERF"]`. Set `[tool.pyright] pythonVersion = "3.12"`, `typeCheckingMode = "strict"`, `include = ["src", "tests"]`, `exclude = ["**/__pycache__", "**/.venv"]`, `reportMissingTypeStubs = "warning"`. Set `[tool.coverage.run] source = ["src/shortfire"]`, `branch = true`.

    BEFORE running `uv add` for `asgi-correlation-id`, run `slopcheck install asgi-correlation-id --ecosystem pypi` and verify `[OK]` (RESEARCH.md §Package Legitimacy Audit action item). If unavailable, hand-roll correlation-id middleware (~30 lines) per RESEARCH.md §Alternatives Considered.

    Run `uv sync` to produce `uv.lock`. Verify uv resolves all 26 packages with no conflicts.

    Create the package skeleton per D-06: `src/shortfire/__init__.py` exports `__version__ = "0.1.0"`. Each of `src/shortfire/{domain,settings,observability,db,clients,entrypoints}/__init__.py` is empty (just package marker). `src/shortfire/{ingest,strategy,execution,risk}/__init__.py` are empty markers (filled in Phases 1-5). Add `tests/__init__.py`, `tests/fakes/__init__.py`, `tests/unit/__init__.py`, `tests/unit/repo_hygiene/__init__.py`, `tests/integration/__init__.py`.

    Create `tests/conftest.py` with the two shared Hypothesis strategies from RESEARCH.md §Code Examples / Common Operation 1: `money = st.decimals(min_value=Decimal("0.00000001"), max_value=Decimal("1000000000"), places=18, allow_nan=False, allow_infinity=False)` and `utc_dt = st.datetimes(min_value=datetime(2020, 1, 1), max_value=datetime(2030, 1, 1), timezones=st.just(timezone.utc))`. Register Hypothesis profile `ci` with `deadline=None, max_examples=200` (per VALIDATION.md Wave 0 Requirements). Use `from hypothesis import settings, HealthCheck; settings.register_profile("ci", deadline=None, max_examples=200, suppress_health_check=[HealthCheck.too_slow])`. Use `settings.load_profile(os.environ.get("HYPOTHESIS_PROFILE", "default"))`.

    Create `tests/unit/test_smoke_imports.py` that imports every top-level subpackage of `shortfire` (covers TEST-01 — confirms pytest + pytest-asyncio + Hypothesis are installed and importable). Per TEST-06: this same file imports `freezegun` and exercises a 1-line tz-aware freeze (`with freeze_time("2026-05-21T00:00:00Z"): assert datetime.now(timezone.utc).isoformat().startswith("2026-05-21")`) — proves freezegun installed and tz-correct usage pattern visible (Pitfall 7).

    Create `tests/unit/repo_hygiene/test_gitignore_covers_env_files.py` that reads `.gitignore` and asserts each pattern from D-23 is present (`.env`, `.env.local`, `.env.*.local`, `*.env.bak`, `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `.coverage`, `htmlcov/`, `coverage.xml`, `dist/`, `build/`, `*.egg-info/`, `.DS_Store`, `.vscode/`, `.idea/`). One test per pattern (parametrize).

    Create `.gitignore` with all D-23 patterns.

    Create `.env.example` per UI-SPEC.md §`.env.example` formatting — 60-column section banners (`# ===...===`), single-line comment per variable, `your_<purpose>_here` placeholders. Sections: COMMON (ENV, LOG_LEVEL), DATABASE (DATABASE_URL), DATA PLATFORM SERVICE (MEXC__READ_KEY, MEXC__READ_SECRET, COINGLASS__API_KEY, COINGECKO__API_KEY), STRATEGY ENGINE SERVICE (MEXC_TRADE__TRADE_KEY and MEXC_TRADE__TRADE_SECRET COMMENTED OUT per D-20), OPTIONAL (TELEGRAM__BOT_TOKEN commented).

    Create `AGENTS.md` with sections (per TEST-02 and Pitfall 4): (1) "Project workflow: GSD" pointing at /Users/mishanikhinkirtill/Desktop/ShortFIRE/CLAUDE.md "GSD Workflow Enforcement" section; (2) "TDD discipline" — every module starts with a failing test; tests use Arrange-Act-Assert; Hypothesis property tests are mandatory for domain invariants (D-15); (3) "Railway healthchecks are NOT liveness probes" — they run only at deploy time (Pitfall 4); (4) "Cross-service env var sharing is forbidden" — use Railway service-scoped Variables only (security domain in 00-RESEARCH.md); (5) "Do not call `repr(settings)`" — use `safe_summary()` (D-21).

    Create `README.md` with a short Quickstart: `git clone`, `uv sync`, `pre-commit install`, `uv run pytest -m "not integration"`. Add a "Destructive — local data loss" banner for `docker compose down -v` per UI-SPEC.md §Destructive operations.
  </action>
  <verify>
    <automated>uv sync --locked &amp;&amp; uv run python -c "import shortfire; assert shortfire.__version__ == '0.1.0'" &amp;&amp; uv run pytest -m "not integration" -q tests/unit/test_smoke_imports.py tests/unit/repo_hygiene/test_gitignore_covers_env_files.py</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` declares `requires-python = ">=3.12"` and contains exact `[tool.pytest.ini_options]`, `[tool.ruff]`, `[tool.pyright]`, `[tool.coverage.run]` blocks (assert via `grep -F 'asyncio_mode = "auto"' pyproject.toml`, `grep -F 'typeCheckingMode = "strict"' pyproject.toml`, `grep -F 'target-version = "py312"' pyproject.toml`)
    - `uv.lock` exists and `uv sync --locked` exits 0 (no resolution drift)
    - `python -c "import shortfire, shortfire.domain, shortfire.settings, shortfire.observability, shortfire.db, shortfire.clients, shortfire.entrypoints"` exits 0
    - `tests/unit/test_smoke_imports.py` passes (imports pytest, pytest_asyncio, hypothesis, freezegun, respx, aioresponses; exercises tz-aware freezegun usage)
    - `tests/unit/repo_hygiene/test_gitignore_covers_env_files.py` passes (all 19 D-23 patterns asserted present)
    - `tests/conftest.py` contains `money` and `utc_dt` Hypothesis strategies importable via `from tests.conftest import money, utc_dt`
    - `.env.example` contains the 5 section banners exactly as specified in UI-SPEC.md and uses `your_<purpose>_here` placeholders (assert via grep for `# ===` lines + `your_mexc_read_key_here`)
    - `MEXC_TRADE__TRADE_KEY` and `MEXC_TRADE__TRADE_SECRET` in `.env.example` are COMMENTED-OUT (lines start with `#`)
    - `AGENTS.md` contains sections "TDD discipline", "Railway healthchecks are NOT liveness probes", "safe_summary" (grep for each heading)
  </acceptance_criteria>
  <done>Repo skeleton in place: uv-managed venv, locked deps, package layout per D-06, .gitignore + .env.example + AGENTS.md per UI-SPEC and D-20/D-23, smoke tests + repo-hygiene tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install pre-commit hooks (ruff + gitleaks + 3 grep guards) and verify all 4 layered guards trigger correctly</name>
  <files>
    .pre-commit-config.yaml
    .gitleaks.toml
    tests/unit/repo_hygiene/test_precommit_guards.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-22, D-23, D-32, D-09)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pattern 8 (4-Layer Secret-Scan Defense — full YAML)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pitfall 6 (gitleaks false positives on uv.lock)
    .gitignore (just created)
    pyproject.toml (just created — pre-commit hook needs to know about ruff config)
  </read_first>
  <behavior>
    - Test 1: `pre-commit run gitleaks --all-files` exits 0 against the current tree (no secrets present)
    - Test 2: `pre-commit run ruff-format --all-files` exits 0 (files formatted by previous task)
    - Test 3: `pre-commit run ruff --all-files` exits 0
    - Test 4: A scratch file containing `: float` annotation under src/shortfire/domain/ would trip `ban-float-in-domain` (assert via subprocess invocation that runs the hook against a temp directory; use `pytest tmp_path` + manual hook invocation)
    - Test 5: A scratch file containing `TIMESTAMP DEFAULT NOW()` (no parentheses around `(timezone=True)`) under alembic/versions/ would trip `ban-naive-timestamp`
    - Test 6: A scratch file containing `ON DELETE CASCADE` under alembic/versions/ would trip `ban-on-delete-cascade`
  </behavior>
  <action>
    Create `.pre-commit-config.yaml` with the EXACT hooks from RESEARCH.md §Pattern 8 Layer 1, in this order: (1) `https://github.com/gitleaks/gitleaks` rev `v8.24.2` id `gitleaks`; (2) `https://github.com/astral-sh/ruff-pre-commit` rev `v0.15.13` ids `ruff-format` then `ruff` with `args: [--fix, --exit-non-zero-on-fix]`; (3) local hooks: `ban-naive-timestamp`, `ban-on-delete-cascade`, `ban-float-in-domain` per the bash entries in RESEARCH.md §Pattern 8. Each local hook uses `language: system`, `pass_filenames: false`, and `files:` regex scoping the check.

    Create `.gitleaks.toml` per RESEARCH.md §Pattern 8 Layer 1 config (Pitfall 6): `title = "shortfire gitleaks config"`, `[extend] useDefault = true`, single `[[allowlists]]` block with `paths = ['''uv\.lock''', '''tests/fixtures/.*''', '''tests/fakes/.*''', '''\.env\.example''']`.

    Run `uv run pre-commit install` to wire `.git/hooks/pre-commit`.

    Run `uv run pre-commit run --all-files` against the current working tree. Must exit 0 (no real secrets, all files formatted).

    Create `tests/unit/repo_hygiene/test_precommit_guards.py` with TDD-style tests:
    - `test_ban_float_in_domain_grep`: write a temp file at `src/shortfire/domain/scratch_violator.py` containing `value: float = 0.0`, run the equivalent grep command (`grep -nE ": float\b" src/shortfire/domain/scratch_violator.py`), assert exit code 0 (match found). Then delete the temp file and assert grep on the now-empty domain dir returns nothing. This tests the GREP LOGIC, not the hook wrapper.
    - `test_ban_naive_timestamp_grep`: temp file `alembic/versions/scratch.py` containing `TIMESTAMP DEFAULT`, grep `TIMESTAMP[^(]` finds it; clean.
    - `test_ban_on_delete_cascade_grep`: temp file `alembic/versions/scratch.py` containing `ON DELETE CASCADE`, grep `ON DELETE CASCADE` finds it; clean.
    - `test_gitleaks_config_valid`: assert `.gitleaks.toml` parses as TOML and contains an `[[allowlists]]` table with `paths` listing `uv\.lock` (use `tomllib`).

    Use `pytest tmp_path` + `monkeypatch` to write the scratch files into a temp project root, NOT the real working tree.
  </action>
  <verify>
    <automated>uv run pre-commit install &amp;&amp; uv run pre-commit run --all-files &amp;&amp; uv run pytest -m "not integration" -q tests/unit/repo_hygiene/test_precommit_guards.py</automated>
  </verify>
  <acceptance_criteria>
    - `.pre-commit-config.yaml` contains hooks in this order: `gitleaks` (rev `v8.24.2`), `ruff-format`, `ruff`, `ban-naive-timestamp`, `ban-on-delete-cascade`, `ban-float-in-domain`. Verify via `grep -c "id: " .pre-commit-config.yaml` returns 6.
    - `.gitleaks.toml` parses as valid TOML and contains `[extend] useDefault = true` plus `[[allowlists]]` with `paths` including `uv\.lock`, `tests/fixtures/.*`, `tests/fakes/.*`, `\.env\.example` (verify via `python -c "import tomllib; d = tomllib.load(open('.gitleaks.toml', 'rb')); assert d['extend']['useDefault']; assert any('uv' in p for p in d['allowlists'][0]['paths'])"`)
    - `uv run pre-commit run --all-files` exits 0 (no real secrets, all files properly formatted)
    - All tests in `tests/unit/repo_hygiene/test_precommit_guards.py` pass: float-in-domain grep, naive-timestamp grep, on-delete-cascade grep, gitleaks-config-valid
    - `.git/hooks/pre-commit` exists (left behind by `pre-commit install`)
  </acceptance_criteria>
  <done>Pre-commit hooks installed and verified end-to-end: ruff format/lint, gitleaks, and 3 grep guards (TIMESTAMP-without-tz, ON DELETE CASCADE, : float in domain) all green on current tree, all logic covered by unit tests in `tests/unit/repo_hygiene/test_precommit_guards.py`.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `uv sync --locked` exits 0 (lockfile reproducible)
2. `uv run pytest -m "not integration" -q` exits 0 (smoke tests + repo-hygiene tests green)
3. `uv run ruff format --check .` exits 0
4. `uv run ruff check .` exits 0
5. `uv run pyright` exits 0 in strict mode (no production code yet — passes trivially, but proves wiring)
6. `uv run pre-commit run --all-files` exits 0
7. Inspect `pyproject.toml` for the locked tool config blocks (pytest, ruff, pyright, coverage).
8. Inspect `.env.example` against UI-SPEC.md §`.env.example` formatting — section banners, comment-per-var, `your_<purpose>_here` placeholders, Phase 5 trade keys commented-out.
9. Inspect `AGENTS.md` for TDD discipline, Railway-healthcheck-pitfall, safe_summary, cross-service-env-var-sharing-forbidden sections.
</verification>

<success_criteria>
- Fresh `git clone` + `uv sync` + `uv run pytest -m "not integration"` exits 0 against this commit (FOUND-01 partial — wave-1 wiring complete; FOUND-04 domain tests land in Plan 00-02)
- `.gitignore` covers all D-23 patterns (FOUND-06)
- `.env.example` exists with correct schema and section banners (UI-SPEC + D-20)
- Pre-commit hooks installed and verified: ruff format + ruff lint + gitleaks + 3 grep guards (OPS-08, D-22 layer 1, D-32)
- AGENTS.md exists and documents TDD discipline (TEST-02)
- Package skeleton in place per D-06: 10 subpackages under `src/shortfire/`, plus `tests/{unit,integration,fakes}/`
- All tests in `tests/unit/test_smoke_imports.py` and `tests/unit/repo_hygiene/` pass (TEST-01, TEST-06 — freezegun used in smoke test)
- pyright strict mode runs against `src/` + `tests/` without errors
</success_criteria>

<output>
Create `.planning/phases/00-foundation/00-01-SUMMARY.md` when done, documenting:
- Final `pyproject.toml` dependency versions actually pinned in `uv.lock`
- Any deviations from the version targets in RESEARCH.md §Standard Stack
- Whether `slopcheck install asgi-correlation-id --ecosystem pypi` returned `[OK]` (per RESEARCH.md §Package Legitimacy Audit action item)
- The exact pre-commit version (`pre-commit --version`)
- Confirmation that `pre-commit run --all-files` exits 0 against the produced tree
</output>
