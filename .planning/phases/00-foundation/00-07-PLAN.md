---
phase: 0
plan: 07
type: execute
wave: 3
depends_on:
  - 00-03
  - 00-04
  - 00-05
files_modified:
  - Dockerfile
  - .dockerignore
  - railway.toml
  - tests/unit/repo_hygiene/test_railway_toml.py
  - tests/unit/repo_hygiene/test_dockerfile_image.py
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-07
tags:
  - railway
  - docker
  - deploy
  - timescaledb
  - infrastructure
user_setup:
  - service: railway
    why: "Railway is the only deployment target (PROJECT.md, D-01..D-05); the user must create a project, provision the TimescaleDB marketplace template, and connect the GitHub repo for auto-deploy"
    dashboard_config:
      - task: "Create Railway project from GitHub repo"
        location: "Railway Dashboard -> New Project -> Deploy from GitHub repo (pointing at the freshly-pushed shortfire repo)"
      - task: "Provision the TimescaleDB marketplace template"
        location: "Railway Dashboard -> Add Service -> Add a Database -> TimescaleDB (image timescale/timescaledb:2.18.0-pg16); name the service 'Postgres'"
      - task: "Create 3 app services from the same repo, with the same Dockerfile, different startCommand"
        location: "Railway Dashboard -> +New -> Empty Service x3 -> name them data-platform, strategy-engine, dashboard. For each: Settings -> Source -> GitHub repo (use the same repo). Settings -> Deploy -> override startCommand."
      - task: "On each app service, set DATABASE_URL = ${{Postgres.DATABASE_URL}} reference variable (D-24)"
        location: "Railway Dashboard -> each service -> Variables -> add DATABASE_URL with value ${{Postgres.DATABASE_URL}}"
      - task: "On data-platform service ONLY: set the preDeployCommand = 'uv run alembic upgrade head' (RESEARCH.md Open Question 1 recommendation; OPS-07)"
        location: "Railway Dashboard -> data-platform -> Settings -> Deploy -> preDeployCommand"
      - task: "Set sleepApplication=true on strategy-engine and dashboard services; leave data-platform always-on (D-04)"
        location: "Railway Dashboard -> {strategy-engine,dashboard} -> Settings -> Serverless -> Application Sleeping = true"
      - task: "Set ENV=production on all 3 app services"
        location: "Railway Dashboard -> each service -> Variables -> ENV=production"
      - task: "Set LOG_LEVEL=INFO on all 3 app services (UI-SPEC §Log levels)"
        location: "Railway Dashboard -> each service -> Variables -> LOG_LEVEL=INFO"
      - task: "Enable GitHub Push Protection and Secret Scanning in the GitHub repo (D-22 layers 3 + 4)"
        location: "GitHub repo -> Settings -> Code security and analysis -> enable Push Protection and Secret scanning"
      - task: "Enable BASIC branch protection on main: require PR review + block force-push + require linear history (OPS-01). Defer the 'Require status checks: test job' rule until AFTER Plan 00-08's first green CI run."
        location: "GitHub repo -> Settings -> Branches -> Add rule for main"
      - task: "[Post-00-08 follow-up] After Plan 00-08 ships its ci.yml AND the workflow has run green AT LEAST ONCE on a PR, add 'Require status checks to pass before merging: test' to the main branch protection rule."
        location: "GitHub repo -> Settings -> Branches -> Edit rule for main -> enable 'Require status checks to pass before merging' -> add 'test' as required check"
    env_vars:
      - name: DATABASE_URL
        source: "Railway Postgres reference variable: ${{Postgres.DATABASE_URL}}"
        scope: "all 3 app services"
      - name: ENV
        source: "Set manually to 'production' in Railway dashboard"
        scope: "all 3 app services"
      - name: LOG_LEVEL
        source: "Set manually to 'INFO' in Railway dashboard"
        scope: "all 3 app services"
must_haves:
  truths:
    - "D-01 (3-service topology): Railway project has 3 app services with final v1 names — data-platform, strategy-engine, dashboard — all scaffolded in Phase 0 (risk-guard deferred to Phase 5)"
    - "D-34 (CI gates + auto-deploy + pre-deploy assertion): Railway auto-deploys on green main; branch protection on main blocks force-push (Plan 00-08 adds the test-job status check post-Plan-00-08); each entrypoint's main() instantiates its *Settings class as a pre-deploy fail-fast assertion on missing env vars"
    - "A single Dockerfile builds the same image for all 3 services (D-03)"
    - ".dockerignore excludes .venv, __pycache__, .git, .pytest_cache, .mypy_cache, tests/, .planning/, *.md"
    - "railway.toml declares [build] dockerfilePath = 'Dockerfile' with NO watchPatterns (D-05) and [deploy] preDeployCommand for migrations (RESEARCH.md Open Question 1)"
    - "The Railway project has 4 services: Postgres (TimescaleDB marketplace template), data-platform, strategy-engine, dashboard"
    - "DATABASE_URL is wired via ${{Postgres.DATABASE_URL}} reference variable on all 3 app services (D-24)"
    - "data-platform service is always-on; strategy-engine and dashboard have sleepApplication=true (D-04)"
    - "After auto-deploy: GET https://data-platform.up.railway.app/health returns 200 with the UI-SPEC 6-field JSON; GET /metrics returns Prometheus text exposition"
    - "alembic upgrade head runs in data-platform's preDeployCommand on each deploy (RESEARCH.md Open Question 1 recommendation)"
    - "Phase-0 step of OPS-01: GitHub Push Protection + Secret Scanning are enabled, AND main branch protection blocks force-push + requires linear history (basic protection, no status-check requirement yet)"
    - "Post-00-08 step of OPS-01: after Plan 00-08's ci.yml ships and runs green at least once, the 'Require status checks: test' rule is added to the main branch protection (cross-plan coordination)"
  artifacts:
    - path: "Dockerfile"
      provides: "Multistage Python 3.12 image with uv-managed deps + the shortfire package + entrypoint command via $START_CMD or PORT-aware uvicorn invocation"
    - path: ".dockerignore"
      provides: "Excludes dev artifacts so image stays slim"
    - path: "railway.toml"
      provides: "build.dockerfilePath = 'Dockerfile', no watchPatterns; [deploy] preDeployCommand for the data-platform-specific migration step + healthcheckPath + restartPolicy"
  key_links:
    - from: "Railway Postgres service"
      to: "data-platform, strategy-engine, dashboard"
      via: "${{Postgres.DATABASE_URL}} reference variable (D-24)"
      pattern: "DATABASE_URL = ${{Postgres.DATABASE_URL}}"
    - from: "GitHub push to main"
      to: "Railway auto-deploy"
      via: "GitHub integration on each Railway service"
      pattern: "every commit triggers redeploy of all 3 services (D-05 — no watchPatterns)"
---

<objective>
Provision the Railway 3-service deployment (data-platform, strategy-engine, dashboard) + TimescaleDB marketplace service, wire DATABASE_URL via reference variable, configure preDeployCommand for migrations, and verify auto-deploy works end-to-end. Per D-01..D-05 + RESEARCH.md Pattern 4. This plan combines static code config (Dockerfile + railway.toml + .dockerignore) with Railway dashboard configuration (human-action checkpoint, since several setup steps are dashboard-only with no Railway CLI equivalents for first-time project creation).

Purpose: ROADMAP Phase 0 success #2 ("pushing to main triggers GitHub Actions; a green build auto-deploys to Railway and the service answers /metrics with Prometheus output and a structured-JSON /health line"). FOUND-02 (Railway project provisioned with TimescaleDB), OPS-02 (auto-deploy from main), and OPS-07 (migration discipline — Alembic in deploy step) all hinge on this plan.

**Branch-protection coordination with Plan 00-08:** This plan establishes BASIC branch protection (require PR review, block force-push, require linear history) but does NOT enable the "Require status checks: test job" rule, because Plan 00-08's `ci.yml` is authored in a parallel wave-3 plan — the status check would not yet exist. The Phase-0 step here covers the protections that do not depend on `ci.yml`. After Plan 00-08 ships its workflow and the first PR exercises it green, the user adds the status-check requirement as a Post-00-08 follow-up (see user_setup tasks).

Output: 3 source/config files (Dockerfile, .dockerignore, railway.toml) + 2 unit tests asserting railway.toml + Dockerfile shape + 1 human-action checkpoint covering all dashboard-only steps + 1 verification checkpoint after first deploy.
</objective>

<execution_context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-UI-SPEC.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-VALIDATION.md
</context>

<interfaces>
RESEARCH.md Pattern 4 (Railway 3-Service Shape via railway.toml) provides the canonical example. Key facts (RESEARCH.md §Pattern 4 + Open Question 1):

- `railway.toml` schema: top-level `[build]` and `[deploy]` keys per docs.railway.com/reference/config-as-code
- `[build] builder = "DOCKERFILE"`, `dockerfilePath = "Dockerfile"`
- NO `watchPatterns` in Phase 0 per D-05
- `[deploy] preDeployCommand = "uv run alembic upgrade head"` ONLY on the data-platform service (RESEARCH.md Open Question 1 recommendation)
- `startCommand` per service: `uvicorn shortfire.entrypoints.{data_platform|strategy_engine|dashboard}:app --host 0.0.0.0 --port $PORT`
- `healthcheckPath = "/health"`, `healthcheckTimeout = 60`, `restartPolicyType = "ON_FAILURE"`, `restartPolicyMaxRetries = 5`
- `sleepApplication = true` on strategy-engine + dashboard; false (default) on data-platform (D-04)
- Reference variable `DATABASE_URL = ${{Postgres.DATABASE_URL}}` set in dashboard (D-24)
- TimescaleDB image: `timescale/timescaledb:2.18.0-pg16` (D-24 — Railway marketplace template)

Railway healthchecks run ONLY at deploy time (Pitfall 4 — already documented in AGENTS.md from Plan 00-01).
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub push -> Railway deploy | Branch protection requires CI green; Railway only deploys after CI passes (OPS-01) |
| Railway dashboard variables -> service process | Per-service variables; cross-service sharing is FORBIDDEN by convention (AGENTS.md from Plan 00-01) |
| Production env -> service | ENV=production set in Railway; pydantic-settings then skips .env.local loading |
| Railway preDeployCommand -> Postgres | alembic upgrade head runs with the same DATABASE_URL reference (D-24); idempotent per D-31 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02 | Elevation of Privilege | Railway env vars on data-platform | mitigate | Per-service Variables in Railway dashboard; MEXC_TRADE__* MUST only be set on strategy-engine (Phase 5). data-platform calls assert_no_trade_env_leaked() at startup (Plan 00-05) to crash the service if a misroute happens. AGENTS.md documents the rule (Plan 00-01). |
| T-00-01 | Information Disclosure | Dockerfile + image | mitigate | .dockerignore excludes .env, .env.local, .planning/, tests/. Dockerfile does NOT COPY .env*. Image runs as a non-root user (UID 1000). |
| T-00-09 | Tampering | preDeployCommand vs startCommand chaining | mitigate | Use preDeployCommand (RESEARCH.md Open Question 1 recommendation) to separate migration concern from start; future-proof for >1 replica scaling. |
| T-00-04 | Tampering of historical data | Production deploys | mitigate | All TIMESTAMP columns are TIMESTAMP(timezone=True) (D-32 grep-ban verified in pre-commit). |
| T-00-05 | Loss of Data | Future migrations | mitigate | ban-on-delete-cascade pre-commit guard from Plan 00-01 + CI re-run (Plan 00-08) prevent regressions. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author Dockerfile + .dockerignore + railway.toml; add static-shape unit tests</name>
  <files>
    Dockerfile
    .dockerignore
    railway.toml
    tests/unit/repo_hygiene/test_railway_toml.py
    tests/unit/repo_hygiene/test_dockerfile_image.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-02, D-03, D-04, D-05, D-24, D-34)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pattern 4 (Railway 3-Service Shape via railway.toml — full example)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Open Questions Q1 (Recommendation: preDeployCommand = ['uv run alembic upgrade head'] for data-platform)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pitfall 4 (Railway healthcheck is not a liveness probe)
  </read_first>
  <action>
    Create `Dockerfile` — multistage Python 3.12 image (lookup current uv-aware best practice via Context7 if needed; default if unknown):
    ```dockerfile
    # syntax=docker/dockerfile:1.7
    FROM python:3.12-slim AS base
    ENV PYTHONUNBUFFERED=1 \
        PYTHONDONTWRITEBYTECODE=1 \
        UV_COMPILE_BYTECODE=1 \
        UV_LINK_MODE=copy \
        UV_PROJECT_ENVIRONMENT=/app/.venv \
        PATH="/app/.venv/bin:$PATH"

    FROM base AS deps
    # Install uv (pinned to the major used by setup-uv@v8 — 0.11.x)
    ADD https://astral.sh/uv/install.sh /uv-install.sh
    RUN sh /uv-install.sh && rm /uv-install.sh
    ENV PATH="/root/.local/bin:$PATH"

    WORKDIR /app
    COPY pyproject.toml uv.lock ./
    # Production deps only (NOT dev group) — image stays slim
    RUN uv sync --locked --no-dev

    FROM base AS runtime
    # Non-root user
    RUN groupadd --system app && useradd --system --gid app --uid 1000 app
    WORKDIR /app
    COPY --from=deps /app/.venv /app/.venv
    COPY src/shortfire /app/src/shortfire
    COPY alembic /app/alembic
    COPY alembic.ini /app/
    USER app
    EXPOSE 8000

    # Default start (overridden per service in Railway).
    # `$PORT` is provided by Railway at runtime; uvicorn reads it.
    CMD ["sh", "-c", "uvicorn shortfire.entrypoints.data_platform:app --host 0.0.0.0 --port ${PORT:-8000}"]
    ```

    Create `.dockerignore`:
    ```
    .git
    .github
    .pytest_cache
    .mypy_cache
    .ruff_cache
    __pycache__
    *.pyc
    .venv
    venv
    .env
    .env.local
    .env.*.local
    tests
    .planning
    *.md
    !README.md
    docker-compose.yml
    .coverage
    htmlcov
    coverage.xml
    dist
    build
    *.egg-info
    .DS_Store
    .vscode
    .idea
    ```

    Create `railway.toml` per RESEARCH.md §Pattern 4 + Open Question 1:
    ```toml
    # railway.toml — single config; per-service startCommand + sleepApplication overridden in Railway dashboard.
    $schema = "https://railway.com/railway.schema.json"

    [build]
    builder = "DOCKERFILE"
    dockerfilePath = "Dockerfile"
    # D-05: no watchPatterns in Phase 0 — every commit redeploys all 3 services.

    [deploy]
    # Data-platform-specific defaults; strategy-engine and dashboard override startCommand + sleepApplication in dashboard
    preDeployCommand = ["uv run alembic upgrade head"]
    startCommand = "uvicorn shortfire.entrypoints.data_platform:app --host 0.0.0.0 --port $PORT"
    healthcheckPath = "/health"
    healthcheckTimeout = 60
    restartPolicyType = "ON_FAILURE"
    restartPolicyMaxRetries = 5
    # sleepApplication NOT set for data-platform (always-on per D-04)
    ```

    Document in `railway.toml` comments (and AGENTS.md from Plan 00-01 — append if needed):
    - For strategy-engine: set in dashboard `startCommand = "uvicorn shortfire.entrypoints.strategy_engine:app --host 0.0.0.0 --port $PORT"`, `sleepApplication = true`
    - For dashboard: set in dashboard `startCommand = "uvicorn shortfire.entrypoints.dashboard:app --host 0.0.0.0 --port $PORT"`, `sleepApplication = true`
    - For ALL 3 services: Variables -> `DATABASE_URL = ${{Postgres.DATABASE_URL}}` (D-24), `ENV = production`, `LOG_LEVEL = INFO`

    Create `tests/unit/repo_hygiene/test_railway_toml.py`:
    - Parse `railway.toml` via `tomllib`; assert top-level `[build].builder == "DOCKERFILE"`, `[build].dockerfilePath == "Dockerfile"`, `"watchPatterns" not in toml["build"]` (D-05).
    - Assert `[deploy].preDeployCommand == ["uv run alembic upgrade head"]` (Open Question 1).
    - Assert `[deploy].startCommand` references `shortfire.entrypoints.data_platform:app`.
    - Assert `[deploy].healthcheckPath == "/health"`.
    - Assert `[deploy].restartPolicyType == "ON_FAILURE"` and `restartPolicyMaxRetries == 5`.

    Create `tests/unit/repo_hygiene/test_dockerfile_image.py`:
    - Read Dockerfile as text; assert contains `FROM python:3.12-slim`, `USER app` (non-root), `EXPOSE 8000`, `uv sync --locked --no-dev` (deps stage skips dev group), and the entrypoint CMD references `shortfire.entrypoints.data_platform:app`.
    - Read .dockerignore as text; assert excludes `.env`, `.env.local`, `tests/`, `.planning/`.
  </action>
  <verify>
    <automated>uv run python -c "import tomllib; doc = tomllib.load(open('railway.toml', 'rb')); assert doc['build']['builder'] == 'DOCKERFILE'; assert 'watchPatterns' not in doc.get('build', {}); assert doc['deploy']['preDeployCommand'] == ['uv run alembic upgrade head']; assert doc['deploy']['healthcheckPath'] == '/health'" &amp;&amp; uv run pytest -m "not integration" -q tests/unit/repo_hygiene/test_railway_toml.py tests/unit/repo_hygiene/test_dockerfile_image.py</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` exists; contains `FROM python:3.12-slim`, `uv sync --locked --no-dev`, `USER app`, `EXPOSE 8000`, default CMD invoking `shortfire.entrypoints.data_platform:app`
    - `.dockerignore` excludes `.env`, `.env.local`, `tests/`, `.planning/`, dev caches (verify via grep)
    - `railway.toml` parses as valid TOML; `[build].builder == "DOCKERFILE"`; no `watchPatterns` key (D-05); `[deploy].preDeployCommand == ["uv run alembic upgrade head"]` (Open Question 1); `[deploy].startCommand` references `shortfire.entrypoints.data_platform:app`; `[deploy].healthcheckPath == "/health"`
    - Both unit tests pass: `uv run pytest -m "not integration" -q tests/unit/repo_hygiene/test_railway_toml.py tests/unit/repo_hygiene/test_dockerfile_image.py`
    - Optional local Docker build smoke (if docker available): `docker build -t shortfire-test .` exits 0 (proves Dockerfile is well-formed; do not push)
  </acceptance_criteria>
  <done>Dockerfile + .dockerignore + railway.toml in place. railway.toml configures the data-platform service shape (preDeployCommand for migrations, healthcheck, restartPolicy, no watchPatterns). Per-service overrides for strategy-engine and dashboard documented in railway.toml comments + AGENTS.md, set manually in the dashboard in the next checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [Railway dashboard setup] Provision project + 4 services + reference variables + per-service overrides + Phase-0 branch protection (Post-00-08 follow-up to attach status-check requirement)</name>
  <files>
    (no source files — Railway dashboard + GitHub repo settings)
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-01, D-03, D-04, D-24)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pattern 4
    railway.toml (Task 1)
    AGENTS.md (Plan 00-01) — confirm the per-service overrides + Railway healthcheck pitfall are documented
  </read_first>
  <what-built>
    A Railway project with 4 services (Postgres TimescaleDB + 3 app services) wired for auto-deploy on `git push origin main`, plus Phase-0 branch protection on the GitHub repo. The "Require status checks: test" rule is deliberately DEFERRED to a Post-00-08 follow-up step to avoid the chicken-and-egg condition (the `test` check does not exist until Plan 00-08 lands its ci.yml and runs at least one green workflow on a PR).
  </what-built>
  <action>
    [BLOCKING — this is genuinely manual; Railway's first-time project creation is dashboard-only. The Railway MCP server can speed up SOME steps (variables, service config) once the project exists, but the project + service creation flow still requires the dashboard.]

    Per CONTEXT.md and RESEARCH.md Pattern 4, perform these dashboard steps:

    1. **Create the GitHub repo** (if not done): `gh repo create shortfire --public --source=. --remote=origin --push` (or `--private` per user's preference). Verify `git push -u origin main` works.

    2. **GitHub branch protection — Phase-0 step (basic protection only)** (OPS-01):
       - Repo Settings -> Branches -> Add rule for `main`
       - Require pull request before merging? -> ENABLE (recommended for solo to enforce the PR-based CI flow once 00-08 ships)
       - Require linear history -> ENABLE (matches "commit -> push -> deploy" PROJECT.md discipline; no merge commits)
       - Block force pushes -> ENABLE
       - Restrict who can push to matching branches -> recommended but optional for solo
       - **DO NOT YET enable "Require status checks to pass before merging"** — the `test` status check from Plan 00-08's ci.yml does not exist yet. This is the chicken-and-egg avoidance: enabling the status-check requirement before the workflow has ever run on a PR would block the very PR that introduces the workflow.

    3. **GitHub Push Protection + Secret Scanning** (D-22 layers 3 + 4):
       - Repo Settings -> Code security and analysis
       - Enable: Secret scanning, Push protection

    4. **Railway project**:
       - Railway Dashboard -> New Project -> Deploy from GitHub repo -> select the shortfire repo
       - This creates one initial app service. RENAME this initial service to `data-platform`.

    5. **TimescaleDB Postgres marketplace service** (D-24):
       - Railway Dashboard -> the new project -> +New -> Database -> Add a Database -> TimescaleDB
       - Name the service `Postgres` (capital P — Railway's default; reference variables use this name)
       - Verify the image: should be `timescale/timescaledb:2.18.0-pg16` (D-24)

    6. **Create the other two app services** (D-01):
       - +New -> Empty Service -> Settings -> Source -> Connect Repo -> select the same repo. Name it `strategy-engine`.
       - Repeat for `dashboard`.

    7. **Per-service configuration** (D-03, D-04):
       For EACH of the 3 app services, in Railway Dashboard:
       - **Variables** tab:
         - `DATABASE_URL = ${{Postgres.DATABASE_URL}}` (D-24 reference variable)
         - `ENV = production`
         - `LOG_LEVEL = INFO`
         - `SERVICE_NAME = data-platform` / `strategy-engine` / `dashboard` (matches each entrypoint's hard-coded default — redundant but harmless)
       - **Settings -> Deploy -> Start Command** override (the data-platform service already inherits railway.toml's startCommand; the other two must override):
         - data-platform: leave inheriting from railway.toml (`uvicorn shortfire.entrypoints.data_platform:app --host 0.0.0.0 --port $PORT`)
         - strategy-engine: `uvicorn shortfire.entrypoints.strategy_engine:app --host 0.0.0.0 --port $PORT`
         - dashboard: `uvicorn shortfire.entrypoints.dashboard:app --host 0.0.0.0 --port $PORT`
       - **Settings -> Deploy -> Pre-Deploy Command** override (D-34, OPS-07):
         - data-platform: inherits railway.toml's `["uv run alembic upgrade head"]` — VERIFY in dashboard
         - strategy-engine: EMPTY (no migrations from this service)
         - dashboard: EMPTY
       - **Settings -> Serverless -> Application Sleeping** (D-04):
         - data-platform: `false` (always-on)
         - strategy-engine: `true`
         - dashboard: `true`

    8. **First deploy verification** (FOUND-02):
       - Push a no-op commit: `git commit --allow-empty -m "chore: bootstrap Railway deploy" && git push origin main`
       - Wait ~3-5 min for the build + deploy.
       - Visit Railway dashboard -> data-platform -> Deployments -> verify the latest deploy is GREEN.
       - Visit Railway dashboard -> data-platform -> Settings -> Networking -> Generate Domain. Save the URL (e.g., `https://data-platform-production-XXXX.up.railway.app`).
       - Verify: `curl -s https://<data-platform-url>/health | jq .` returns the 6-field JSON (correlation_id, env, service_name, status, ts, version) with `status=ok`, `env=production`, `service_name=data-platform`.
       - Verify: `curl -s https://<data-platform-url>/metrics | head -20` returns Prometheus text exposition with `shortfire_data_platform_*` metric families.
       - Repeat for strategy-engine and dashboard (after their first inbound request to wake them, per Pitfall 4 — cold start ~2-3s).

    9. **[POST-00-08 FOLLOW-UP — execute AFTER Plan 00-08 ships ci.yml AND the workflow has run green on at least one PR]** (OPS-01 completion):
       - This step CANNOT execute during Plan 00-07's initial run because Plan 00-08 is in the same wave (wave 3) and its ci.yml may not yet exist at the time this checkpoint resolves.
       - Once Plan 00-08 ships its `.github/workflows/ci.yml` AND a PR has run the `test` workflow green at least once, return to: GitHub repo Settings -> Branches -> Edit rule for `main`.
       - Enable: "Require status checks to pass before merging"
       - In the status-check search box, add: `test` (the job name from Plan 00-08's ci.yml)
       - Also enable: "Require branches to be up to date before merging" (recommended; ensures PRs are tested against the latest main)
       - Save the rule.
       - From this point on, no PR can merge to main unless the `test` workflow run for the PR head SHA is green. This completes OPS-01 status-check enforcement.
       - Plan 00-08's SUMMARY.md MUST note whether this Post-00-08 step has been completed (or queued for the user). If queued, list it as an open follow-up so it does not get forgotten.
  </action>
  <how-to-verify>
    1. `gh repo view --json url -q .url` returns the GitHub repo URL.
    2. `git push origin main` succeeds; GitHub Actions CI from Plan 00-08 runs green (note: the first push happens before Plan 00-08's ci.yml exists, so CI is N/A on that initial push; CI starts running after Plan 00-08 lands its workflow).
    3. Railway Dashboard shows 4 services: Postgres (TimescaleDB), data-platform, strategy-engine, dashboard, all on the same project.
    4. Each app service shows `DATABASE_URL = ${{Postgres.DATABASE_URL}}` in Variables.
    5. data-platform shows preDeployCommand = `uv run alembic upgrade head` in Settings -> Deploy; the strategy-engine and dashboard show no preDeployCommand.
    6. data-platform sleepApplication = false; strategy-engine sleepApplication = true; dashboard sleepApplication = true.
    7. Latest deploy on data-platform is GREEN (Railway dashboard shows green checkmark in Deployments tab).
    8. `curl https://<data-platform-url>/health` returns 200 + JSON body matching the UI-SPEC §`GET /health` schema (6 fields, sorted, ts ends with Z, status=ok).
    9. `curl https://<data-platform-url>/metrics` returns 200 + `text/plain; version=0.0.4; charset=utf-8` body containing `shortfire_data_platform_http_requests_total`, `shortfire_data_platform_http_request_duration_seconds`, `shortfire_data_platform_service_event_emitted_total`, `shortfire_data_platform_build_info` metric families.
    10. `curl https://<strategy-engine-url>/health` returns 200 with `service_name=strategy-engine` (first request may take ~2-3s cold start per Pitfall 4 — acceptable).
    11. Same for dashboard.
    12. GitHub repo Settings -> Code security and analysis shows Secret Scanning and Push Protection both enabled.
    13. GitHub repo Settings -> Branches -> main shows the protection rule with PR review required, linear history required, and force-pushes blocked. (The "Require status checks: test" requirement is INTENTIONALLY ABSENT at the Phase-0 step — it gets added as the Post-00-08 follow-up.)
    14. **[After Post-00-08 follow-up]** GitHub repo Settings -> Branches -> main shows "Require status checks to pass before merging" enabled with `test` as a required check. The next PR demonstrates that the merge button is blocked until the `test` workflow is green.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — verify Railway dashboard + GitHub repo settings; no automated check available for first-time project provisioning. After the checkpoint, the Plan 00-08 CI run + manual /health curls serve as the automated gate."</automated>
  </verify>
  <acceptance_criteria>
    - GitHub repo created; main branch has BASIC protection (require PR review + require linear history + block force pushes) at Phase-0 (OPS-01 partial — status-check requirement deferred to Post-00-08)
    - GitHub Push Protection + Secret Scanning enabled (D-22 layers 3+4)
    - Railway project with 4 services: Postgres (TimescaleDB 2.18.0-pg16), data-platform, strategy-engine, dashboard
    - All 3 app services have DATABASE_URL = ${{Postgres.DATABASE_URL}}, ENV=production, LOG_LEVEL=INFO (D-24)
    - data-platform has preDeployCommand="uv run alembic upgrade head" (OPS-07, RESEARCH.md Open Question 1)
    - data-platform sleepApplication=false; strategy-engine and dashboard sleepApplication=true (D-04)
    - Each entrypoint's startCommand correctly references the corresponding Python module (D-03)
    - First deploy on main succeeds and all 3 services answer `/health` with 200 + the 6-field JSON
    - All 3 services answer `/metrics` with Prometheus text exposition containing `shortfire_<svc>_*` metric families
    - On at least one deploy, Railway logs show the `service.settings.loaded` structlog line with safe_summary() output (NOT SecretStr values)
    - On at least one deploy, Railway logs show the `db.migration.applied` event (from alembic upgrade head as the preDeployCommand)
    - **Post-00-08 follow-up queued:** The `test` status check from Plan 00-08's ci.yml is recorded as a queued open follow-up in 00-07-SUMMARY.md and 00-08-SUMMARY.md, to be added to the main branch protection AFTER Plan 00-08's first green CI run on a PR. OPS-01 is fully complete only after that follow-up step lands.
  </acceptance_criteria>
  <resume-signal>Type "approved" once: (a) all 3 services respond on /health and /metrics, (b) GitHub Phase-0 branch protection (PR review + linear history + block force-push) + Push Protection + Secret Scanning are enabled, (c) data-platform's preDeployCommand ran alembic upgrade head successfully on the first deploy (verify in Railway deploy logs), (d) the Post-00-08 status-check follow-up is queued as an open item in 00-07-SUMMARY.md. Or describe failures: which service didn't deploy? What did /health return? Did the migration run?</resume-signal>
  <done>Railway 3-service topology + TimescaleDB are live; auto-deploy from main works; each service answers /health (6-field UI-SPEC JSON) and /metrics (Prometheus); Phase-0 branch protection (PR review + linear history + block force-push) + secret scanning enabled. The "Require status checks: test" rule is deliberately DEFERRED to a Post-00-08 follow-up to avoid the chicken-and-egg race between this plan and Plan 00-08 in wave 3.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `uv run pytest -m "not integration" -q tests/unit/repo_hygiene/test_railway_toml.py tests/unit/repo_hygiene/test_dockerfile_image.py` — static-shape tests green
2. `docker build -t shortfire-local-smoke .` exits 0 if Docker available (optional)
3. Manual curl verification on all 3 Railway service URLs:
   ```
   curl -s https://<data-platform-url>/health | jq . | head -20
   curl -s https://<data-platform-url>/metrics | head -30
   curl -s https://<strategy-engine-url>/health | jq .
   curl -s https://<dashboard-url>/health | jq .
   ```
4. Verify in Railway logs the `service.settings.loaded` JSON line on each service startup
5. Verify in Railway logs the `db.migration.applied` event from data-platform's preDeployCommand
6. Verify GitHub repo Settings shows Secret Scanning enabled + Phase-0 branch protection on main (PR review + linear history + block force-push)
7. Confirm the Post-00-08 status-check follow-up is captured as a queued open item in 00-07-SUMMARY.md (cross-reference 00-08-SUMMARY.md for completion status)
</verification>

<success_criteria>
- Dockerfile builds a single image for all 3 services (D-03)
- .dockerignore excludes dev artifacts + .env files
- railway.toml configures data-platform's defaults: preDeployCommand for migrations (Open Question 1), no watchPatterns (D-05), healthcheck on /health, restartPolicy ON_FAILURE max 5
- Static unit tests verify railway.toml + Dockerfile shape (catches regressions on watchPatterns, image base, preDeployCommand)
- Railway dashboard fully configured per the checkpoint: 4 services, reference variables, per-service overrides, sleepApplication, Phase-0 branch protection (basic protection only — status-check requirement deferred to Post-00-08 follow-up)
- FOUND-01 satisfied (CI runs on push — Plan 00-08 finalizes the CI workflow, but Railway auto-deploy on green main is wired here)
- FOUND-02 satisfied (Railway TimescaleDB provisioned + connected from GitHub)
- OPS-01 satisfied at Phase-0 level (branch protection on main with PR review + linear history + block force-push + Push Protection + Secret Scanning); OPS-01 is FULLY satisfied only after the Post-00-08 follow-up adds the `test` required status check
- OPS-02 satisfied (Railway auto-deploys on push to main)
- OPS-03 partially supported (Plan 00-08 ships the actual CI workflow file)
- OPS-07 satisfied at runtime (alembic upgrade head runs as preDeployCommand on each deploy)
</success_criteria>

<output>
Create `.planning/phases/00-foundation/00-07-SUMMARY.md` documenting:
- The 3 Railway service URLs
- A captured /health response body from each service (with correlation_id redacted)
- The exact preDeployCommand string Railway shows in the data-platform dashboard
- A note on the cold-start time observed on strategy-engine and dashboard (Pitfall 4 — expect ~2-3s)
- Confirmation that GitHub Phase-0 branch protection (PR review + linear history + block force-push) + secret scanning + push protection are enabled (screenshot path optional)
- **Open follow-up:** "Add `test` as required status check to main branch protection — execute AFTER Plan 00-08's first green CI run on a PR." Mark this as queued; cross-reference 00-08-SUMMARY.md.
</output>
</output>
