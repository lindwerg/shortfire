---
phase: 0
plan: 04
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - src/shortfire/db/__init__.py
  - src/shortfire/db/base.py
  - src/shortfire/db/engine.py
  - src/shortfire/db/timescale.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/0001_init_timescaledb.py
  - alembic/versions/0002_service_event_hypertable.py
  - tests/unit/db/__init__.py
  - tests/unit/db/test_timescale_helpers.py
  - tests/unit/db/test_url_rewrite.py
  - tests/unit/db/test_naming_convention.py
autonomous: true
requirements:
  - FOUND-03
  - OPS-07
tags:
  - python
  - sqlalchemy
  - alembic
  - timescaledb
  - asyncpg
  - migrations
must_haves:
  truths:
    - "src/shortfire/db/base.py defines DeclarativeBase with NAMING_CONVENTION (D-29) so constraint names follow pk_/uq_/ix_/ck_/fk_ schema"
    - "src/shortfire/db/timescale.py exposes idempotent helpers create_hypertable, enable_compression, add_compression_policy, add_retention_policy (D-27)"
    - "alembic/env.py uses async_engine_from_config + connection.run_sync(do_run_migrations) + transaction_per_migration=True + compare_type=True + compare_server_default=True (D-26, Pattern 6)"
    - "alembic/env.py rewrites postgres:// and postgresql:// to postgresql+asyncpg:// (Pitfall 3)"
    - "alembic/env.py imports Base from shortfire.db.base to inherit NAMING_CONVENTION (Pitfall 3)"
    - "Migration 0001_init_timescaledb.py runs CREATE EXTENSION IF NOT EXISTS timescaledb (D-28)"
    - "Migration 0002_service_event_hypertable.py creates service_event(ts TIMESTAMPTZ, service_name TEXT, event_type TEXT, payload JSONB) + create_hypertable + enable_compression(segment_by='service_name') + add_compression_policy(after_age='7 days') (D-28)"
    - "All TIMESTAMP columns are TIMESTAMP(timezone=True) — ban-naive-timestamp pre-commit guard from Plan 00-01 enforces"
    - "No ON DELETE CASCADE in any migration — ban-on-delete-cascade pre-commit guard from Plan 00-01 enforces"
    - "asyncpg 0.30+ is the ONLY postgres driver in v1 (D-30)"
    - "Schema push runs via `uv run alembic upgrade head` and is idempotent (rerun-safe, D-31)"
  artifacts:
    - path: "src/shortfire/db/base.py"
      provides: "DeclarativeBase Base + NAMING_CONVENTION (D-29)"
    - path: "src/shortfire/db/engine.py"
      provides: "create_async_engine factory; URL rewrite helper (postgres:// -> postgresql+asyncpg://)"
    - path: "src/shortfire/db/timescale.py"
      provides: "create_hypertable, enable_compression, add_compression_policy, add_retention_policy idempotent wrappers (D-27)"
    - path: "alembic/env.py"
      provides: "Async Alembic env reading DATABASE_URL, rewriting to asyncpg, using Base.metadata for autogenerate (D-26)"
    - path: "alembic/versions/0001_init_timescaledb.py"
      provides: "CREATE EXTENSION IF NOT EXISTS timescaledb migration (D-28)"
    - path: "alembic/versions/0002_service_event_hypertable.py"
      provides: "service_event table + hypertable + compression + 7-day compression policy (D-28)"
  key_links:
    - from: "alembic/env.py"
      to: "src/shortfire/db/base.py (Base.metadata)"
      via: "target_metadata = Base.metadata (Pitfall 3: propagates NAMING_CONVENTION)"
      pattern: "from shortfire.db.base import Base; target_metadata = Base.metadata"
    - from: "alembic/versions/0002_*"
      to: "src/shortfire/db/timescale.py"
      via: "create_hypertable / enable_compression / add_compression_policy helpers (D-27 — NEVER raw op.execute SQL)"
      pattern: "from shortfire.db.timescale import create_hypertable, enable_compression, add_compression_policy"
    - from: "alembic/versions/0002_*"
      to: "alembic/versions/0001_init_timescaledb.py"
      via: "down_revision = '0001' (Pitfall 3: 0002 cannot run before 0001 because create_hypertable doesn't exist yet)"
      pattern: "down_revision = \"0001\""
---

<objective>
Build the database adapter layer per D-24..D-32: SQLAlchemy 2.x DeclarativeBase with naming convention, idempotent TimescaleDB DDL helpers, Alembic async env.py with URL rewrite, and the 2 Phase 0 migrations (init timescaledb extension + `service_event` hypertable with compression policy).

Purpose: This is the schema-as-code surface. Every future migration uses these helpers; never raw `op.execute("SELECT create_hypertable(...)")`. The `service_event` table is a REAL long-term observability asset (CONTEXT.md §Specific Ideas, D-28) — not a smoke object. It survives into Phase 1+ to power ad-hoc operator queries (`SELECT * FROM service_event WHERE event_type='startup'`).

Output: 3 source files under src/shortfire/db/, alembic scaffolding (env.py + script.py.mako + alembic.ini), 2 migration files, 3 unit test files. The [BLOCKING] schema push task in Plan 00-06 runs `uv run alembic upgrade head` against a testcontainers Postgres and asserts the hypertable + compression policy land correctly.
</objective>

<execution_context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-UI-SPEC.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/REQUIREMENTS.md
</context>

<interfaces>
RESEARCH.md Pattern 3 (TimescaleDB-Aware Migration via Idempotent Helpers) — copy verbatim:

- `create_hypertable(table, time_column='ts', chunk_interval='7 days', if_not_exists=True)`
- `enable_compression(table, segment_by, order_by='ts DESC')`
- `add_compression_policy(table, after_age='7 days', if_not_exists=True)`
- `add_retention_policy(table, drop_after, if_not_exists=True)`

RESEARCH.md Pattern 6 (Alembic Async env.py) — full ~50-line implementation to copy.

RESEARCH.md §Pattern 3 supplies the full 0002 migration body (service_event with `sa.TIMESTAMP(timezone=True)` + `JSONB` + the 3 helper calls).
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration files -> Postgres DDL | Helper module API forbids dynamic user input (Phase 0 helpers accept only hardcoded constants from migration files reviewed by humans) |
| DATABASE_URL env var -> SQLAlchemy engine | URL rewrite logic (postgres:// -> postgresql+asyncpg://) is the only transformation; password embedded in URL never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Tampering | src/shortfire/db/timescale.py helper module | mitigate | Helpers accept hardcoded `table` constants from migration files only. Migration files are code-reviewed. No dynamic user input crosses into DDL string construction. D-27 forbids raw `op.execute("SELECT create_hypertable(...)")` in migrations — all DDL goes through helpers. |
| T-00-04 | Tampering of historical data | `service_event.ts` column | mitigate | Column type is `sa.TIMESTAMP(timezone=True)` (TIMESTAMPTZ); `ban-naive-timestamp` pre-commit guard from Plan 00-01 fails the commit if any migration file contains `TIMESTAMP[^(]` (D-32). |
| T-00-05 | Tampering / Loss of Data | future migrations | mitigate | `ban-on-delete-cascade` pre-commit guard from Plan 00-01 forbids `ON DELETE CASCADE` under `alembic/versions/` (D-32, STOR-07). |
| T-00-01 | Information Disclosure | DATABASE_URL containing password in URL | mitigate | engine.py does NOT log the raw URL; `safe_summary()` in Settings already strips creds. Migration logs (`alembic` stdout) print the URL ONLY if `sqlalchemy.url` is logged — alembic.ini sets `sqlalchemy.url = ` empty (env.py sources from env). |
| T-00-09 | Tampering | Alembic env.py | mitigate | env.py forces `async_engine_from_config()` (NOT `engine_from_config()` which is sync) — Pitfall 3 catches the silent crash mode of mixing sync engine with asyncpg URL. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build src/shortfire/db/ — DeclarativeBase + naming convention + async engine + Timescale DDL helpers, with unit tests</name>
  <files>
    src/shortfire/db/__init__.py
    src/shortfire/db/base.py
    src/shortfire/db/engine.py
    src/shortfire/db/timescale.py
    tests/unit/db/__init__.py
    tests/unit/db/test_timescale_helpers.py
    tests/unit/db/test_url_rewrite.py
    tests/unit/db/test_naming_convention.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-27, D-29, D-30)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pattern 3 (TimescaleDB DDL helpers — full source)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pitfall 3 (Alembic async + URL rewriting + extension ordering — exact rewrite logic)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/CLAUDE.md (SQLAlchemy 2.x + asyncpg, Use Core not ORM for hot-path)
  </read_first>
  <behavior>
    - Test 1 (url rewrite): `_rewrite_url("postgres://u:p@h:5432/d") == "postgresql+asyncpg://u:p@h:5432/d"`
    - Test 2 (url rewrite): `_rewrite_url("postgresql://u:p@h:5432/d") == "postgresql+asyncpg://u:p@h:5432/d"`
    - Test 3 (url rewrite no-op): `_rewrite_url("postgresql+asyncpg://u:p@h:5432/d") == "postgresql+asyncpg://u:p@h:5432/d"`
    - Test 4 (naming convention): Base.metadata.naming_convention contains keys `ix`, `uq`, `ck`, `fk`, `pk` matching D-29 exactly
    - Test 5 (naming convention applied): create a stub Table on Base.metadata with a PK and a UNIQUE; serialize via `CreateTable(table).compile(...)` and assert constraint names start with `pk_` and `uq_`
    - Test 6 (timescale helper SQL shape): patch `alembic.op.execute` with `unittest.mock.MagicMock`; call `create_hypertable("service_event")`; assert mock was called once with a SQL string containing `create_hypertable`, `'service_event'`, `chunk_time_interval => INTERVAL '7 days'`, and `if_not_exists => TRUE`
    - Test 7 (timescale helper SQL shape): `enable_compression("service_event", segment_by="service_name")` issues `ALTER TABLE service_event SET (timescaledb.compress, ..., timescaledb.compress_segmentby = 'service_name', timescaledb.compress_orderby = 'ts DESC')`
    - Test 8 (timescale helper SQL shape): `add_compression_policy("service_event")` issues `SELECT add_compression_policy('service_event', INTERVAL '7 days', if_not_exists => TRUE)`
    - Test 9 (timescale helper SQL shape): `add_retention_policy("service_event", drop_after="365 days")` issues correct SQL
  </behavior>
  <action>
    Create `src/shortfire/db/base.py` per D-29 verbatim:
    ```python
    from sqlalchemy import MetaData
    from sqlalchemy.orm import DeclarativeBase

    NAMING_CONVENTION = {
        "ix": "ix_%(column_0_label)s",
        "uq": "uq_%(table_name)s_%(column_0_name)s",
        "ck": "ck_%(table_name)s_%(constraint_name)s",
        "fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s",
        "pk": "pk_%(table_name)s",
    }

    class Base(DeclarativeBase):
        metadata = MetaData(naming_convention=NAMING_CONVENTION)
    ```

    Create `src/shortfire/db/engine.py`: `_rewrite_url(url: str) -> str` per Pattern 6 — rewrites `postgres://` and `postgresql://` prefixes to `postgresql+asyncpg://`; no-op when already correct. `def create_engine_from_env() -> AsyncEngine`: reads `DATABASE_URL` from env (KeyError-on-missing is acceptable since Settings already validated; runtime callers should use Settings.db.url), passes through `_rewrite_url()`, returns `sqlalchemy.ext.asyncio.create_async_engine(url, pool_size=5, pool_pre_ping=True, pool_recycle=1800, future=True)`. Per CLAUDE.md, prefer Core for hot-path inserts (Phase 1+). Phase 0 only needs the engine factory.

    Create `src/shortfire/db/timescale.py` — COPY VERBATIM from RESEARCH.md §Pattern 3. Functions: `create_hypertable(table, time_column='ts', chunk_interval='7 days', if_not_exists=True)`, `enable_compression(table, segment_by, order_by='ts DESC')`, `add_compression_policy(table, after_age='7 days', if_not_exists=True)`, `add_retention_policy(table, drop_after, if_not_exists=True)`. Use `from alembic import op; from sqlalchemy import text`. SECURITY: do NOT accept any input that hasn't been declared at the migration's source-code level. Type hints: `table: str`, `time_column: str`, `chunk_interval: str`, `if_not_exists: bool`. Module docstring: "Phase 0 helpers — accept hardcoded constants from migration files only. NEVER call from request-handling code or with user-controlled input (D-27 / threat T-00-03)."

    Update `src/shortfire/db/__init__.py` to re-export `Base, create_engine_from_env, create_hypertable, enable_compression, add_compression_policy, add_retention_policy`.

    Tests (`tests/unit/db/`):
    - `test_url_rewrite.py`: parametrize over the 3 URL forms; assert correct output. Also one negative case: empty string raises ValueError or returns empty (whatever the implementation decides — document).
    - `test_naming_convention.py`: assert `Base.metadata.naming_convention == NAMING_CONVENTION` (dict equality); declare a tiny test table inline and assert the rendered DDL via SQLAlchemy's `CreateTable(...).compile(dialect=postgresql.dialect())` contains `CONSTRAINT pk_<table>` and `CONSTRAINT uq_<table>_<col>`.
    - `test_timescale_helpers.py`: monkeypatch `alembic.op` with a `MagicMock`. For each helper, assert `op.execute.call_count == 1` and inspect the call argument (a `sqlalchemy.text` Clause) — convert to string via `str(call_arg)` and assert the expected substrings (`create_hypertable`, `INTERVAL '7 days'`, `if_not_exists => TRUE`, `timescaledb.compress`, `compress_segmentby = 'service_name'`, `compress_orderby = 'ts DESC'`, `add_compression_policy`, `add_retention_policy`).

    Per Pitfall 8, all helper functions and tests use explicit type annotations; `pyright` strict will catch missing return types.
  </action>
  <verify>
    <automated>uv run pytest -m "not integration" -q tests/unit/db/ &amp;&amp; uv run pyright src/shortfire/db/ tests/unit/db/ &amp;&amp; uv run ruff check src/shortfire/db/ tests/unit/db/</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from shortfire.db.base import Base, NAMING_CONVENTION; assert Base.metadata.naming_convention == NAMING_CONVENTION; assert set(NAMING_CONVENTION.keys()) == {'ix', 'uq', 'ck', 'fk', 'pk'}"` succeeds (D-29)
    - `python -c "from shortfire.db.timescale import create_hypertable, enable_compression, add_compression_policy, add_retention_policy"` succeeds (D-27 all 4 helpers exported)
    - `python -c "from shortfire.db.engine import create_engine_from_env, _rewrite_url; assert _rewrite_url('postgres://u:p@h/d') == 'postgresql+asyncpg://u:p@h/d'; assert _rewrite_url('postgresql://u:p@h/d') == 'postgresql+asyncpg://u:p@h/d'; assert _rewrite_url('postgresql+asyncpg://u:p@h/d') == 'postgresql+asyncpg://u:p@h/d'"` succeeds (Pitfall 3)
    - `uv run pyright src/shortfire/db/` exits 0
    - `uv run pytest -m "not integration" -q tests/unit/db/` reports all 3 test files green (>=9 test functions)
    - `grep -F "psycopg" src/shortfire/db/` returns no matches (D-30 — asyncpg only)
    - `grep -F "engine_from_config" src/shortfire/db/engine.py` does NOT find the sync form (only `create_async_engine` is used; Pitfall 3 avoidance)
  </acceptance_criteria>
  <done>src/shortfire/db/ exposes Base+NAMING_CONVENTION, async engine factory with URL rewrite, and the 4 Timescale DDL helpers; all helpers + URL rewrite + naming convention covered by unit tests with mocked alembic.op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Initialize Alembic async + write 2 migrations (0001 CREATE EXTENSION + 0002 service_event hypertable with compression policy)</name>
  <files>
    alembic.ini
    alembic/env.py
    alembic/script.py.mako
    alembic/versions/0001_init_timescaledb.py
    alembic/versions/0002_service_event_hypertable.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-CONTEXT.md (D-26, D-28, D-32)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pattern 6 (Alembic Async env.py with DATABASE_URL Rewrite — full source)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pattern 3 (0002 migration full body)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-RESEARCH.md §Pitfall 3 (Alembic async + URL rewriting + extension creation ordering — three failures stack)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/00-foundation/00-UI-SPEC.md §service_event JSONB column
    src/shortfire/db/base.py (Task 1 — Base.metadata)
    src/shortfire/db/timescale.py (Task 1 — helper functions imported by 0002)
  </read_first>
  <behavior>
    - Integration tests live in Plan 00-06 (testcontainers). This task verifies STATICALLY that:
      - 0001 contains `op.execute("CREATE EXTENSION IF NOT EXISTS timescaledb")`
      - 0002's `upgrade()` calls `op.create_table("service_event", ...)` with `sa.Column("ts", sa.TIMESTAMP(timezone=True), ...)` AND no `TIMESTAMP[^(]` violations AND no `ON DELETE CASCADE`
      - 0002 imports the helpers from `shortfire.db.timescale`, NOT raw SQL
      - 0002's `down_revision = "0001"`, ensuring ordering (Pitfall 3)
      - env.py imports `from shortfire.db.base import Base; target_metadata = Base.metadata` (Pitfall 3 NAMING_CONVENTION propagation)
      - env.py uses `async_engine_from_config` (NOT the sync variant)
      - env.py calls `_resolved_url()` rewriting prefixes (Pitfall 3)
      - env.py sets `transaction_per_migration=True`, `compare_type=True`, `compare_server_default=True` (D-26)
  </behavior>
  <action>
    Run `uv run alembic init -t async alembic` to bootstrap. This creates `alembic.ini`, `alembic/env.py`, `alembic/script.py.mako`, `alembic/versions/`.

    OVERWRITE `alembic/env.py` with the Pattern 6 source from RESEARCH.md (lines 882-941). Key elements: `from shortfire.db.base import Base` (Pitfall 3 — NAMING_CONVENTION propagation); `target_metadata = Base.metadata`; `_resolved_url()` rewrites `postgres://` AND `postgresql://` to `postgresql+asyncpg://`; `async_engine_from_config(section, prefix='sqlalchemy.', poolclass=pool.NullPool)`; `do_run_migrations(connection)` configures with `compare_type=True, compare_server_default=True, transaction_per_migration=True, target_metadata=target_metadata`; `run_async_migrations()` async function; `run_migrations_online()` calls `asyncio.run(run_async_migrations())`; `if context.is_offline_mode(): raise RuntimeError("Offline migrations not supported")`.

    Edit `alembic.ini`:
    - `script_location = alembic`
    - `prepend_sys_path = . src`
    - `sqlalchemy.url =` (empty — env.py sources from DATABASE_URL env)
    - `version_path_separator = os` (Posix only — fine for Railway/Linux)
    - `[loggers]` configured: keep alembic logger at INFO; sqlalchemy.engine at WARNING (no SQL spam in tests)

    Adjust `alembic/script.py.mako` minimally: ensure `from typing import Sequence` is imported (pyright-strict alembic templates need this).

    Create `alembic/versions/0001_init_timescaledb.py`:
    ```python
    """init timescaledb extension"""
    from alembic import op

    revision = "0001"
    down_revision = None
    branch_labels = None
    depends_on = None

    def upgrade() -> None:
        op.execute("CREATE EXTENSION IF NOT EXISTS timescaledb")

    def downgrade() -> None:
        op.execute("DROP EXTENSION IF EXISTS timescaledb")
    ```
    (D-28; Pitfall 3 — must exist BEFORE 0002 runs.)

    Create `alembic/versions/0002_service_event_hypertable.py` — COPY VERBATIM from RESEARCH.md §Pattern 3:
    ```python
    """service_event hypertable + compression policy"""
    from alembic import op
    import sqlalchemy as sa
    from sqlalchemy.dialects.postgresql import JSONB
    from shortfire.db.timescale import create_hypertable, enable_compression, add_compression_policy

    revision = "0002"
    down_revision = "0001"
    branch_labels = None
    depends_on = None

    def upgrade() -> None:
        op.create_table(
            "service_event",
            sa.Column("ts", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
            sa.Column("service_name", sa.Text(), nullable=False),
            sa.Column("event_type", sa.Text(), nullable=False),
            sa.Column("payload", JSONB(), nullable=True),
        )
        create_hypertable("service_event", time_column="ts", chunk_interval="7 days")
        enable_compression("service_event", segment_by="service_name", order_by="ts DESC")
        add_compression_policy("service_event", after_age="7 days")

    def downgrade() -> None:
        op.drop_table("service_event")
    ```

    Verify the migration files would PASS the Plan 00-01 pre-commit guards:
    - Run `grep -nE "TIMESTAMP[^(]" alembic/versions/` — must return no matches (ban-naive-timestamp; D-32). NOTE: `sa.TIMESTAMP(timezone=True)` contains `TIMESTAMP(` so the grep regex `TIMESTAMP[^(]` correctly skips it.
    - Run `grep -niE "ON DELETE CASCADE" alembic/versions/` — must return no matches (ban-on-delete-cascade; D-32).

    Verify migrations COMPILE statically (without DB):
    - `uv run python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; cfg = Config('alembic.ini'); sd = ScriptDirectory.from_config(cfg); revs = list(sd.walk_revisions()); assert len(revs) == 2; print([r.revision for r in revs])"` succeeds and prints both revisions.

    Add unit-level static-shape test (in addition to Task 1 helpers): `tests/unit/db/test_migrations_static.py` (add as additional file):
    - Test reads `alembic/versions/0001_init_timescaledb.py` as text, asserts contains `CREATE EXTENSION IF NOT EXISTS timescaledb`
    - Test reads `0002_service_event_hypertable.py`, asserts contains `sa.TIMESTAMP(timezone=True)`, `JSONB`, `create_hypertable`, `enable_compression`, `add_compression_policy`, `down_revision = "0001"`
    - Test reads `alembic/env.py`, asserts contains `from shortfire.db.base import Base`, `target_metadata = Base.metadata`, `async_engine_from_config`, `transaction_per_migration=True`, `compare_type=True`, `compare_server_default=True`, and either `postgres://` or `postgresql://` URL rewrite logic
  </action>
  <verify>
    <automated>uv run python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; cfg = Config('alembic.ini'); sd = ScriptDirectory.from_config(cfg); revs = list(sd.walk_revisions()); assert len(revs) == 2" &amp;&amp; uv run pytest -m "not integration" -q tests/unit/db/ &amp;&amp; ! grep -nE "TIMESTAMP[^(]" alembic/versions/*.py &amp;&amp; ! grep -niE "ON DELETE CASCADE" alembic/versions/*.py</automated>
  </verify>
  <acceptance_criteria>
    - `alembic.ini`, `alembic/env.py`, `alembic/script.py.mako`, `alembic/versions/0001_init_timescaledb.py`, `alembic/versions/0002_service_event_hypertable.py` all exist
    - `alembic/env.py` contains `from shortfire.db.base import Base`, `target_metadata = Base.metadata`, `async_engine_from_config`, `transaction_per_migration=True`, `compare_type=True`, `compare_server_default=True`, and URL-rewrite logic for `postgres://` and `postgresql://`
    - `alembic/versions/0001_init_timescaledb.py` contains exactly `CREATE EXTENSION IF NOT EXISTS timescaledb` and `down_revision = None`
    - `alembic/versions/0002_service_event_hypertable.py` contains `sa.TIMESTAMP(timezone=True)`, `JSONB`, calls `create_hypertable("service_event", ...)`, `enable_compression("service_event", segment_by="service_name", order_by="ts DESC")`, `add_compression_policy("service_event", after_age="7 days")`, and `down_revision = "0001"`
    - `grep -nE "TIMESTAMP[^(]" alembic/versions/*.py` returns no matches (D-32 + ban-naive-timestamp)
    - `grep -niE "ON DELETE CASCADE" alembic/versions/*.py` returns no matches (D-32 + ban-on-delete-cascade)
    - `python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; cfg = Config('alembic.ini'); sd = ScriptDirectory.from_config(cfg); assert len(list(sd.walk_revisions())) == 2"` succeeds (both revisions registered, linked)
    - `tests/unit/db/test_migrations_static.py` passes (static-shape assertions on env.py + both migrations)
    - `uv run pre-commit run --files alembic/env.py alembic/versions/0001_init_timescaledb.py alembic/versions/0002_service_event_hypertable.py` exits 0 (all 3 grep guards + ruff + gitleaks green)
  </acceptance_criteria>
  <done>Alembic async wired with URL rewrite + Base.metadata propagation; 2 migrations written using D-27 helpers (no raw DDL); D-32 grep guards verified passing on the new migration files; static-shape tests in tests/unit/db/test_migrations_static.py.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `uv run pyright src/shortfire/db/ tests/unit/db/` — strict mode passes
2. `uv run ruff check src/shortfire/db/ tests/unit/db/ alembic/` exits 0
3. `uv run pytest -m "not integration" -q tests/unit/db/` — all unit tests green
4. `grep -nE "TIMESTAMP[^(]" alembic/versions/*.py` returns nothing (D-32)
5. `grep -niE "ON DELETE CASCADE" alembic/versions/*.py` returns nothing (D-32)
6. `grep -F "psycopg" src/shortfire/db/ alembic/` returns nothing (D-30)
7. `grep -F "engine_from_config" alembic/env.py` does not match — only `async_engine_from_config` (Pitfall 3)
8. `python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; sd = ScriptDirectory.from_config(Config('alembic.ini')); revs = list(sd.walk_revisions()); assert len(revs) == 2 and revs[0].revision == '0002' and revs[1].revision == '0001'"` confirms linked migrations

**NOTE — Schema push:** Actually running `alembic upgrade head` against a real Postgres+Timescale container is a [BLOCKING] task in Plan 00-06. This plan only verifies static shape of the migration files. The runtime `alembic upgrade head` + idempotency test + hypertable existence + compression policy existence are in Plan 00-06's integration tests, which depend on this plan (00-06 has `depends_on: [00-04]`).
</verification>

<success_criteria>
- src/shortfire/db/base.py defines Base + NAMING_CONVENTION (D-29)
- src/shortfire/db/engine.py defines _rewrite_url + create_engine_from_env (Pitfall 3)
- src/shortfire/db/timescale.py exposes the 4 idempotent DDL helpers (D-27)
- alembic/env.py uses async_engine_from_config + Base.metadata + URL rewrite + per-migration transaction (D-26, Pitfall 3)
- 2 migrations exist with correct ordering: 0001 -> 0002 (Pitfall 3)
- All TIMESTAMP usage is TIMESTAMP(timezone=True) (D-32 ban-naive-timestamp)
- No ON DELETE CASCADE (D-32 ban-on-delete-cascade)
- asyncpg only (D-30)
- Unit tests in tests/unit/db/ cover URL rewrite + naming convention + helper SQL shapes + static migration shape
- FOUND-03 partially satisfied (Alembic + Timescale DDL wired and statically tested; runtime test in Plan 00-06)
- OPS-07 partially satisfied (migration discipline established; Railway deploy wiring in Plan 00-07)
</success_criteria>

<output>
Create `.planning/phases/00-foundation/00-04-SUMMARY.md` documenting:
- Alembic head revision after init (`alembic heads` output)
- pyright + ruff + pytest unit test counts
- The 5 NAMING_CONVENTION keys (assert all present)
- Reminder for Plan 00-06: it owns the actual `alembic upgrade head` integration test against testcontainers
</output>
