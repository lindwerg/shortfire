---
phase: 1
plan: 04
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - alembic/versions/0009_raw_coinglass.py
  - alembic/versions/0010_raw_coingecko_market.py
  - alembic/versions/0011_universe_snapshots.py
  - alembic/versions/0012_symbols_lookup.py
  - alembic/versions/0013_dead_letter_and_ingest_runs.py
  - alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py
  - src/shortfire/db/timescale.py
  - src/shortfire/db/models/__init__.py
  - src/shortfire/db/models/symbols.py
  - src/shortfire/db/models/ingest_runs.py
  - src/shortfire/db/models/dead_letter.py
  - tests/unit/db/test_create_continuous_aggregate_helper.py
  - tests/integration/db/test_phase1_aux_schema.py
  - tests/integration/db/test_continuous_aggregates.py
  - tests/integration/db/test_symbols_soft_delete.py
autonomous: true
requirements:
  - DATA-07
  - DATA-08
  - DATA-11
  - DATA-12
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-05
  - STOR-06
  - STOR-07
  - UNIV-01
  - UNIV-03
tags:
  - alembic
  - timescaledb
  - continuous-aggregates
  - migrations
  - sqlalchemy-models
must_haves:
  truths:
    - "Migrations include compression policy only; no retention policy added in Phase 1 (D-68)"
    - "Alembic migrations 0009–0014 each ship one logical schema unit per D-96"
    - "Alembic migrations 0009–0013 land Coinglass (funding_agg + oi + liq + lsr in one file), CoinGecko market, universe_snapshots, symbols (relational, soft-delete), and dead_letter + ingest_runs hypertables (D-58 inventory rows 9–14, 16, 17, 18)"
    - "Migration 0014 creates 4 continuous aggregates (5m / 15m / 1h / 4h OHLCV) over `raw_mexc_candles_1m` with the locked `(start_offset, end_offset, schedule_interval)` per D-67"
    - "`shortfire.db.timescale` gains a `create_continuous_aggregate(...)` helper called by migration 0014 — preserves the D-27 'never raw op.execute for TimescaleDB DDL' discipline"
    - "SQLAlchemy ORM models for the 3 relational/admin lookup tables exist on the Phase 0 `Base` with NAMING_CONVENTION (`symbols`, `ingest_runs`, `dead_letter`)"
    - "`symbols.delisted_at` is nullable TIMESTAMPTZ; `ON DELETE CASCADE` appears nowhere in 0012"
    - "Continuous aggregate refresh policies use `start_offset < 7 days` so refresh never touches compressed 1m chunks (D-67 + RESEARCH.md §3.5)"
    - "Three integration tests pass: aux-table existence + compression, continuous-aggregate SUM-parity vs hand-rolled SQL (STOR-05 keystone), symbols soft-delete probe"
    - "Unit test asserts `create_continuous_aggregate` issues SQL containing `CREATE MATERIALIZED VIEW`, `WITH (timescaledb.continuous)`, and `add_continuous_aggregate_policy`"
  artifacts:
    - path: "alembic/versions/0009_raw_coinglass.py"
      provides: "Four Coinglass hypertables: funding_agg, oi, liq, lsr (D-58 rows 9–12)"
      contains: "raw_coinglass_funding_agg, raw_coinglass_oi, raw_coinglass_liq, raw_coinglass_lsr"
    - path: "alembic/versions/0010_raw_coingecko_market.py"
      provides: "raw_coingecko_market hypertable (D-58 row 13; D-57 columns)"
      contains: "price_usd, volume_24h_usd, market_cap_usd, category, listing_date, raw_payload JSONB"
    - path: "alembic/versions/0011_universe_snapshots.py"
      provides: "universe_snapshots hypertable on snapshot_date DATE (D-64)"
      contains: "is_qualifying, volume_24h_usd, snapshot_date DATE"
    - path: "alembic/versions/0012_symbols_lookup.py"
      provides: "symbols relational lookup table (D-63 + specifics §3 `tier INTEGER`)"
      contains: "delisted_at, mexc_native_symbol, coinglass_symbol, coingecko_id, tier"
    - path: "alembic/versions/0013_dead_letter_and_ingest_runs.py"
      provides: "dead_letter + ingest_runs hypertables (D-74 + D-77)"
      contains: "raw_payload JSONB, error_type, error_msg"
    - path: "alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py"
      provides: "Continuous aggregates 5m / 15m / 1h / 4h over raw_mexc_candles_1m (D-67)"
      contains: "create_continuous_aggregate, time_bucket"
    - path: "src/shortfire/db/timescale.py"
      provides: "New `create_continuous_aggregate` helper (D-27 carve-out via helper, RESEARCH.md §3.5 recommendation A)"
      contains: "def create_continuous_aggregate"
    - path: "src/shortfire/db/models/symbols.py"
      provides: "SQLAlchemy ORM model on Phase 0 Base"
      contains: "class Symbol(Base)"
    - path: "tests/integration/db/test_continuous_aggregates.py"
      provides: "STOR-05 keystone — CA refresh + SUM/MAX/MIN parity vs hand-rolled SQL"
    - path: "tests/integration/db/test_symbols_soft_delete.py"
      provides: "STOR-07 soft-delete behavior probe"
  key_links:
    - from: "alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py"
      to: "src/shortfire/db/timescale.py::create_continuous_aggregate"
      via: "imported helper call"
      pattern: "from shortfire.db.timescale import create_continuous_aggregate"
    - from: "src/shortfire/db/models/{symbols,ingest_runs,dead_letter}.py"
      to: "src/shortfire/db/base.py::Base"
      via: "DeclarativeBase inheritance"
      pattern: "from shortfire.db.base import Base"
---

<objective>
Finish the Phase 1 schema by landing Alembic migrations 0009–0014 (Coinglass + CoinGecko + universe_snapshots + symbols + dead_letter + ingest_runs + continuous aggregates) along with the SQLAlchemy ORM models for the three relational/admin tables and a new `create_continuous_aggregate` helper that keeps the D-27 "no raw op.execute for TimescaleDB DDL" discipline intact for migration 0014's four materialized views. Three integration tests land in the same plan: aux-table schema check, continuous-aggregate SUM-parity (STOR-05 keystone), and symbols soft-delete behavior.

Purpose: Plan 01-03 covered the MEXC-native hypertables; this plan covers everything else the warehouse needs (Coinglass aggregates, CoinGecko metadata, universe snapshots, symbol lifecycle, dead-letter routing, and the 5m/15m/1h/4h continuous aggregates that downstream features depend on). Together with Plan 01-03 the schema is complete and `alembic upgrade head` reaches revision 0014.
Output: 6 migration files + 1 new helper added to `shortfire.db.timescale` + 3 ORM model files + 4 test files.
</objective>

<execution_context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-platform/01-CONTEXT.md
@.planning/phases/01-data-platform/01-RESEARCH.md
@.planning/phases/01-data-platform/01-PATTERNS.md
@.planning/phases/01-data-platform/01-VALIDATION.md
@alembic/versions/0001_init_timescaledb.py
@alembic/versions/0002_service_event_hypertable.py
@src/shortfire/db/base.py
@src/shortfire/db/timescale.py
@tests/integration/conftest.py
@tests/integration/db/test_alembic_and_hypertables.py
@tests/unit/db/test_timescale_helpers.py

<interfaces>
<!-- D-67 continuous aggregate refresh policies (locked). -->
| View | bucket | start_offset | end_offset | schedule_interval |
|------|--------|--------------|------------|-------------------|
| raw_mexc_candles_5m | 5 minutes | 2 hours | 5 minutes | 5 minutes |
| raw_mexc_candles_15m | 15 minutes | 4 hours | 15 minutes | 15 minutes |
| raw_mexc_candles_1h | 1 hour | 12 hours | 1 hour | 1 hour |
| raw_mexc_candles_4h | 4 hours | 2 days | 4 hours | 4 hours |

From src/shortfire/db/base.py:
```python
NAMING_CONVENTION: dict[str, str] = {
    "ix": "ix_%(column_0_label)s",
    "uq": "uq_%(table_name)s_%(column_0_name)s",
    "ck": "ck_%(table_name)s_%(constraint_name)s",
    "fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s",
    "pk": "pk_%(table_name)s",
}

class Base(DeclarativeBase):
    metadata = MetaData(naming_convention=NAMING_CONVENTION)
```

From src/shortfire/db/timescale.py (existing — will be extended in Task 2):
```python
def create_hypertable(table, time_column="ts", chunk_interval="7 days", if_not_exists=True) -> None: ...
def enable_compression(table, segment_by, order_by="ts DESC") -> None: ...
def add_compression_policy(table, after_age="7 days", if_not_exists=True) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations 0009–0013 (Coinglass × 4 in one file + CoinGecko + universe_snapshots + symbols + dead_letter/ingest_runs) and SQLAlchemy ORM models</name>
  <files>
    alembic/versions/0009_raw_coinglass.py
    alembic/versions/0010_raw_coingecko_market.py
    alembic/versions/0011_universe_snapshots.py
    alembic/versions/0012_symbols_lookup.py
    alembic/versions/0013_dead_letter_and_ingest_runs.py
    src/shortfire/db/models/__init__.py
    src/shortfire/db/models/symbols.py
    src/shortfire/db/models/ingest_runs.py
    src/shortfire/db/models/dead_letter.py
    tests/integration/db/test_phase1_aux_schema.py
    tests/integration/db/test_symbols_soft_delete.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/alembic/versions/0002_service_event_hypertable.py (canonical migration shape — same as plan 01-03 source)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-CONTEXT.md D-58 (aux table inventory), D-63 (symbols table full DDL), D-64 (universe_snapshots full DDL), D-74 (dead_letter schema), D-77 (ingest_runs purpose)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §3.1 (aux table rows in inventory), §3.4 (universe_snapshots + point-in-time query + new-listing detection), §3.6 (symbols with `tier INTEGER`)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-PATTERNS.md Group 1 (Alembic migration deltas) + Group 2 (SQLAlchemy lookup-table model on Phase 0 Base + NAMING_CONVENTION)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/src/shortfire/db/base.py (Base + NAMING_CONVENTION — every model imports from here)
  </read_first>
  <behavior>
    - Test 1: `alembic upgrade head` extends to 0013 successfully (revision chain 0008 → 0009 → 0010 → 0011 → 0012 → 0013)
    - Test 2: `SELECT hypertable_name FROM timescaledb_information.hypertables` returns the 7 MEXC tables (from plan 01-03) + 4 Coinglass + raw_coingecko_market + universe_snapshots + dead_letter + ingest_runs = 14 hypertables (excluding `symbols` which is relational)
    - Test 3: `symbols` exists as a regular table (not a hypertable): `SELECT 1 FROM pg_tables WHERE tablename='symbols'` returns 1 row; `SELECT 1 FROM timescaledb_information.hypertables WHERE hypertable_name='symbols'` returns 0 rows
    - Test 4: `symbols.delisted_at` is nullable; UPDATE-ing it to `now()` doesn't physically delete; foreign keys NEVER specify `ON DELETE CASCADE` (Phase 0 grep guard passes)
    - Test 5: `universe_snapshots` PK is `(snapshot_date, symbol)`; partition column is `snapshot_date` (DATE, not TIMESTAMPTZ — D-64 chose DATE for daily granularity); chunk_interval = 90 days
    - Test 6: SQLAlchemy `Symbol`, `IngestRun`, `DeadLetter` models can be imported and inspected via `Base.metadata.tables` — confirms NAMING_CONVENTION applied correctly (PK named `pk_symbols`, etc.)
  </behavior>
  <action>
    Create migrations 0009–0013 following the canonical shape from `alembic/versions/0002_service_event_hypertable.py`. Revision chain: 0009 down_revision="0008" (from plan 01-03 Task 2), 0010 down_revision="0009", 0011="0010", 0012="0011", 0013="0012".

    **Migration 0009 (`alembic/versions/0009_raw_coinglass.py`)** — creates FOUR Coinglass hypertables in one migration file because they share schema rhythm (D-58 + D-96 "0009_raw_coinglass.py"):

    For `raw_coinglass_funding_agg` (D-58 + D-52):
    - Columns: `symbol Text NOT NULL`, `ts sa.TIMESTAMP(timezone=True) NOT NULL`, `funding_rate NUMERIC(28,18) NOT NULL`, `source Text NOT NULL`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(symbol, ts, source)` per D-58 (source IS in the PK for Coinglass tables)
    - CHECK constraints: source enum (same 4 values), quality_flag enum (same 7 values)
    - `create_hypertable("raw_coinglass_funding_agg", time_column="ts", chunk_interval="30 days")`
    - `enable_compression(..., segment_by="symbol", order_by="ts DESC")`
    - `add_compression_policy(..., after_age="7 days")`

    For `raw_coinglass_oi` (D-58 row 10):
    - Same columns as funding_agg but replace `funding_rate` with `open_interest NUMERIC(28,10) NOT NULL`
    - Same PK + constraints
    - `chunk_interval="30 days"`, compression segment_by="symbol", compress after 7 days

    For `raw_coinglass_liq` (D-58 row 11):
    - Columns: symbol, ts, `side Text NOT NULL CHECK (side IN ('short','long'))`, `liquidation_usd NUMERIC(28,10) NOT NULL`, source, quality_flag, ingested_at
    - PK `(symbol, ts, source, side)` (side must be in PK or two liquidations at same ts would collide)
    - `chunk_interval="7 days"`, compress after 7 days, segment_by="symbol"

    For `raw_coinglass_lsr` (D-58 row 12 — long/short ratio):
    - Columns: symbol, ts, `long_short_ratio NUMERIC(28,10) NOT NULL`, source, quality_flag, ingested_at
    - PK `(symbol, ts, source)`
    - `chunk_interval="30 days"`, compress after 7 days, segment_by="symbol"

    `downgrade()` drops all four in reverse.

    **Migration 0010 (`alembic/versions/0010_raw_coingecko_market.py`)** — per D-57:
    - Import `from sqlalchemy.dialects.postgresql import JSONB`
    - Columns: `symbol Text NOT NULL`, `ts sa.TIMESTAMP(timezone=True) NOT NULL`, `price_usd NUMERIC(28,10)`, `volume_24h_usd NUMERIC(28,10)`, `market_cap_usd NUMERIC(28,10)`, `category Text`, `listing_date sa.Date`, `raw_payload JSONB`, `source Text NOT NULL server_default='coingecko'`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(symbol, ts, source)`
    - CHECK constraints (source enum + quality_flag enum)
    - `create_hypertable("raw_coingecko_market", time_column="ts", chunk_interval="90 days")`
    - `enable_compression("raw_coingecko_market", segment_by="source", order_by="ts DESC")` (D-58 row 13 explicitly says segment_by=source for this table)
    - `add_compression_policy("raw_coingecko_market", after_age="30 days")`

    **Migration 0011 (`alembic/versions/0011_universe_snapshots.py`)** — per D-64:
    - Columns: `snapshot_date sa.Date NOT NULL`, `symbol Text NOT NULL`, `volume_24h_usd NUMERIC(28,10) NOT NULL`, `price_usd NUMERIC(20,10)`, `is_qualifying Boolean NOT NULL`, `source Text NOT NULL server_default='mexc_native'`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(snapshot_date, symbol)` per D-64 (note: NOT including `source` since this is canonical daily snapshot from MEXC universe filter)
    - CHECK constraints on source + quality_flag (same enums)
    - `create_hypertable("universe_snapshots", time_column="snapshot_date", chunk_interval="90 days")` — note `time_column="snapshot_date"` (DATE-typed) and 90-day chunks per D-64
    - NO compression policy (D-58 row 15: "NONE (tiny)" — universe is small)
    - NO `add_compression_policy` call

    **Migration 0012 (`alembic/versions/0012_symbols_lookup.py`)** — per D-63 + RESEARCH.md §3.6 + `<specifics>` §1 (`tier INTEGER`):
    - This table is RELATIONAL — NOT a hypertable. Migration uses `op.create_table` BUT does NOT call `create_hypertable`/`enable_compression`/`add_compression_policy`.
    - Columns: `symbol Text PRIMARY KEY`, `exchange Text NOT NULL server_default='mexc'`, `market_type Text NOT NULL server_default='swap'`, `mexc_native_symbol Text NOT NULL`, `coinglass_symbol Text`, `coingecko_id Text`, `tier sa.Integer NOT NULL server_default='2'`, `first_seen_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`, `delisted_at sa.TIMESTAMP(timezone=True)`, `listed_at sa.TIMESTAMP(timezone=True)`
    - Constraints: `sa.PrimaryKeyConstraint("symbol", name="pk_symbols")`, `sa.CheckConstraint("tier IN (1, 2)", name="ck_symbols_tier")` (per D-46 — tier 1 = top-50, tier 2 = rest)
    - NO foreign keys defined yet (other tables reference `symbols.symbol` via TEXT only, not FK — preserves the soft-delete invariant; no risk of accidental `ON DELETE CASCADE` ever surfacing)

    **Migration 0013 (`alembic/versions/0013_dead_letter_and_ingest_runs.py`)** — TWO hypertables in one file per D-96 + D-74:

    For `dead_letter`:
    - Need pgcrypto extension for `gen_random_uuid()`: `op.execute("CREATE EXTENSION IF NOT EXISTS pgcrypto")` (NOT a TimescaleDB DDL — D-27 grep guard does not flag this; the guard is for `create_hypertable`/`enable_compression`/`add_compression_policy`)
    - Import `from sqlalchemy.dialects.postgresql import UUID, JSONB`
    - Columns: `id UUID PRIMARY KEY server_default=sa.text('gen_random_uuid()')`, `ts sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`, `source Text NOT NULL`, `endpoint Text NOT NULL`, `symbol Text`, `raw_payload sa.Text NOT NULL` (changed from JSONB to Text since the planner-shipping `dead_letter.writer` passes raw_payload as a string — JSONB requires valid JSON which a malformed response may not be; storing as TEXT preserves forensics), `error_type Text NOT NULL`, `error_msg Text NOT NULL`, `retries_attempted Integer NOT NULL server_default='0'`, `quality_flag Text NOT NULL server_default='schema_warn'`
    - PK `(id)` per D-74
    - CHECK constraints: source enum + quality_flag enum
    - `create_hypertable("dead_letter", time_column="ts", chunk_interval="30 days")`
    - `enable_compression("dead_letter", segment_by="source", order_by="ts DESC")`
    - `add_compression_policy("dead_letter", after_age="30 days")`

    For `ingest_runs`:
    - Columns: `id UUID PRIMARY KEY server_default=sa.text('gen_random_uuid()')`, `ts sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`, `source Text NOT NULL`, `dataset Text NOT NULL`, `job_id Text NOT NULL`, `started_at sa.TIMESTAMP(timezone=True) NOT NULL`, `finished_at sa.TIMESTAMP(timezone=True)`, `status Text NOT NULL` (CHECK `status IN ('running','succeeded','failed')`), `rows_written Integer NOT NULL server_default='0'`, `error_msg Text`, `kv_state JSONB` (for round-robin cursor state per RESEARCH.md §4.6), `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(id)`
    - CHECK constraints on source + quality_flag + status
    - `create_hypertable("ingest_runs", time_column="ts", chunk_interval="30 days")`
    - `enable_compression("ingest_runs", segment_by="source", order_by="ts DESC")`
    - `add_compression_policy("ingest_runs", after_age="30 days")`

    Create SQLAlchemy ORM models per PATTERNS.md Group 2:

    `src/shortfire/db/models/__init__.py` — empty package marker.

    `src/shortfire/db/models/symbols.py`:
    ```
    from shortfire.db.base import Base
    from sqlalchemy.orm import Mapped, mapped_column
    from sqlalchemy import Integer, Text, TIMESTAMP, CheckConstraint
    from datetime import datetime

    class Symbol(Base):
        __tablename__ = "symbols"
        __table_args__ = (CheckConstraint("tier IN (1, 2)", name="ck_symbols_tier"),)
        symbol: Mapped[str] = mapped_column(Text, primary_key=True)
        exchange: Mapped[str] = mapped_column(Text, nullable=False, default="mexc")
        market_type: Mapped[str] = mapped_column(Text, nullable=False, default="swap")
        mexc_native_symbol: Mapped[str] = mapped_column(Text, nullable=False)
        coinglass_symbol: Mapped[str | None] = mapped_column(Text, nullable=True)
        coingecko_id: Mapped[str | None] = mapped_column(Text, nullable=True)
        tier: Mapped[int] = mapped_column(Integer, nullable=False, default=2)
        first_seen_at: Mapped[datetime] = mapped_column(TIMESTAMP(timezone=True), nullable=False)
        delisted_at: Mapped[datetime | None] = mapped_column(TIMESTAMP(timezone=True), nullable=True)
        listed_at: Mapped[datetime | None] = mapped_column(TIMESTAMP(timezone=True), nullable=True)
    ```

    `src/shortfire/db/models/ingest_runs.py` and `src/shortfire/db/models/dead_letter.py`: same shape, ORM-modeling the columns from migration 0013. These models are used by the universe snapshot job and tier-1 round-robin state writers in plan 01-09, and by the dead-letter alerter in plan 01-10. Hypertable INSERTs from the hot ingest path still go through `copy_into_hypertable`, NOT the ORM (D-69, D-70) — the ORM models are read-only/admin-write only.

    Create integration tests:

    `tests/integration/db/test_phase1_aux_schema.py` modeled on plan 01-03 Task 3's `test_phase1_mexc_schema.py`. Mark every test `@pytest.mark.integration @pytest.mark.asyncio`.
    - `async def test_migrations_through_0013_apply(migrated_db)` — assert hypertables exist: `raw_coinglass_funding_agg`, `raw_coinglass_oi`, `raw_coinglass_liq`, `raw_coinglass_lsr`, `raw_coingecko_market`, `universe_snapshots`, `dead_letter`, `ingest_runs` (8 new hypertables; plus the 7 from plan 01-03 + the 1 Phase 0 service_event = 16 total — assert at least 16)
    - `async def test_symbols_is_relational_not_hypertable(migrated_db)` — `SELECT 1 FROM pg_tables WHERE tablename='symbols'` returns 1 row; `SELECT count(*) FROM timescaledb_information.hypertables WHERE hypertable_name='symbols'` returns 0
    - `async def test_universe_snapshots_partition_column_is_date(migrated_db)` — query `pg_attribute`/`pg_type` to assert `universe_snapshots.snapshot_date` is DATE (not TIMESTAMPTZ)

    `tests/integration/db/test_symbols_soft_delete.py`:
    - `async def test_symbols_can_be_soft_deleted(migrated_db)`: INSERT a symbol row, UPDATE `delisted_at = now()`, then SELECT — assert row still exists and `delisted_at` is non-null
    - `async def test_no_on_delete_cascade_anywhere(migrated_db)` — query `pg_constraint` for `confdeltype = 'c'` (CASCADE delete in pg) and assert zero matches across all Phase 1 tables. Also `grep -rF 'ON DELETE CASCADE' alembic/versions/000{3..14}*.py` returns no matches (assert via `subprocess.run` in the test or use `pathlib` to read the migration files and assert the substring is absent).
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files &amp;&amp; uv run pytest -m integration tests/integration/db/test_phase1_aux_schema.py tests/integration/db/test_symbols_soft_delete.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'create_hypertable\(' alembic/versions/0009_raw_coinglass.py` returns 4 (four Coinglass tables); `grep -c create_hypertable alembic/versions/0010_raw_coingecko_market.py` returns 1; `grep -c create_hypertable alembic/versions/0011_universe_snapshots.py` returns 1; `grep -c create_hypertable alembic/versions/0013_dead_letter_and_ingest_runs.py` returns 2; `grep -c create_hypertable alembic/versions/0012_symbols_lookup.py` returns 0 (symbols is relational)
    - `grep -E 'TIMESTAMP[^(]' alembic/versions/0009*.py alembic/versions/0010*.py alembic/versions/0011*.py alembic/versions/0012*.py alembic/versions/0013*.py` returns no matches
    - `grep -F 'ON DELETE CASCADE' alembic/versions/0009*.py alembic/versions/0010*.py alembic/versions/0011*.py alembic/versions/0012*.py alembic/versions/0013*.py` returns no matches
    - `grep -F 'op.execute(' alembic/versions/0009*.py alembic/versions/0010*.py alembic/versions/0011*.py alembic/versions/0012*.py` returns no matches (D-27); `grep -F 'op.execute(' alembic/versions/0013*.py` may return ONE match for `CREATE EXTENSION IF NOT EXISTS pgcrypto` (justified — pgcrypto is NOT TimescaleDB DDL)
    - `grep -cE 'class (Symbol|IngestRun|DeadLetter)\(Base\):' src/shortfire/db/models/*.py` returns 3
    - `grep -nE 'down_revision = "0008"' alembic/versions/0009_raw_coinglass.py` returns one match; chain continues 0009→0010→0011→0012→0013
    - `uv run pytest -m integration tests/integration/db/test_phase1_aux_schema.py tests/integration/db/test_symbols_soft_delete.py -v` exits 0
    - `uv run python -c "from shortfire.db.models.symbols import Symbol; from shortfire.db.models.ingest_runs import IngestRun; from shortfire.db.models.dead_letter import DeadLetter; print(Symbol.__tablename__, IngestRun.__tablename__, DeadLetter.__tablename__)"` prints `symbols ingest_runs dead_letter`
  </acceptance_criteria>
  <done>Five aux migrations + 3 ORM models exist; all auxiliary hypertables created with locked chunk/compress; symbols is relational with soft-delete column; aux + soft-delete integration tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New `create_continuous_aggregate` helper + migration 0014 (4 CAs over raw_mexc_candles_1m) + unit + integration tests</name>
  <files>
    src/shortfire/db/timescale.py
    alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py
    tests/unit/db/test_create_continuous_aggregate_helper.py
    tests/integration/db/test_continuous_aggregates.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/src/shortfire/db/timescale.py (existing 4 helpers — Task adds a 5th using the same module style)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-CONTEXT.md D-67 (per-aggregate refresh policy locked table) + D-95 carve-out paragraph
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §3.5 (full `create_continuous_aggregate` signature recommendation A + 5m view SELECT clause + [ASSUMED] `max(quality_flag)` gotcha)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-PATTERNS.md Group 1 "Migration 0014 carve-out" subsection (lines 194-204 — the analog is `alembic/versions/0001_init_timescaledb.py`'s raw `op.execute` style + RESEARCH.md §3.5 signature)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/unit/db/test_timescale_helpers.py (Phase 0 patch-based pattern — every helper has a `with patch("shortfire.db.timescale.op")` test asserting the SQL string contains the expected fragments)
  </read_first>
  <behavior>
    - Test 1: `create_continuous_aggregate("raw_mexc_candles_5m", "raw_mexc_candles_1m", bucket="5 minutes", select_columns=..., start_offset="2 hours", end_offset="5 minutes", schedule_interval="5 minutes")` issues SQL containing `CREATE MATERIALIZED VIEW raw_mexc_candles_5m`, `WITH (timescaledb.continuous)`, `time_bucket('5 minutes', ts)`, `SELECT add_continuous_aggregate_policy('raw_mexc_candles_5m'`, `INTERVAL '2 hours'`
    - Test 2: After `alembic upgrade head`, all 4 CAs exist in `timescaledb_information.continuous_aggregates`: `raw_mexc_candles_5m`, `raw_mexc_candles_15m`, `raw_mexc_candles_1h`, `raw_mexc_candles_4h`
    - Test 3: Inserting synthetic 1m candles into `raw_mexc_candles_1m`, then `CALL refresh_continuous_aggregate('raw_mexc_candles_5m', NULL, NULL)`, returns 5m buckets whose `open` is `first(open, ts)`, `high` is `MAX(high)`, `low` is `MIN(low)`, `close` is `last(close, ts)`, `volume` is `SUM(volume)` — match a hand-rolled SQL aggregate over the same window byte-for-byte (parity)
    - Test 4: Phase 0 grep guards still green: zero naive `TIMESTAMP` in 0014; the helper module signature uses `op.execute(text(...))` consistent with existing helpers
  </behavior>
  <action>
    **Step 1: Extend `src/shortfire/db/timescale.py`** with a new helper `create_continuous_aggregate(view_name, source_table, bucket, select_columns, group_by=None, start_offset="2 hours", end_offset="5 minutes", schedule_interval="5 minutes")` per RESEARCH.md §3.5. Signature mirrors the other helpers; uses `op.execute(text(f"""..."""))`. SQL body:
    ```
    CREATE MATERIALIZED VIEW {view_name}
    WITH (timescaledb.continuous) AS
    SELECT {select_columns}
    FROM {source_table}
    GROUP BY {group_by};

    SELECT add_continuous_aggregate_policy(
        '{view_name}',
        start_offset => INTERVAL '{start_offset}',
        end_offset   => INTERVAL '{end_offset}',
        schedule_interval => INTERVAL '{schedule_interval}'
    );
    ```
    Default `group_by` if None: `f"symbol, time_bucket('{bucket}', ts)"`. Docstring cites D-27 carve-out justification (continuous aggregate API has no Alembic helper; helper preserves the discipline) + RESEARCH.md §3.5.

    Note: Match existing helpers' docstring style — module docstring at top of file already justifies "SECURITY (T-00-03): Helpers accept hardcoded constants from migration files only". This new helper inherits the same caveat.

    **Step 2: Create `alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py`** per D-67 + D-96:
    - down_revision = "0013"
    - Imports: `from sqlalchemy import text`, `from alembic import op`, `from shortfire.db.timescale import create_continuous_aggregate`
    - Docstring: cites D-67, D-95 (carve-out via helper preserves D-27), RESEARCH.md §3.5
    - `upgrade()` body (single function, four calls):

    The common SELECT clause shape for each CA:
    ```
    SELECT
        symbol,
        time_bucket('<bucket>', ts) AS ts,
        first(open, ts) AS open,
        max(high) AS high,
        min(low) AS low,
        last(close, ts) AS close,
        sum(volume) AS volume,
        max(quality_flag) AS quality_flag,
        'mexc_native'::TEXT AS source
    ```

    Call the helper four times with the locked tuples from D-67:
    - `create_continuous_aggregate("raw_mexc_candles_5m", "raw_mexc_candles_1m", bucket="5 minutes", select_columns=<5m SELECT>, start_offset="2 hours", end_offset="5 minutes", schedule_interval="5 minutes")`
    - `create_continuous_aggregate("raw_mexc_candles_15m", "raw_mexc_candles_1m", bucket="15 minutes", select_columns=<15m SELECT>, start_offset="4 hours", end_offset="15 minutes", schedule_interval="15 minutes")`
    - `create_continuous_aggregate("raw_mexc_candles_1h", "raw_mexc_candles_1m", bucket="1 hour", select_columns=<1h SELECT>, start_offset="12 hours", end_offset="1 hour", schedule_interval="1 hour")`
    - `create_continuous_aggregate("raw_mexc_candles_4h", "raw_mexc_candles_1m", bucket="4 hours", select_columns=<4h SELECT>, start_offset="2 days", end_offset="4 hours", schedule_interval="4 hours")`

    `downgrade()` body: four `op.execute("DROP MATERIALIZED VIEW IF EXISTS raw_mexc_candles_5m CASCADE")` calls (in reverse order). Note: `CASCADE` here is on `DROP MATERIALIZED VIEW`, not `ON DELETE CASCADE` — these are different SQL constructs; the Phase 0 grep guard only blocks `ON DELETE CASCADE`. Verify by running the grep guard locally before commit.

    Inline justification comment ABOVE the upgrade() function: "D-95 + RESEARCH.md §3.5: continuous aggregate DDL goes through `create_continuous_aggregate` helper, NOT raw op.execute — preserves D-27 discipline. The helper is the lone TimescaleDB DDL whose Alembic story did not exist before Phase 1."

    **Step 3: Unit test `tests/unit/db/test_create_continuous_aggregate_helper.py`** modeled on `test_timescale_helpers.py`:
    ```
    from unittest.mock import patch
    from shortfire.db.timescale import create_continuous_aggregate

    def test_create_continuous_aggregate_5m_sql_shape():
        with patch("shortfire.db.timescale.op") as mock_op:
            create_continuous_aggregate(
                view_name="raw_mexc_candles_5m",
                source_table="raw_mexc_candles_1m",
                bucket="5 minutes",
                select_columns="symbol, time_bucket('5 minutes', ts) AS ts, sum(volume) AS volume",
                start_offset="2 hours",
                end_offset="5 minutes",
                schedule_interval="5 minutes",
            )
            mock_op.execute.assert_called_once()
            sql_arg = str(mock_op.execute.call_args[0][0])
            assert "CREATE MATERIALIZED VIEW raw_mexc_candles_5m" in sql_arg
            assert "WITH (timescaledb.continuous)" in sql_arg
            assert "FROM raw_mexc_candles_1m" in sql_arg
            assert "add_continuous_aggregate_policy" in sql_arg
            assert "INTERVAL '2 hours'" in sql_arg
            assert "INTERVAL '5 minutes'" in sql_arg
    ```
    Plus a second test verifying default `group_by` when None gets passed: `assert "GROUP BY symbol, time_bucket('5 minutes', ts)" in sql_arg`.

    **Step 4: Integration test `tests/integration/db/test_continuous_aggregates.py`** — the STOR-05 keystone:
    - `@pytest.mark.integration @pytest.mark.asyncio async def test_continuous_aggregates_exist(migrated_db)`: queries `SELECT view_name FROM timescaledb_information.continuous_aggregates`; asserts result superset of `{'raw_mexc_candles_5m','raw_mexc_candles_15m','raw_mexc_candles_1h','raw_mexc_candles_4h'}`.
    - `async def test_ca_5m_sum_parity_vs_handrolled_sql(migrated_db)`: seed 60 synthetic 1m candles for symbol `"BTC/USDT:USDT"` covering 60 minutes (one hour spanning twelve 5m buckets); INSERT via asyncpg parametrized query; refresh CA: `await conn.execute("CALL refresh_continuous_aggregate('raw_mexc_candles_5m', NULL, NULL)")`; then compute hand-rolled aggregate via SQL: `SELECT symbol, time_bucket('5 minutes', ts) AS bucket, first(open, ts) AS o, max(high) AS h, min(low) AS l, last(close, ts) AS c, sum(volume) AS v FROM raw_mexc_candles_1m WHERE symbol='BTC/USDT:USDT' GROUP BY symbol, bucket ORDER BY bucket`; then read CA: `SELECT symbol, ts AS bucket, open, high, low, close, volume FROM raw_mexc_candles_5m WHERE symbol='BTC/USDT:USDT' ORDER BY bucket`; assert both result sets are equal (zipper compare row by row). This is the STOR-05 keystone test required by VALIDATION.md.
    - TRUNCATE `raw_mexc_candles_1m` before and after the parity test (test isolation).
    - Tolerance for Decimal comparison: exact equality (NUMERIC arithmetic is deterministic).
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files &amp;&amp; uv run pytest tests/unit/db/test_create_continuous_aggregate_helper.py -x -q &amp;&amp; uv run pytest -m integration tests/integration/db/test_continuous_aggregates.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'def create_continuous_aggregate' src/shortfire/db/timescale.py` returns one line; the function uses `op.execute(text(f"""…"""))` consistent with the other helpers
    - `grep -F 'add_continuous_aggregate_policy' src/shortfire/db/timescale.py` returns at least one match
    - `grep -cE 'create_continuous_aggregate\(' alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py` returns 4 (four CAs)
    - `grep -nE 'down_revision = "0013"' alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py` returns one line
    - `grep -F 'INTERVAL '\''2 hours'\''' alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py` returns at least one match (5m start_offset); analogous greps for `INTERVAL '4 hours'`, `INTERVAL '12 hours'`, `INTERVAL '2 days'` each return at least one match (matching D-67 table row by row)
    - `grep -F 'ON DELETE CASCADE' alembic/versions/0014_continuous_aggregates_5m_15m_1h_4h.py` returns no matches (the `DROP MATERIALIZED VIEW ... CASCADE` form is a DIFFERENT SQL construct — verify Phase 0 grep guard does not flag it; if it does, remove the CASCADE keyword from downgrade())
    - `uv run pytest tests/unit/db/test_create_continuous_aggregate_helper.py -x -q` exits 0
    - `uv run pytest -m integration tests/integration/db/test_continuous_aggregates.py -v` exits 0 (CAs exist and 5m parity holds)
    - `uv run pre-commit run --all-files` exits 0
  </acceptance_criteria>
  <done>`create_continuous_aggregate` helper added to `shortfire.db.timescale` (preserves D-27 discipline); migration 0014 creates 4 CAs over `raw_mexc_candles_1m` with locked refresh policies; unit + integration tests prove SQL shape and SUM/MIN/MAX/FIRST/LAST parity vs hand-rolled aggregate (STOR-05 keystone).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration→TimescaleDB | DDL with hardcoded view names; same posture as plan 01-03 |
| ORM→DB (lookup tables) | SQLAlchemy ORM models constrained to admin-only writes; hot-path writes go through `copy_into_hypertable` (D-69, D-70) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-CA-01 | Tampering (data integrity) | continuous aggregate `max(quality_flag)` ordering | accept | RESEARCH.md §3.5 [ASSUMED] gotcha: alphabetical max coincidentally maps to severity for current enum; Phase 2 adds a `quality_severity SMALLINT` column if the assumption breaks down; Phase 1 ships with the assumption documented. |
| T-1-CA-02 | Loss of Data | CA refresh over compressed chunks | mitigate | D-67 + RESEARCH.md §P1-C: all `start_offset < 7 days` (the compression window on the 1m base table) so refresh never touches compressed chunks; CI integration test refreshes within the uncompressed window only. |
| T-1-DDL-01 | Tampering | continuous aggregate carve-out from D-27 | mitigate | New `create_continuous_aggregate` helper preserves the "helpers always" discipline; raw `op.execute` for CA DDL still BANNED except inside this helper. |
| T-1-SOFT-01 | Loss of Data | symbols table deleted instead of soft-deleted | mitigate | `delisted_at` column + project-wide `ON DELETE CASCADE` ban; integration test `test_no_on_delete_cascade_anywhere` queries `pg_constraint` AND greps migration files. |
| T-1-EXT-01 | Privilege Elevation | `CREATE EXTENSION pgcrypto` in migration 0013 | accept | Railway PG superuser already has `CREATE EXTENSION` privilege; pgcrypto is a standard Postgres contrib module; alternative (`uuid_generate_v4()`) requires `uuid-ossp` which is functionally equivalent risk; planner picked pgcrypto for forward parity with anyone using a stricter Postgres distribution. |
</threat_model>

<verification>
After both tasks complete:

1. `uv run pre-commit run --all-files` exits 0 (grep guards green; no naive TIMESTAMP, no ON DELETE CASCADE, no `: float` in domain)
2. `uv run pytest tests/unit/db -x -q` exits 0 — `test_create_continuous_aggregate_helper.py` green; existing `test_timescale_helpers.py` not regressed
3. `uv run pytest -m integration tests/integration/db -v` exits 0 — schema check for all aux tables, continuous-aggregate parity, symbols soft-delete probe
4. `uv run python -c "from shortfire.db.timescale import create_hypertable, enable_compression, add_compression_policy, add_retention_policy, create_continuous_aggregate; from shortfire.db.models.symbols import Symbol; from shortfire.db.models.ingest_runs import IngestRun; from shortfire.db.models.dead_letter import DeadLetter; print('ok')"` prints `ok`
5. `grep -cE '^revision = "00(09|10|11|12|13|14)"' alembic/versions/00{09,10,11,12,13,14}*.py | awk -F: '{s+=$2}END{print s}'` returns 6 (revision chain intact)
</verification>

<success_criteria>
- 6 Alembic migrations (0009–0014) extend the schema to the locked D-58 inventory: 4 Coinglass + CoinGecko + universe_snapshots + symbols (relational) + dead_letter + ingest_runs + 4 continuous aggregates
- `create_continuous_aggregate` helper added to `shortfire.db.timescale` (D-27 discipline preserved)
- 3 SQLAlchemy ORM models on Phase 0 `Base` + NAMING_CONVENTION
- 3 integration tests + 1 unit test added; STOR-05 keystone (CA SUM parity) green
- After Plans 01-02 + 01-03, `alembic upgrade head` reaches revision 0014; `timescaledb_information.hypertables` shows 16 entries; `timescaledb_information.continuous_aggregates` shows 4 entries
- All Phase 0 grep guards green
</success_criteria>

<output>
Create `.planning/phases/01-data-platform/01-04-SUMMARY.md` when done. Record:
- Final hypertable count + continuous aggregate count
- Whether `dead_letter.raw_payload` ended up TEXT or JSONB (D-74 spec said JSONB but malformed payloads can't be valid JSON — record the planner's final call and the rationale)
- Whether the alphabetical `max(quality_flag)` ordering accident actually maps to severity for the current enum (smoke against the synthetic 5m parity test data)
- Number of CHECK constraints across all Phase 1 raw_* tables (sanity check: should be roughly 2 per table × 12 tables = ~24)
</output>
