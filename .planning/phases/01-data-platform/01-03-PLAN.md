---
phase: 1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0003_raw_mexc_candles_1m_1d.py
  - alembic/versions/0004_raw_mexc_funding.py
  - alembic/versions/0005_raw_mexc_oi.py
  - alembic/versions/0006_raw_mexc_trades.py
  - alembic/versions/0007_raw_mexc_l2_top20.py
  - alembic/versions/0008_raw_mexc_liquidations.py
  - tests/integration/db/test_phase1_mexc_schema.py
  - tests/integration/db/test_source_check.py
  - tests/integration/ingest/__init__.py
  - tests/integration/ingest/test_idempotency.py
autonomous: true
requirements:
  - DATA-12
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-07
  - STOR-09
  - DATA-09
tags:
  - alembic
  - timescaledb
  - migrations
  - hypertables
  - mexc
must_haves:
  truths:
    - "Migrations include compression policy only; no retention policy added in Phase 1 (D-68)"
    - "Alembic migrations 0003–0008 each ship one logical schema unit per D-96"
    - "Alembic migrations 0003–0008 land 6 MEXC-native hypertables (`raw_mexc_candles_1m`, `raw_mexc_candles_1d`, `raw_mexc_funding`, `raw_mexc_oi`, `raw_mexc_trades`, `raw_mexc_l2_top20`, `raw_mexc_liquidations`) all created via `shortfire.db.timescale` helpers (D-27, D-66)"
    - "Every new hypertable has `source TEXT NOT NULL` + `CHECK (source IN ('mexc_native','coinglass_aggregate','coinglass_mexc_only','coingecko'))` (D-59, DATA-12)"
    - "Every new hypertable has `quality_flag TEXT NOT NULL DEFAULT 'ok'` + CHECK enum from D-60 + `ingested_at TIMESTAMPTZ NOT NULL DEFAULT now()` (D-60, D-61)"
    - "Every time column uses `sa.TIMESTAMP(timezone=True)` — Phase 0 grep guard `TIMESTAMP[^(]` passes (D-65, STOR-03)"
    - "`ON DELETE CASCADE` appears in ZERO new files — Phase 0 grep guard passes (D-63, STOR-07)"
    - "Per-table chunk_interval / segment_by / compress_after match RESEARCH.md §3.1 table inventory exactly"
    - "Three integration tests pass under `uv run pytest -m integration tests/integration/db/test_phase1_mexc_schema.py tests/integration/db/test_source_check.py tests/integration/ingest/test_idempotency.py -v`: hypertable existence + compression policy, source CHECK rejection, Hypothesis idempotency property"
    - "`alembic upgrade head` exits 0 against a fresh `timescale/timescaledb:2.18.0-pg16` testcontainer"
  artifacts:
    - path: "alembic/versions/0003_raw_mexc_candles_1m_1d.py"
      provides: "raw_mexc_candles_1m (1-day chunk, 7-day compress, segment_by=symbol) + raw_mexc_candles_1d (90-day chunk, 30-day compress, segment_by=symbol)"
      contains: "create_hypertable, enable_compression, add_compression_policy, CHECK (source IN, CHECK (quality_flag IN"
    - path: "alembic/versions/0004_raw_mexc_funding.py"
      provides: "raw_mexc_funding hypertable on settlement_ts (30-day chunk, 7-day compress, segment_by=symbol); BOTH settlement_ts + published_ts columns (Pitfall 2)"
      contains: "settlement_ts, published_ts, funding_rate"
    - path: "alembic/versions/0005_raw_mexc_oi.py"
      provides: "raw_mexc_oi hypertable (7-day chunk, 7-day compress)"
      contains: "open_interest"
    - path: "alembic/versions/0006_raw_mexc_trades.py"
      provides: "raw_mexc_trades hypertable (1-day chunk, 2-day compress)"
      contains: "exchange_trade_id, side, price, qty"
    - path: "alembic/versions/0007_raw_mexc_l2_top20.py"
      provides: "raw_mexc_l2_top20 hypertable (1-day chunk, 2-day compress) — bids JSONB + asks JSONB"
      contains: "bids JSONB, asks JSONB"
    - path: "alembic/versions/0008_raw_mexc_liquidations.py"
      provides: "raw_mexc_liquidations hypertable (7-day chunk, 7-day compress)"
      contains: "side, qty, price"
    - path: "tests/integration/db/test_phase1_mexc_schema.py"
      provides: "Hypertable existence + compression-policy assertions for 7 new MEXC tables"
    - path: "tests/integration/db/test_source_check.py"
      provides: "DATA-12 source CHECK constraint rejection of unknown source value"
    - path: "tests/integration/ingest/test_idempotency.py"
      provides: "DATA-09 Hypothesis property test: re-running ingest with re-ordered records is row-count-stable"
  key_links:
    - from: "alembic/versions/0003*..0008*.py"
      to: "src/shortfire/db/timescale.py"
      via: "imports create_hypertable, enable_compression, add_compression_policy"
      pattern: "from shortfire.db.timescale import"
    - from: "tests/integration/ingest/test_idempotency.py"
      to: "src/shortfire/ingest/storage/copy.py::copy_into_hypertable"
      via: "calls helper twice with shuffled records and asserts row-count stable"
      pattern: "copy_into_hypertable\\(.*raw_mexc_candles_1m"
---

<objective>
Land Alembic migrations 0003–0008 — six MEXC-native hypertables (with `raw_mexc_candles_1d` co-located in migration 0003 since it's the 1m table's structural counterpart) — together with three Wave-0 integration tests that prove (a) the schema actually applies on a fresh PG16+Timescale 2.18 container, (b) the `source` CHECK constraint rejects unknown values (DATA-12), and (c) the `copy_into_hypertable` write path is structurally idempotent (DATA-09 Hypothesis property). Plan 01-04 lands the Coinglass/CoinGecko/universe/symbols/dead_letter migrations + continuous aggregates.

Purpose: Without this plan, no MEXC ingest worker has a destination to write to. The integration tests are Wave-0 keystones for the phase: idempotency and source-attribution are the two architectural invariants that survivorship + audit later depend on.
Output: 6 migration files + 3 integration test files; `alembic upgrade head` lands 7 new hypertables.
</objective>

<execution_context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-platform/01-CONTEXT.md
@.planning/phases/01-data-platform/01-RESEARCH.md
@.planning/phases/01-data-platform/01-PATTERNS.md
@.planning/phases/01-data-platform/01-VALIDATION.md
@.planning/phases/00-foundation/00-CONTEXT.md
@alembic/versions/0001_init_timescaledb.py
@alembic/versions/0002_service_event_hypertable.py
@src/shortfire/db/timescale.py
@src/shortfire/db/base.py
@tests/integration/conftest.py
@tests/integration/db/test_alembic_and_hypertables.py

<interfaces>
<!-- Phase 0 helper contracts that every migration uses. -->

From src/shortfire/db/timescale.py:
```python
def create_hypertable(table: str, time_column: str = "ts", chunk_interval: str = "7 days", if_not_exists: bool = True) -> None: ...
def enable_compression(table: str, segment_by: str, order_by: str = "ts DESC") -> None: ...
def add_compression_policy(table: str, after_age: str = "7 days", if_not_exists: bool = True) -> None: ...
```

From alembic/versions/0002_service_event_hypertable.py — the canonical migration shape:
```python
revision = "0002"
down_revision = "0001"
branch_labels = None
depends_on = None

def upgrade() -> None:
    op.create_table("service_event",
        sa.Column("ts", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
        sa.Column("service_name", sa.Text(), nullable=False),
        ...
    )
    create_hypertable("service_event", time_column="ts", chunk_interval="7 days")
    enable_compression("service_event", segment_by="service_name", order_by="ts DESC")
    add_compression_policy("service_event", after_age="7 days")

def downgrade() -> None:
    op.drop_table("service_event")
```

From tests/integration/conftest.py:
```python
_TIMESCALE_IMAGE = "timescale/timescaledb:2.18.0-pg16"

@pytest.fixture(scope="session")
def timescale_container() -> Generator[PostgresContainer, None, None]: ...

@pytest.fixture(scope="session")
def migrated_db(timescale_container) -> str: ...  # returns DATABASE_URL after alembic upgrade head
```
</interfaces>

<!-- Per-table inventory from CONTEXT.md D-58 / RESEARCH.md §3.1 -->
<table_inventory>
| Table | chunk_interval | segment_by | compress_after | PK / dedup key |
|-------|----------------|------------|----------------|----------------|
| raw_mexc_candles_1m | 1 day | symbol | 7 days | (symbol, ts) |
| raw_mexc_candles_1d | 90 days | symbol | 30 days | (symbol, ts) |
| raw_mexc_funding | 30 days | symbol | 7 days | (symbol, settlement_ts) |
| raw_mexc_oi | 7 days | symbol | 7 days | (symbol, ts) |
| raw_mexc_trades | 1 day | symbol | 2 days | (symbol, ts, exchange_trade_id) |
| raw_mexc_l2_top20 | 1 day | symbol | 2 days | (symbol, ts) |
| raw_mexc_liquidations | 7 days | symbol | 7 days | (symbol, ts, side, qty, price) |
</table_inventory>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations 0003–0005 (raw_mexc_candles_1m + raw_mexc_candles_1d + raw_mexc_funding + raw_mexc_oi)</name>
  <files>
    alembic/versions/0003_raw_mexc_candles_1m_1d.py
    alembic/versions/0004_raw_mexc_funding.py
    alembic/versions/0005_raw_mexc_oi.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/alembic/versions/0002_service_event_hypertable.py (canonical migration shape — every Phase 1 migration mirrors lines 1–22 header + lines 27–44 upgrade + lines 47–49 downgrade)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/src/shortfire/db/timescale.py (helpers — every migration MUST go through these per D-27)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-CONTEXT.md D-58 (full table inventory), D-59 (source CHECK), D-60 (quality_flag CHECK), D-61 (ingested_at default), D-62 (idempotency PK), D-65 (TIMESTAMPTZ-only), D-66 (compression via helpers), D-96 (migration filename convention)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §3.1 (hypertable inventory table), §3.2 (source CHECK), §3.3 (quality_flag enum)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-PATTERNS.md Group 1 (Alembic migration analog with full lines 1–49 reference + per-migration deltas list)
  </read_first>
  <behavior>
    - Test 1: `alembic upgrade head` against a fresh `timescale/timescaledb:2.18.0-pg16` container exits 0 (revision tree: 0001 → 0002 → 0003 → 0004 → 0005)
    - Test 2: `SELECT hypertable_name FROM timescaledb_information.hypertables WHERE hypertable_name IN ('raw_mexc_candles_1m','raw_mexc_candles_1d','raw_mexc_funding','raw_mexc_oi')` returns 4 rows
    - Test 3: Each table has the locked compression policy via `SELECT * FROM timescaledb_information.jobs WHERE proc_name='policy_compression' AND hypertable_name=<table>`
    - Test 4: `INSERT INTO raw_mexc_candles_1m (symbol, ts, open, high, low, close, volume, source, quality_flag) VALUES ('BTC/USDT:USDT', now(), 1, 1, 1, 1, 1, 'bogus_source', 'ok')` raises a CHECK violation
    - Test 5: `INSERT INTO raw_mexc_candles_1m ... VALUES ('BTC/USDT:USDT', '2026-05-21 12:00:00+00', 1, 1, 1, 1, 1, 'mexc_native', 'invalid_flag')` raises a CHECK violation on quality_flag
    - Test 6: Phase 0 pre-commit grep guards still green: `grep -E 'TIMESTAMP[^(]' alembic/versions/0003*.py alembic/versions/0004*.py alembic/versions/0005*.py` returns no matches; `grep -F 'ON DELETE CASCADE' alembic/versions/000{3,4,5}*.py` returns no matches
  </behavior>
  <action>
    Create migrations 0003–0005, each following the exact header + upgrade + downgrade shape from `alembic/versions/0002_service_event_hypertable.py`. Per-migration revision chain: 0003 down_revision="0002", 0004 down_revision="0003", 0005 down_revision="0004".

    **Migration 0003 (`alembic/versions/0003_raw_mexc_candles_1m_1d.py`)** — creates TWO tables in one upgrade because they are the OHLCV pair (D-58 + CONTEXT.md `<specifics>` paragraph "Continuous aggregates ... but a SEPARATE 1d hypertable"):

    For `raw_mexc_candles_1m`:
    - Columns: `symbol Text NOT NULL`, `ts sa.TIMESTAMP(timezone=True) NOT NULL`, `open NUMERIC(28,10) NOT NULL`, `high NUMERIC(28,10) NOT NULL`, `low NUMERIC(28,10) NOT NULL`, `close NUMERIC(28,10) NOT NULL`, `volume NUMERIC(28,10) NOT NULL`, `source Text NOT NULL`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - Constraints: `sa.PrimaryKeyConstraint("symbol", "ts", name="pk_raw_mexc_candles_1m")`, `sa.CheckConstraint("source IN ('mexc_native','coinglass_aggregate','coinglass_mexc_only','coingecko')", name="ck_raw_mexc_candles_1m_source")`, `sa.CheckConstraint("quality_flag IN ('ok','gap_detected','partial_candle','late_arrival','ws_rest_divergence','schema_warn','partial_capture')", name="ck_raw_mexc_candles_1m_quality")`
    - Hypertable: `create_hypertable("raw_mexc_candles_1m", time_column="ts", chunk_interval="1 day")`
    - Compression: `enable_compression("raw_mexc_candles_1m", segment_by="symbol", order_by="ts DESC")`, `add_compression_policy("raw_mexc_candles_1m", after_age="7 days")`

    For `raw_mexc_candles_1d` (same columns, different chunk/compress per D-58):
    - Same column shape as 1m
    - PK `(symbol, ts)`, same two CHECK constraints (with `_1d` suffix in name)
    - `create_hypertable("raw_mexc_candles_1d", time_column="ts", chunk_interval="90 days")`
    - `enable_compression("raw_mexc_candles_1d", segment_by="symbol", order_by="ts DESC")`, `add_compression_policy("raw_mexc_candles_1d", after_age="30 days")`

    `downgrade()` drops both tables in reverse order.

    **Migration 0004 (`alembic/versions/0004_raw_mexc_funding.py`)** — `raw_mexc_funding`:
    - Columns: `symbol Text NOT NULL`, `settlement_ts sa.TIMESTAMP(timezone=True) NOT NULL`, `published_ts sa.TIMESTAMP(timezone=True) NOT NULL` (Pitfall 2 — both timestamps; D-44), `funding_rate NUMERIC(28,18) NOT NULL`, `next_funding_ts sa.TIMESTAMP(timezone=True) NULLABLE`, `source Text NOT NULL`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(symbol, settlement_ts)` per D-58
    - Same two CHECK constraints (source enum + quality_flag enum, names `ck_raw_mexc_funding_*`)
    - `create_hypertable("raw_mexc_funding", time_column="settlement_ts", chunk_interval="30 days")` — NOTE: `time_column` is `settlement_ts`, NOT `ts`
    - `enable_compression("raw_mexc_funding", segment_by="symbol", order_by="settlement_ts DESC")`
    - `add_compression_policy("raw_mexc_funding", after_age="7 days")`

    **Migration 0005 (`alembic/versions/0005_raw_mexc_oi.py`)** — `raw_mexc_oi`:
    - Columns: `symbol Text NOT NULL`, `ts sa.TIMESTAMP(timezone=True) NOT NULL`, `open_interest NUMERIC(28,10) NOT NULL`, `source Text`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(symbol, ts)`
    - Same two CHECK constraints
    - `create_hypertable("raw_mexc_oi", time_column="ts", chunk_interval="7 days")`
    - `enable_compression("raw_mexc_oi", segment_by="symbol", order_by="ts DESC")`
    - `add_compression_policy("raw_mexc_oi", after_age="7 days")`

    Every migration imports the helpers exactly: `from shortfire.db.timescale import add_compression_policy, create_hypertable, enable_compression`. NO raw `op.execute("SELECT create_hypertable...")` calls (D-27). NO `ON DELETE CASCADE` anywhere (D-63 grep guard). Every `sa.TIMESTAMP` uses `(timezone=True)` (D-65 grep guard). Use `NUMERIC(28, 10)` for price/volume to match the existing `Decimal` domain convention; use `NUMERIC(28, 18)` for `funding_rate` (more precision for sub-bps rates).

    Add a top-of-file docstring per migration citing the relevant decisions (e.g., "raw_mexc_candles_1m + raw_mexc_candles_1d hypertables — D-58 inventory rows 1+5; D-59 source CHECK; D-60 quality_flag CHECK; D-61 ingested_at; D-65 TIMESTAMPTZ; D-66 compression via helpers; D-67 1m is the source for continuous aggregates 5m/15m/1h/4h in migration 0014").
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files &amp;&amp; uv run pytest -m integration tests/integration/db/test_phase1_mexc_schema.py::test_migrations_0003_through_0005_apply -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'create_hypertable\(' alembic/versions/0003_raw_mexc_candles_1m_1d.py` returns 2 (two tables in one migration); `grep -c` for 0004 and 0005 each return 1
    - `grep -cE 'add_compression_policy\(' alembic/versions/0003_raw_mexc_candles_1m_1d.py` returns 2; for 0004 and 0005 each returns 1
    - `grep -E 'TIMESTAMP[^(]' alembic/versions/0003*.py alembic/versions/0004*.py alembic/versions/0005*.py` returns no matches (D-65 guard)
    - `grep -F 'ON DELETE CASCADE' alembic/versions/0003*.py alembic/versions/0004*.py alembic/versions/0005*.py` returns no matches (D-63 guard)
    - `grep -F "CHECK" alembic/versions/0003_raw_mexc_candles_1m_1d.py | grep -F "source IN ('mexc_native'"` returns at least one match in both tables (DATA-12)
    - `grep -F "CHECK" alembic/versions/0003_raw_mexc_candles_1m_1d.py | grep -F "quality_flag IN"` returns at least one match in both tables (D-60)
    - `grep -nE 'down_revision = "0002"' alembic/versions/0003_raw_mexc_candles_1m_1d.py` returns one line; `down_revision = "0003"` in 0004; `down_revision = "0004"` in 0005 (revision chain intact)
    - `uv run pre-commit run --all-files` exits 0
    - `uv run pytest -m integration tests/integration/db/test_phase1_mexc_schema.py::test_migrations_0003_through_0005_apply -v` exits 0 (the per-table existence + compression-policy probe added in Task 3)
  </acceptance_criteria>
  <done>Migrations 0003 + 0004 + 0005 created using the `shortfire.db.timescale` helpers; revision chain extends Phase 0; every Phase 0 grep guard still green; 4 new hypertables exist after `alembic upgrade head` in a fresh testcontainer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations 0006–0008 (raw_mexc_trades + raw_mexc_l2_top20 + raw_mexc_liquidations)</name>
  <files>
    alembic/versions/0006_raw_mexc_trades.py
    alembic/versions/0007_raw_mexc_l2_top20.py
    alembic/versions/0008_raw_mexc_liquidations.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/alembic/versions/0002_service_event_hypertable.py (canonical migration shape — lines 1–49)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/alembic/versions/0003_raw_mexc_candles_1m_1d.py (Task 1 output — same plan; the L1m_1d structure is the template for the remaining three)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-CONTEXT.md D-58 (rows for trades / l2_top20 / liquidations), D-47 (trades 1-min batching context), D-46 (L2 top-20 + tier-1 logic), D-48 (liquidations dual-source)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §3.1 (per-table chunk/compress numbers), §4.4 (L2 JSONB wide-row rationale; "JSONB arrays of `[price, qty]`" — NOT objects), §3.6 (dedup tuple discussion)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-PATTERNS.md Group 1 (Alembic analog details + JSONB import note: `from sqlalchemy.dialects.postgresql import JSONB`)
  </read_first>
  <behavior>
    - Test 1: `alembic upgrade head` extends to 0008 successfully on a fresh testcontainer
    - Test 2: `SELECT hypertable_name FROM timescaledb_information.hypertables WHERE hypertable_name IN ('raw_mexc_trades','raw_mexc_l2_top20','raw_mexc_liquidations')` returns 3 rows
    - Test 3: `INSERT INTO raw_mexc_l2_top20 (symbol, ts, bids, asks, source, quality_flag) VALUES ('BTC/USDT:USDT', now(), '[[1,1],[0.5,2]]'::JSONB, '[[1.5,1],[2,2]]'::JSONB, 'mexc_native', 'ok')` succeeds
    - Test 4: `raw_mexc_trades` PK `(symbol, ts, exchange_trade_id)` allows two trades from the same symbol with the same ts but different `exchange_trade_id`
    - Test 5: `raw_mexc_liquidations` PK `(symbol, ts, side, qty, price)` deduplicates a row inserted twice (since the vendor lacks stable id per D-58)
    - Test 6: Phase 0 grep guards still green
  </behavior>
  <action>
    Create three migrations following the analog from Task 1.

    **Migration 0006 (`alembic/versions/0006_raw_mexc_trades.py`)** — `raw_mexc_trades`:
    - down_revision = "0005"
    - Columns: `symbol Text NOT NULL`, `ts sa.TIMESTAMP(timezone=True) NOT NULL`, `exchange_trade_id Text NOT NULL` (D-47 — primary dedup; fallback tuple captured in PK below), `side Text NOT NULL` (CHECK `side IN ('buy','sell')`), `price NUMERIC(28,10) NOT NULL`, `qty NUMERIC(28,10) NOT NULL`, `source Text NOT NULL`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(symbol, ts, exchange_trade_id)` — note the three-column PK (D-58 dedup key with fallback inline in comment; the planner DOES NOT split into two indexes — the (symbol, ts, side, price, qty) fallback is informational only, the primary dedup is `(symbol, ts, exchange_trade_id)` since MEXC ws does return a stable id field for ccxt 4.5 unified trades)
    - CHECK constraints on `source` + `quality_flag` + `side`
    - `create_hypertable("raw_mexc_trades", time_column="ts", chunk_interval="1 day")`
    - `enable_compression("raw_mexc_trades", segment_by="symbol", order_by="ts DESC")`
    - `add_compression_policy("raw_mexc_trades", after_age="2 days")` (aggressive — D-58 trade-table row)

    **Migration 0007 (`alembic/versions/0007_raw_mexc_l2_top20.py`)** — `raw_mexc_l2_top20`:
    - down_revision = "0006"
    - Import `from sqlalchemy.dialects.postgresql import JSONB`
    - Columns: `symbol Text NOT NULL`, `ts sa.TIMESTAMP(timezone=True) NOT NULL`, `bids JSONB NOT NULL`, `asks JSONB NOT NULL`, `source Text NOT NULL`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(symbol, ts)`
    - CHECK constraints on `source` + `quality_flag`
    - Add a comment block ABOVE the column declarations explaining the JSONB structure exactly: "bids JSONB is an array of [price, qty] pairs (RESEARCH.md §4.4), e.g. `[[42100.5, 0.3], [42100.4, 1.2], ...]` — narrower JSON, faster scan than object form. asks structurally identical (ascending order)."
    - `create_hypertable("raw_mexc_l2_top20", time_column="ts", chunk_interval="1 day")`
    - `enable_compression("raw_mexc_l2_top20", segment_by="symbol", order_by="ts DESC")`
    - `add_compression_policy("raw_mexc_l2_top20", after_age="2 days")` (storage-pressured — D-58 + RESEARCH.md §4.4 budget sanity check)

    **Migration 0008 (`alembic/versions/0008_raw_mexc_liquidations.py`)** — `raw_mexc_liquidations`:
    - down_revision = "0007"
    - Columns: `symbol Text NOT NULL`, `ts sa.TIMESTAMP(timezone=True) NOT NULL`, `side Text NOT NULL`, `qty NUMERIC(28,10) NOT NULL`, `price NUMERIC(28,10) NOT NULL`, `source Text NOT NULL`, `quality_flag Text NOT NULL server_default='ok'`, `ingested_at sa.TIMESTAMP(timezone=True) NOT NULL server_default=sa.func.now()`
    - PK `(symbol, ts, side, qty, price)` per D-58 (vendor lacks stable id; dedup-by-tuple)
    - CHECK constraints on `source` + `quality_flag` + `side` (where `side IN ('short','long')` matching domain `LiquidationSide` literal)
    - `create_hypertable("raw_mexc_liquidations", time_column="ts", chunk_interval="7 days")`
    - `enable_compression("raw_mexc_liquidations", segment_by="symbol", order_by="ts DESC")`
    - `add_compression_policy("raw_mexc_liquidations", after_age="7 days")`

    All migrations: NO raw `op.execute` for TimescaleDB DDL (D-27); helpers only. JSONB arrays for L2 — pre-decided per D-58 + RESEARCH.md §4.4.
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files &amp;&amp; uv run pytest -m integration tests/integration/db/test_phase1_mexc_schema.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'down_revision = "0005"' alembic/versions/0006_raw_mexc_trades.py` returns one line; `"0006"` in 0007; `"0007"` in 0008
    - `grep -F 'from sqlalchemy.dialects.postgresql import JSONB' alembic/versions/0007_raw_mexc_l2_top20.py` returns one line
    - `grep -nE 'bids.*JSONB' alembic/versions/0007_raw_mexc_l2_top20.py` and `grep -nE 'asks.*JSONB' alembic/versions/0007_raw_mexc_l2_top20.py` each return at least one match
    - `grep -E 'TIMESTAMP[^(]' alembic/versions/0006*.py alembic/versions/0007*.py alembic/versions/0008*.py` returns no matches
    - `grep -F 'ON DELETE CASCADE' alembic/versions/0006*.py alembic/versions/0007*.py alembic/versions/0008*.py` returns no matches
    - `grep -F 'op.execute(' alembic/versions/0006*.py alembic/versions/0007*.py alembic/versions/0008*.py` returns no matches (D-27 — helpers only)
    - `uv run pytest -m integration tests/integration/db/test_phase1_mexc_schema.py -v` exits 0 (all 7 MEXC tables verified as hypertables with compression policies)
  </acceptance_criteria>
  <done>Migrations 0006 + 0007 + 0008 land trades + L2 top-20 (JSONB wide) + liquidations hypertables; D-27 helper discipline preserved; all Phase 0 grep guards green; `alembic upgrade head` now reaches revision 0008 cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests — schema/hypertable assertion, source CHECK rejection, idempotency Hypothesis property</name>
  <files>
    tests/integration/db/test_phase1_mexc_schema.py
    tests/integration/db/test_source_check.py
    tests/integration/ingest/__init__.py
    tests/integration/ingest/test_idempotency.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/integration/db/test_alembic_and_hypertables.py (canonical pattern: `_to_asyncpg_url` helper, hypertable existence assertion, compression-settings query — Phase 1 mirrors this exactly)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/integration/conftest.py (session fixtures `timescale_container`, `migrated_db`, `clean_service_event` — extend, do not replace)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-VALIDATION.md (Wave 0 Requirements section listing the four keystone tests)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §10 Validation Architecture / "Hypothesis property tests" (full idempotency property test sketch lines 1244–1272)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-PATTERNS.md Group 8 (Integration tests testcontainers analog with full line refs)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/src/shortfire/ingest/storage/copy.py (just shipped in plan 01-01 — `copy_into_hypertable` signature; the idempotency property test calls this)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/conftest.py (`money`, `utc_dt` Hypothesis strategies)
  </read_first>
  <behavior>
    - Test 1: `test_migrations_0003_through_0005_apply` — calls `alembic upgrade head` in a fresh testcontainer, then queries `timescaledb_information.hypertables` and asserts that `raw_mexc_candles_1m`, `raw_mexc_candles_1d`, `raw_mexc_funding`, `raw_mexc_oi` all appear; uses the `_to_asyncpg_url` helper from `test_alembic_and_hypertables.py`.
    - Test 2: `test_migrations_through_0008_apply` — extends Test 1 to assert all 7 MEXC tables exist after `alembic upgrade head` (4 from Task 1 + 3 from Task 2).
    - Test 3: `test_compression_policy_attached_per_table` — parametrized over all 7 MEXC hypertables; asserts each has an entry in `timescaledb_information.jobs` with `proc_name='policy_compression'`.
    - Test 4: `test_source_check_rejects_unknown_value` (in `test_source_check.py`) — connects to migrated_db, attempts to `INSERT INTO raw_mexc_candles_1m (symbol, ts, open, high, low, close, volume, source, quality_flag) VALUES (..., 'bogus_source', 'ok')` via asyncpg, asserts `asyncpg.CheckViolationError` raises.
    - Test 5: `test_source_check_accepts_all_four_valid_values` — for each of `('mexc_native','coinglass_aggregate','coinglass_mexc_only','coingecko')`, INSERT succeeds.
    - Test 6: `test_reingest_is_idempotent` (in `test_idempotency.py`) — Hypothesis property test: generate up to 100 synthetic candle records, call `copy_into_hypertable` twice with the second call's records shuffled, assert the table row count is identical.
  </behavior>
  <action>
    Create `tests/integration/db/test_phase1_mexc_schema.py` modeled on `test_alembic_and_hypertables.py`. Reuse the session-scoped fixtures `timescale_container` and `migrated_db` from `tests/integration/conftest.py`. Reuse the `_to_asyncpg_url` helper (either import it or copy-paste — Phase 0 plan 00-06 set the precedent for copy-paste). Mark every test with `@pytest.mark.integration` + `@pytest.mark.asyncio`.

    Test bodies:
    - `async def test_migrations_through_0008_apply(migrated_db: str)` — connects via asyncpg, executes `SELECT hypertable_name FROM timescaledb_information.hypertables ORDER BY hypertable_name`, asserts the result set is the superset of `{'service_event','raw_mexc_candles_1m','raw_mexc_candles_1d','raw_mexc_funding','raw_mexc_oi','raw_mexc_trades','raw_mexc_l2_top20','raw_mexc_liquidations'}` (8 hypertables: Phase 0's service_event + 7 new MEXC tables).
    - `async def test_compression_policy_attached_per_table(migrated_db: str)` parametrized with `@pytest.mark.parametrize("table", ["raw_mexc_candles_1m","raw_mexc_candles_1d",...])`: executes `SELECT COUNT(*) FROM timescaledb_information.jobs WHERE proc_name = 'policy_compression' AND hypertable_name = $1` for each table, asserts count == 1.
    - `async def test_chunk_intervals_match_locked_values(migrated_db: str)` parametrized with the expected chunk_interval per table from CONTEXT.md D-58: executes `SELECT chunk_time_interval_microseconds FROM timescaledb_information.dimensions WHERE hypertable_name = $1`, converts microseconds back to a human interval, asserts match.

    Create `tests/integration/db/test_source_check.py`:
    - `async def test_source_check_rejects_unknown_value(migrated_db: str)`: attempts INSERT with `source='bogus_source'` into `raw_mexc_candles_1m`; uses `pytest.raises(asyncpg.CheckViolationError)` (the asyncpg-specific exception class — `import asyncpg.exceptions`).
    - `async def test_source_check_accepts_all_four_valid_values(migrated_db: str)` parametrized over the four locked source values: INSERT then SELECT, assert the row landed; TRUNCATE the table at the end of each test invocation to keep tests isolated (mirror `clean_service_event` shape from `conftest.py:148-171`).
    - `async def test_source_check_present_on_all_raw_tables(migrated_db: str)`: queries `SELECT conname FROM pg_constraint WHERE conname LIKE 'ck_raw_%_source'`; asserts at least one constraint per raw_mexc_* table (validates the CHECK is everywhere — DATA-12 grep guard at the integration level).

    Create `tests/integration/ingest/__init__.py` (empty).

    Create `tests/integration/ingest/test_idempotency.py` per RESEARCH.md §10 lines 1244–1272 (the full Hypothesis property test sketch):
    - Import `from hypothesis import given, strategies as st, settings, HealthCheck`, `from hypothesis.strategies import composite`, `import random`, `from datetime import timezone`.
    - Define a `@composite` strategy `candle_record(draw)` returning a 9-tuple matching the `raw_mexc_candles_1m` insert column order: `(symbol str sampled_from(["BTC/USDT:USDT","ETH/USDT:USDT","XYZ/USDT:USDT"]), ts datetime tz-aware, open Decimal, high Decimal, low Decimal, close Decimal, volume Decimal nonneg, 'mexc_native', 'ok')` — use the `tests/conftest.py` `money` and `utc_dt` strategies as building blocks; enforce `low <= open,close <= high` invariant via `draw` then `assume`.
    - `@pytest.mark.integration @pytest.mark.asyncio @settings(max_examples=20, deadline=10_000, suppress_health_check=[HealthCheck.function_scoped_fixture]) @given(records=st.lists(candle_record(), min_size=0, max_size=50))` `async def test_reingest_is_idempotent(migrated_db, records)`:
      - Construct `engine = create_async_engine(_to_asyncpg_url(migrated_db).replace('postgresql://','postgresql+asyncpg://'))`
      - `TRUNCATE raw_mexc_candles_1m` at the start (test isolation)
      - Call `copy_into_hypertable(engine, "raw_mexc_candles_1m", records, columns=("symbol","ts","open","high","low","close","volume","source","quality_flag"), conflict_columns=("symbol","ts"))`
      - Read `SELECT count(*) FROM raw_mexc_candles_1m` → `count_after_1`
      - Deduplicate `records` by `(symbol, ts)` BEFORE inserting again (in case the synthesized list has dupes by chance) — the test asserts that re-running the SAME dedup'ed records produces the same count
      - Shuffle records with `random.shuffle(records)` (use deterministic seed = `hash(tuple(str(r[0]) + r[1].isoformat() for r in records[:5]))` for reproducibility under Hypothesis shrinking)
      - Call `copy_into_hypertable` again with shuffled records
      - Read `SELECT count(*) FROM raw_mexc_candles_1m` → `count_after_2`
      - Assert `count_after_1 == count_after_2`, with message "Re-ingest produced duplicates (DATA-09 invariant violated)"
      - Cleanup: dispose engine in a try/finally

    Mark the file with VALIDATION.md crosswalk: top-of-file docstring "DATA-09 idempotency property test (VALIDATION.md keystone)".
  </action>
  <verify>
    <automated>uv run pytest -m integration tests/integration/db/test_phase1_mexc_schema.py tests/integration/db/test_source_check.py tests/integration/ingest/test_idempotency.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '@pytest.mark.integration' tests/integration/db/test_phase1_mexc_schema.py | wc -l` returns at least 3
    - `grep -nE '@pytest.mark.parametrize' tests/integration/db/test_phase1_mexc_schema.py | wc -l` returns at least 2
    - `grep -nE 'raw_mexc_(candles_1m|candles_1d|funding|oi|trades|l2_top20|liquidations)' tests/integration/db/test_phase1_mexc_schema.py | wc -l` returns at least 7 (all 7 tables referenced)
    - `grep -F 'CheckViolationError' tests/integration/db/test_source_check.py` returns at least one match
    - `grep -F 'mexc_native' tests/integration/db/test_source_check.py` and `grep -F 'coinglass_aggregate' tests/integration/db/test_source_check.py` and `grep -F 'coinglass_mexc_only' tests/integration/db/test_source_check.py` and `grep -F 'coingecko' tests/integration/db/test_source_check.py` each return at least one match (all 4 valid values tested)
    - `grep -F '@given' tests/integration/ingest/test_idempotency.py` returns at least one match; `grep -F '@composite' tests/integration/ingest/test_idempotency.py` returns at least one match
    - `grep -F 'copy_into_hypertable' tests/integration/ingest/test_idempotency.py | wc -l` returns at least 2 (called twice — once per ingest pass)
    - `uv run pytest -m integration tests/integration/db/test_phase1_mexc_schema.py tests/integration/db/test_source_check.py tests/integration/ingest/test_idempotency.py -v` exits 0 (all three keystone tests green against testcontainer)
  </acceptance_criteria>
  <done>Three integration tests land: schema/hypertable/compression assertions for all 7 MEXC tables; DATA-12 source CHECK rejection + acceptance probe; DATA-09 Hypothesis idempotency property green against the freshly migrated testcontainer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration→TimescaleDB | DDL with hardcoded table names; security relies on migration files never accepting user input |
| asyncpg→Postgres | All INSERTs in tests use parametrized queries (no string concatenation of values) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-DDL-01 | Tampering (data integrity) | every `raw_mexc_*` table | mitigate | `source` + `quality_flag` CHECK constraints in every migration (D-59, D-60); CI test `test_source_check_present_on_all_raw_tables` greps `pg_constraint` to enforce coverage |
| T-1-DDL-02 | Tampering (timestamp) | every `raw_mexc_*` table | mitigate | `sa.TIMESTAMP(timezone=True)` on every time column (D-65); Phase 0 grep guard rejects naive form |
| T-1-DDL-03 | Loss of Data | future symbols table FK | mitigate | `ON DELETE CASCADE` banned project-wide (D-63, STOR-07); Phase 0 grep guard rejects |
| T-1-DDL-04 | Tampering (DDL drift) | hypertable creation path | mitigate | All hypertable DDL goes through `shortfire.db.timescale` helpers (D-27); CI grep `op.execute(` in migrations 0003–0008 returns no matches |
| T-1-IDEM-01 | Tampering / Loss of Data | re-ingest produces duplicates or overwrites | mitigate | `ON CONFLICT … DO NOTHING` (NEVER `DO UPDATE`) in `copy_into_hypertable` (D-62, Plan 01-01 Task 2); DATA-09 Hypothesis property test in Task 3 of this plan asserts behavior holds |
| T-1-LEAK-01 | Information Disclosure | testcontainer Postgres credentials in CI logs | accept | testcontainers generates random throwaway passwords; container destroyed at session end |
</threat_model>

<verification>
After all 3 tasks complete:

1. `uv run pre-commit run --all-files` exits 0 — Phase 0 grep guards confirm zero naive TIMESTAMP, zero `ON DELETE CASCADE`, zero `: float` in domain
2. `uv run pytest -m integration tests/integration/db tests/integration/ingest -v` exits 0 — all schema + source-check + idempotency assertions green
3. `uv run pytest -m "not integration" -q` exits 0 — no Phase 0 unit test regressed
4. Manual smoke: `docker run --rm -d --name ts -e POSTGRES_PASSWORD=test timescale/timescaledb:2.18.0-pg16 && sleep 5 && DATABASE_URL=postgresql://postgres:test@localhost/postgres uv run alembic upgrade head && docker rm -f ts` exits 0 (migrations apply against a real container outside testcontainers harness too)
5. `grep -rcE 'create_hypertable\(' alembic/versions/0003*.py alembic/versions/0004*.py alembic/versions/0005*.py alembic/versions/0006*.py alembic/versions/0007*.py alembic/versions/0008*.py | awk -F: '{s+=$2}END{print s}'` returns 7 (one create per table; 0003 has two)
</verification>

<success_criteria>
- 6 Alembic migrations (0003–0008) lay 7 MEXC-native hypertables with locked chunk_interval / segment_by / compress_after per D-58
- Every new hypertable has `source` + `quality_flag` + `ingested_at` columns + CHECK constraints (D-59, D-60, D-61)
- All migrations use `shortfire.db.timescale` helpers (D-27) — zero raw `op.execute` for hypertable/compression DDL
- Three Wave-0 keystone integration tests green: schema/hypertable existence + compression policy, source CHECK rejection, DATA-09 Hypothesis idempotency
- Phase 0 grep guards (TIMESTAMP[^(], ON DELETE CASCADE, : float in domain) all still green
</success_criteria>

<output>
Create `.planning/phases/01-data-platform/01-03-SUMMARY.md` when done. Record:
- The 7 hypertables that now exist after `alembic upgrade head`
- The PK / dedup key chosen for `raw_mexc_trades` (was the `exchange_trade_id` field of ccxt 4.5 trades stable? if not, document the fallback)
- The Hypothesis idempotency test's actual `max_examples` and total execution time (informs CI budget at the per-wave sampling rate from VALIDATION.md)
- Whether `asyncpg.CheckViolationError` was the right exception class to assert on, or whether `asyncpg.exceptions.CheckViolationError` is needed
</output>
