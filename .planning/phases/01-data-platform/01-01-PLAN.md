---
phase: 1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shortfire/domain/market.py
  - src/shortfire/ingest/__init__.py
  - src/shortfire/ingest/base.py
  - src/shortfire/ingest/context.py
  - src/shortfire/ingest/retry.py
  - src/shortfire/ingest/rate_limit.py
  - src/shortfire/ingest/storage/__init__.py
  - src/shortfire/ingest/storage/copy.py
  - src/shortfire/ingest/dead_letter/__init__.py
  - src/shortfire/ingest/dead_letter/writer.py
  - pyproject.toml
  - tests/conftest.py
  - tests/fakes/mexc.py
  - tests/fakes/coinglass.py
  - tests/fakes/coingecko.py
  - tests/fakes/repos.py
  - tests/unit/domain/test_candle.py
  - tests/unit/ingest/__init__.py
  - tests/unit/ingest/test_retry_policies.py
  - tests/unit/ingest/test_rate_limit.py
  - tests/unit/ingest/test_copy_into_hypertable.py
  - tests/unit/ingest/test_dead_letter_writer.py
autonomous: true
requirements:
  - DATA-09
  - DATA-10
  - DATA-11
  - DATA-12
  - ORCH-03
tags:
  - python
  - ingest
  - infrastructure
  - domain
must_haves:
  truths:
    - "Hot-path hypertable writes use SQLAlchemy 2.x Core via `copy_into_hypertable`; ORM is reserved for low-volume relational tables only (D-69)"
    - "Each per-timeframe CandleRepo delegates to the same `copy_into_hypertable` helper parameterized by target table (D-71)"
    - "Creates new Phase 1 directory tree under `src/shortfire/ingest/{storage,dead_letter}` and `src/shortfire/domain/` per D-95"
    - "Domain `Source` literal is widened to `Literal['mexc_native','coinglass_aggregate','coinglass_mexc_only','coingecko']` and every Phase 0 fixture / fake / test using the old `mexc|coinglass|coingecko` values is updated in the same commit (D-59)"
    - "`src/shortfire/ingest/storage/copy.py` exposes `async def copy_into_hypertable(engine, target_table, records, columns, conflict_columns) -> int` that does asyncpg COPY → UNLOGGED staging → INSERT … ON CONFLICT DO NOTHING (D-62, D-70)"
    - "`src/shortfire/ingest/retry.py` declares three named tenacity retry decorators `mexc_retry`, `coinglass_retry`, `coingecko_retry` using `wait_exponential_jitter` with per-source initial/max numbers (D-72)"
    - "`src/shortfire/ingest/rate_limit.py` declares module-level `MEXC_LIMITER = AsyncLimiter(max_rate=18, time_period=1)`, `COINGLASS_LIMITER = AsyncLimiter(max_rate=28, time_period=60)`, `COINGECKO_LIMITER = AsyncLimiter(max_rate=28, time_period=60)` (D-51, D-56, D-73)"
    - "`src/shortfire/ingest/dead_letter/writer.py` exposes `async def write_to_dead_letter(...)` writing a row to the `dead_letter` hypertable with UUID PK + raw_payload + error_type + truncated error_msg (D-74)"
    - "`pyproject.toml` `[tool.coverage.run] omit` no longer contains `src/shortfire/ingest/*` (D-91)"
    - "All unit tests under `tests/unit/ingest/` pass under `uv run pytest tests/unit -x -q`"
  artifacts:
    - path: "src/shortfire/domain/market.py"
      provides: "Source literal widened per D-59"
      contains: "Source = Literal[\"mexc_native\", \"coinglass_aggregate\", \"coinglass_mexc_only\", \"coingecko\"]"
    - path: "src/shortfire/ingest/storage/copy.py"
      provides: "`copy_into_hypertable` helper (D-62, D-70)"
      contains: "async def copy_into_hypertable"
      min_lines: 35
    - path: "src/shortfire/ingest/retry.py"
      provides: "Per-source tenacity decorators (D-72)"
      contains: "mexc_retry, coinglass_retry, coingecko_retry"
    - path: "src/shortfire/ingest/rate_limit.py"
      provides: "Per-source aiolimiter token buckets (D-51, D-56, D-73)"
      contains: "MEXC_LIMITER, COINGLASS_LIMITER, COINGECKO_LIMITER"
    - path: "src/shortfire/ingest/context.py"
      provides: "Process-wide singleton accessors (D-79)"
      contains: "get_engine, get_settings, get_metrics"
    - path: "src/shortfire/ingest/dead_letter/writer.py"
      provides: "`write_to_dead_letter` function (D-74)"
      contains: "async def write_to_dead_letter"
  key_links:
    - from: "src/shortfire/ingest/storage/copy.py"
      to: "asyncpg.Connection.copy_records_to_table"
      via: "AsyncEngine.get_raw_connection().driver_connection"
      pattern: "copy_records_to_table"
    - from: "src/shortfire/ingest/dead_letter/writer.py"
      to: "src/shortfire/ingest/storage/copy.py::copy_into_hypertable"
      via: "function call into staging-COPY-then-merge helper"
      pattern: "copy_into_hypertable\\(.*dead_letter"
---

<objective>
Lay down the Phase 1 ingest infrastructure seam that every downstream plan in this phase depends on at the code-call-site level: the single `copy_into_hypertable` helper, per-source tenacity retry decorators, per-source aiolimiter rate limiters, the `dead_letter` writer, process-wide singleton context accessors, and the domain `Source` literal widening (D-59). No real network calls, no live ingest, no migrations yet — this plan is the SEAM that Plans 01-05..01-11 hang off at the call site. Observability extensions and settings blocks ship in companion plan 01-02 (same Wave 1, no file overlap).

Purpose: Without this plan, every other Phase 1 plan would either re-invent the COPY shape, re-declare the limiters, or fail the `assert_event_registered` guard from Phase 0. The seam must land first.
Output: 22 files created or modified across `src/shortfire/ingest/`, `src/shortfire/domain/`, `pyproject.toml`, and matching `tests/unit/` coverage.
</objective>

<execution_context>
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mishanikhinkirtill/Desktop/ShortFIRE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-platform/01-CONTEXT.md
@.planning/phases/01-data-platform/01-RESEARCH.md
@.planning/phases/01-data-platform/01-PATTERNS.md
@.planning/phases/01-data-platform/01-VALIDATION.md
@.planning/phases/00-foundation/00-CONTEXT.md
@src/shortfire/domain/market.py
@src/shortfire/db/engine.py
@src/shortfire/db/timescale.py
@src/shortfire/clients/mexc.py
@src/shortfire/clients/coinglass.py
@src/shortfire/clients/coingecko.py
@src/shortfire/clients/repos.py
@tests/fakes/mexc.py
@tests/fakes/coinglass.py
@tests/fakes/coingecko.py
@tests/fakes/repos.py
@tests/conftest.py

<interfaces>
<!-- Phase 0 contracts that this plan binds to. -->

From src/shortfire/domain/market.py (Source literal — to be widened in this plan):
```python
# BEFORE (current):
Source = Literal["mexc", "coinglass", "coingecko"]
# AFTER (this plan, per D-59):
Source = Literal["mexc_native", "coinglass_aggregate", "coinglass_mexc_only", "coingecko"]
```

From src/shortfire/db/engine.py:
```python
def create_engine_from_env() -> AsyncEngine: ...
```

From src/shortfire/observability/metrics.py (Phase 0 — referenced by context.py's `get_metrics`):
```python
REGISTRY = CollectorRegistry()
def build_metrics_for_service(name: str) -> "ServiceMetrics": ...
```

NOTE: Plan 01-02 (companion Wave 1) extends EVENTS frozenset with 17 new Phase 1 names and registers 8 new metric families on this REGISTRY. This plan does NOT touch events.py or metrics_data_platform.py. The `dead_letter.writer` calls into a `dead_letter_total` metric that exists only after 01-02 lands — but since 01-01 and 01-02 are SAME-WAVE (Wave 1) and executor runs both before any Wave-2 plan, the runtime ordering is fine. Unit tests in this plan patch the metrics import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen domain Source literal (D-59) and update Phase 0 fixtures/fakes/tests in the same commit</name>
  <files>
    src/shortfire/domain/market.py
    tests/conftest.py
    tests/fakes/mexc.py
    tests/fakes/coinglass.py
    tests/fakes/coingecko.py
    tests/fakes/repos.py
    tests/unit/domain/test_candle.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/src/shortfire/domain/market.py (current `Source` literal at line 22; downstream `Candle.source`, `OrderBook.source`, `Funding.source`, `Liquidation.source` all reference this alias)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §3.2 "Source CHECK constraint (D-59)" — the four allowed values and rationale
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-PATTERNS.md Group 3 (Pydantic schema files; Source widening note in same section)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-CONTEXT.md D-59 + `<code_context>` paragraph "deliberate domain-type change"
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/fakes/mexc.py + /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/fakes/coinglass.py + /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/fakes/coingecko.py + /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/fakes/repos.py (current implementations — will surface every Source string literal that needs replacement)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/tests/unit/domain/test_candle.py (Phase 0 Hypothesis property test that calls `Candle(..., source="mexc", ...)`)
  </read_first>
  <behavior>
    - Test 1: `Candle(..., source="mexc_native", ...)` constructs successfully; `Candle(..., source="mexc", ...)` raises `ValidationError`
    - Test 2: `Candle(..., source="coinglass_aggregate", ...)` constructs successfully; `Candle(..., source="coinglass_mexc_only", ...)` constructs successfully
    - Test 3: `FakeMexcClient` emits candles with `source="mexc_native"`
    - Test 4: `FakeCoinglassClient` emits funding aggregates with `source="coinglass_aggregate"`
    - Test 5: `FakeCoinGeckoClient` emits market rows with `source="coingecko"` (unchanged label, but now lives in widened Literal)
    - Test 6: Every existing Phase 0 unit test in `tests/unit/domain/`, `tests/unit/clients/`, `tests/fakes/` still passes (`uv run pytest tests/unit -x -q` exits 0)
  </behavior>
  <action>
    Edit `src/shortfire/domain/market.py` line 22: replace `Source = Literal["mexc", "coinglass", "coingecko"]` with `Source = Literal["mexc_native", "coinglass_aggregate", "coinglass_mexc_only", "coingecko"]`. Do not touch any other line in market.py — the validators (`@model_validator(mode="after")`) and the four BaseModel classes (Candle/OrderBook/OrderBookLevel/Funding/Liquidation) need no further change because they reference the alias.

    Sweep every test fixture and fake that still uses the old labels. The substitution table is:
    - `source="mexc"` → `source="mexc_native"`
    - `source="coinglass"` → `source="coinglass_aggregate"`
    - `source="coingecko"` → `source="coingecko"` (unchanged label, but the Literal widened)

    Apply this substitution in: `tests/unit/domain/test_candle.py` (Phase 0 Hypothesis property test), `tests/fakes/mexc.py` (FakeMexcClient canned data), `tests/fakes/coinglass.py`, `tests/fakes/coingecko.py`, `tests/fakes/repos.py` (InMemoryCandleRepo), and `tests/conftest.py` (if it builds Candle fixtures with a hardcoded source value — check before editing).

    Do NOT use a project-wide sed/find-replace — read each file, identify each `source=` literal, change it deliberately. There may be unrelated occurrences of the word "mexc" in URLs, env-var names, or settings field names that MUST NOT change.

    Add one new negative invariant test to `tests/unit/domain/test_candle.py`: `test_candle_rejects_old_mexc_source_label` — construct `Candle(..., source="mexc", ...)` and assert it raises `pydantic.ValidationError` with a message mentioning either `Input should be 'mexc_native'`, `coinglass_aggregate`, `coinglass_mexc_only`, or `coingecko`.

    The domain widening is the FIRST step in this plan because every subsequent task constructs domain types with the new labels.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/domain tests/unit/clients -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Source = Literal\[' src/shortfire/domain/market.py` returns exactly one line, and that line contains all four new values `"mexc_native"`, `"coinglass_aggregate"`, `"coinglass_mexc_only"`, `"coingecko"`
    - `grep -rnE 'source="mexc"' tests/ src/` returns no matches (the old short label is fully eliminated)
    - `grep -rnE 'source="coinglass"' tests/ src/` returns no matches (excluding "coinglass_aggregate" / "coinglass_mexc_only")
    - `tests/unit/domain/test_candle.py` contains a test named `test_candle_rejects_old_mexc_source_label` (grep `def test_candle_rejects_old_mexc_source_label` returns one line)
    - `uv run pytest tests/unit/domain -x -q` exits 0
    - `uv run pytest tests/unit/clients -x -q` exits 0
    - `uv run pyright src/shortfire/domain/market.py tests/fakes/ tests/unit/domain/` exits 0 (strict mode catches stale Literal references)
  </acceptance_criteria>
  <done>Domain `Source` literal widened to the four CHECK-constraint-aligned values; every Phase 0 fixture, fake, and test using the old labels updated in the same commit; new negative invariant test green; pyright strict passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ingest infrastructure (retry, rate-limit, copy, context, dead-letter writer, base) + supporting unit tests</name>
  <files>
    src/shortfire/ingest/__init__.py
    src/shortfire/ingest/base.py
    src/shortfire/ingest/context.py
    src/shortfire/ingest/retry.py
    src/shortfire/ingest/rate_limit.py
    src/shortfire/ingest/storage/__init__.py
    src/shortfire/ingest/storage/copy.py
    src/shortfire/ingest/dead_letter/__init__.py
    src/shortfire/ingest/dead_letter/writer.py
    pyproject.toml
    tests/unit/ingest/__init__.py
    tests/unit/ingest/test_retry_policies.py
    tests/unit/ingest/test_rate_limit.py
    tests/unit/ingest/test_copy_into_hypertable.py
    tests/unit/ingest/test_dead_letter_writer.py
  </files>
  <read_first>
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §2 Pattern 1 "Single-helper hypertable write path" (full sketch of `copy_into_hypertable`) and §2 Pattern 2 "Layered rate-limit + retry decorator stack" (the concrete `coinglass_retry`/`mexc_retry`/`coingecko_retry` declarations)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-RESEARCH.md §12 "Code Examples" → "Dead-letter writer (D-74)" — the `write_to_dead_letter` signature
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-CONTEXT.md D-62, D-70, D-72, D-73, D-74, D-79, D-91, D-95
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/.planning/phases/01-data-platform/01-PATTERNS.md Group 1 (analog: `src/shortfire/db/timescale.py` — helper module shape) + Group 7 (analog: `tests/unit/db/test_timescale_helpers.py` for the patch-based test style)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/src/shortfire/db/engine.py (the `create_engine_from_env` factory the context module wraps)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/src/shortfire/db/timescale.py (module-of-stateless-functions analog — same docstring style, same `op.execute(text(f"…"))` call shape that copy.py will mirror with asyncpg)
    /Users/mishanikhinkirtill/Desktop/ShortFIRE/pyproject.toml (current `[tool.coverage.run]` block — `omit` list contains `"src/shortfire/ingest/*"` that D-91 mandates removing)
  </read_first>
  <behavior>
    - Test 1: `mexc_retry`, `coinglass_retry`, `coingecko_retry` exist as importable tenacity decorators; each retries on `httpx.HTTPStatusError`, `httpx.TransportError`, and (for mexc) `TimeoutError`; each uses `wait_exponential_jitter` and `stop_after_attempt(N)` with per-source N (mexc=6, coinglass=5, coingecko=4) and per-source `max` (mexc=120, coinglass=60, coingecko=60); `reraise=True` on all
    - Test 2: `MEXC_LIMITER`, `COINGLASS_LIMITER`, `COINGECKO_LIMITER` are `aiolimiter.AsyncLimiter` instances with the locked rate/period values (mexc=18 per 1s, coinglass=28 per 60s, coingecko=28 per 60s)
    - Test 3: `copy_into_hypertable(engine, "raw_mexc_candles_1m", [], cols, conflict)` returns 0 without error (empty records short-circuit)
    - Test 4: `copy_into_hypertable` issues SQL containing `CREATE UNLOGGED TABLE IF NOT EXISTS raw_mexc_candles_1m_staging`, `TRUNCATE TABLE raw_mexc_candles_1m_staging`, and `INSERT INTO raw_mexc_candles_1m ... ON CONFLICT (symbol, ts) DO NOTHING` (assert via patched `raw_conn.execute` call_args, not real DB)
    - Test 5: `write_to_dead_letter("coinglass_aggregate", "/api/futures/funding-rate-list", None, '{"bad":1}', "ValidationError", "field required", 0)` constructs a record tuple with a UUID4 PK, a tz-aware `ts`, error_msg truncated to ≤2000 chars, and calls `copy_into_hypertable` with `target_table="dead_letter"`, `conflict_columns=("id",)`
    - Test 6: Removing `"src/shortfire/ingest/*"` from `[tool.coverage.run] omit` in `pyproject.toml` does not break `uv run pytest --cov` (coverage report now includes the new ingest files)
  </behavior>
  <action>
    Create the package skeleton: `src/shortfire/ingest/__init__.py` (empty package marker — Phase 0 created an empty file at this path; this task confirms it stays empty), `src/shortfire/ingest/storage/__init__.py` (empty), `src/shortfire/ingest/dead_letter/__init__.py` (empty).

    Create `src/shortfire/ingest/retry.py` per RESEARCH.md §2 Pattern 2. Import `httpx, logging` and from `tenacity` import `retry, retry_if_exception_type, stop_after_attempt, wait_exponential_jitter, before_sleep_log`. Declare module-level logger `log = logging.getLogger(__name__)`. Declare three top-level decorators: `coinglass_retry` = `retry(retry=retry_if_exception_type((httpx.HTTPStatusError, httpx.TransportError)), wait=wait_exponential_jitter(initial=1.0, max=60.0), stop=stop_after_attempt(5), before_sleep=before_sleep_log(log, logging.WARNING), reraise=True)`; `mexc_retry` = same shape with `(httpx.HTTPStatusError, httpx.TransportError, TimeoutError)` and `initial=2.0, max=120.0, stop_after_attempt(6)`; `coingecko_retry` = same shape as coinglass but `stop_after_attempt(4)`. Add a module docstring citing D-72 + RESEARCH.md §2 Pattern 2 + the rule "Do NOT retry on 4xx-non-429 — those land in dead_letter at the call site".

    Create `src/shortfire/ingest/rate_limit.py`. Import `from aiolimiter import AsyncLimiter`. Declare three module-level constants: `MEXC_LIMITER = AsyncLimiter(max_rate=18, time_period=1)` (D-73 defense-in-depth ON; project budget under 20 req/s public quota), `COINGLASS_LIMITER = AsyncLimiter(max_rate=28, time_period=60)` (D-51 — 5% headroom under 30 req/min Hobbyist per D-35), `COINGECKO_LIMITER = AsyncLimiter(max_rate=28, time_period=60)` (D-56 — same headroom shape). Docstring cites D-35, D-51, D-56, D-73.

    Create `src/shortfire/ingest/context.py` per D-79. Module-level singletons that scheduler job callables (which must be importable, no closures) use to look up dependencies. Expose three functions: `def get_engine() -> AsyncEngine` (lazy-initialize from `shortfire.db.engine.create_engine_from_env` on first call, cache in module-level variable), `def get_settings() -> DataPlatformSettings` (lazy-initialize from `shortfire.settings.data_platform.DataPlatformSettings()`, cache), `def get_metrics() -> object` (lazy-initialize from `shortfire.observability.metrics.build_metrics_for_service("data-platform")`, cache). Use `_engine: AsyncEngine | None = None`, `_settings: DataPlatformSettings | None = None`, `_metrics: ServiceMetrics | None = None` module-level None sentinels guarded by `if X is None: X = factory()`. Docstring cites D-79.

    Create `src/shortfire/ingest/storage/copy.py` per RESEARCH.md §2 Pattern 1. Signature exactly: `async def copy_into_hypertable(engine: AsyncEngine, target_table: str, records: Iterable[tuple[Any, ...]], columns: tuple[str, ...], conflict_columns: tuple[str, ...]) -> int`. Behavior: materialize records to a list; short-circuit return 0 if list empty; compute `staging = f"{target_table}_staging"`, `conflict_cols = ", ".join(conflict_columns)`, `col_list = ", ".join(columns)`; open `async with engine.connect() as conn` then obtain raw asyncpg connection via `raw_conn = (await conn.get_raw_connection()).driver_connection` (RESEARCH.md §2 Pattern 1 Note + Pitfall P1-D); enter `async with raw_conn.transaction()`; execute `CREATE UNLOGGED TABLE IF NOT EXISTS {staging} (LIKE {target_table} INCLUDING DEFAULTS); TRUNCATE TABLE {staging};` then `await raw_conn.copy_records_to_table(staging, records=records_list, columns=columns)` then `await raw_conn.execute(f"INSERT INTO {target_table} ({col_list}) SELECT {col_list} FROM {staging} ON CONFLICT ({conflict_cols}) DO NOTHING")`; return `len(records_list)`. Docstring cites D-62, D-70, Pitfall P1-D; explicitly notes `ON CONFLICT DO NOTHING` (NEVER `DO UPDATE` per D-62 first-write-wins).

    Create `src/shortfire/ingest/dead_letter/writer.py` per RESEARCH.md §12 "Dead-letter writer (D-74)". Signature: `async def write_to_dead_letter(source: str, endpoint: str, symbol: str | None, raw_payload: str | bytes, error_type: str, error_msg: str, retries_attempted: int = 0) -> None`. Behavior: import `uuid, datetime, timezone`; construct record tuple `(uuid.uuid4(), datetime.now(timezone.utc), source, endpoint, symbol, raw_payload-decoded-to-str-with-errors='replace', error_type, error_msg[:2000], retries_attempted, "schema_warn")`; obtain engine via `get_engine()` from `shortfire.ingest.context`; call `await copy_into_hypertable(engine, "dead_letter", [record], columns=("id","ts","source","endpoint","symbol","raw_payload","error_type","error_msg","retries_attempted","quality_flag"), conflict_columns=("id",))`. Increment `metrics.dead_letter_total.labels(source=source, error_type=error_type).inc()` (metric will exist after plan 01-02 — the call refers to a metric registered in `metrics_data_platform.py`, which is the companion Wave 1 plan; at unit-test time the metric import is patched).

    Create `src/shortfire/ingest/base.py` as a tiny module with a `log = structlog.get_logger("ingest")` and a shared helper `_ts_from_ms(ms: int) -> datetime` returning `datetime.fromtimestamp(ms / 1000, tz=timezone.utc)`. Cite ARCHITECTURE.md timestamp discipline (D-65, Pitfall 17).

    Edit `pyproject.toml`: locate `[tool.coverage.run]` block; remove the entry `"src/shortfire/ingest/*"` from `omit = [...]` (D-91). Keep `"src/shortfire/risk/*"`, `"src/shortfire/execution/*"`, `"src/shortfire/strategy/*"` omissions (those modules are still empty scaffolding per Phase 0 plan 00-01).

    Write the four unit tests, all using the patch-based shape from Group 7 analog:
    - `tests/unit/ingest/test_retry_policies.py`: import each decorator; assert it's a tenacity-wrapped callable (`hasattr(coinglass_retry, "__wrapped__")` or check `coinglass_retry.retry`); assert per-source `stop` is `stop_after_attempt(N)` with the locked N values; use a small `@coinglass_retry async def _stub(): raise httpx.TransportError("boom")` and `freezegun` to verify the decorator retries the configured number of times before re-raising; verify `mexc_retry` also catches `TimeoutError` while `coingecko_retry` does not.
    - `tests/unit/ingest/test_rate_limit.py`: import the three limiters; assert each is an `aiolimiter.AsyncLimiter` instance; assert `MEXC_LIMITER.max_rate == 18`, `MEXC_LIMITER.time_period == 1`, `COINGLASS_LIMITER.max_rate == 28`, `COINGLASS_LIMITER.time_period == 60`, `COINGECKO_LIMITER.max_rate == 28`, `COINGECKO_LIMITER.time_period == 60`.
    - `tests/unit/ingest/test_copy_into_hypertable.py`: patch `engine.connect()` to return an async-mock that itself provides a `.get_raw_connection().driver_connection` whose `copy_records_to_table` and `execute` are AsyncMocks; call `copy_into_hypertable(engine, "raw_mexc_candles_1m", [("BTC/USDT:USDT", utc_dt_value, Decimal("100"), …)], ("symbol","ts","open",…), ("symbol","ts"))`; assert: (a) the first `execute` call SQL string contains `CREATE UNLOGGED TABLE IF NOT EXISTS raw_mexc_candles_1m_staging`, `TRUNCATE TABLE raw_mexc_candles_1m_staging`; (b) `copy_records_to_table` was called with `staging="raw_mexc_candles_1m_staging"`, `records=[(…)]`, `columns=("symbol","ts","open",…)`; (c) the second `execute` call SQL string contains `INSERT INTO raw_mexc_candles_1m`, `FROM raw_mexc_candles_1m_staging`, `ON CONFLICT (symbol, ts) DO NOTHING`; (d) empty records list returns 0 with NO DB calls.
    - `tests/unit/ingest/test_dead_letter_writer.py`: patch `shortfire.ingest.dead_letter.writer.copy_into_hypertable` (the import inside the module); patch `shortfire.ingest.context.get_engine` to return a sentinel; call `await write_to_dead_letter("coinglass_aggregate", "/api/futures/funding-rate-list", None, '{"bad":1}', "ValidationError", "x"*3000, 0)`; assert `copy_into_hypertable` was called with `target_table="dead_letter"`, `conflict_columns=("id",)`, exactly one record, that record's index 7 (error_msg) has `len(record[7]) == 2000` (truncation worked), record index 0 is a `UUID`, record index 1 is a tz-aware datetime, record index 9 is `"schema_warn"`.

    Create `tests/unit/ingest/__init__.py` (empty).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ingest -x -q &amp;&amp; uv run pyright src/shortfire/ingest tests/unit/ingest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^(MEXC|COINGLASS|COINGECKO)_LIMITER\s*=' src/shortfire/ingest/rate_limit.py` returns exactly 3 lines; each contains `AsyncLimiter(max_rate=` with the locked values 18/1, 28/60, 28/60
    - `grep -nE '^(mexc|coinglass|coingecko)_retry\s*=' src/shortfire/ingest/retry.py` returns exactly 3 lines; each contains `wait_exponential_jitter` and `stop_after_attempt`
    - `grep -nE 'async def copy_into_hypertable' src/shortfire/ingest/storage/copy.py` returns exactly one match; file contains `ON CONFLICT.*DO NOTHING` and `CREATE UNLOGGED TABLE` (assert via two grep calls)
    - `grep -F 'ON CONFLICT' src/shortfire/ingest/storage/copy.py | grep -v 'DO UPDATE'` succeeds (no `DO UPDATE` ever used — D-62)
    - `grep -nE 'async def write_to_dead_letter' src/shortfire/ingest/dead_letter/writer.py` returns exactly one match
    - `grep -F 'src/shortfire/ingest/*' pyproject.toml` returns no match (omit entry removed per D-91)
    - `uv run pytest tests/unit/ingest -x -q` exits 0
    - `uv run pyright src/shortfire/ingest tests/unit/ingest` exits 0 (strict mode)
    - `uv run python -c "from shortfire.ingest.retry import mexc_retry, coinglass_retry, coingecko_retry; from shortfire.ingest.rate_limit import MEXC_LIMITER, COINGLASS_LIMITER, COINGECKO_LIMITER; from shortfire.ingest.storage.copy import copy_into_hypertable; from shortfire.ingest.dead_letter.writer import write_to_dead_letter; from shortfire.ingest.context import get_engine, get_settings, get_metrics; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>Ingest infrastructure modules exist with locked signatures, four corresponding unit tests green, coverage gate now sees `src/shortfire/ingest/*`, every import path used by Plans 01-05..01-11 is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→DB | `copy_into_hypertable` issues raw SQL strings with `f"{target_table}"` interpolation — only safe because callers pass hardcoded table names from D-58 |
| dead_letter | failed Pydantic validation lands the raw API payload in DB — payload may include API keys if a provider misroutes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-INF-01 | Tampering | `copy_into_hypertable` SQL string assembly | mitigate | Callers MUST pass hardcoded table names; planner-enforced by every Phase 1 plan's `target_table=` argument literal. Type-checked by pyright strict (caller can't accidentally pass `str(user_input)`). |
| T-1-INF-02 | Information Disclosure | `dead_letter.raw_payload` JSONB | mitigate | `write_to_dead_letter` truncates `error_msg` to 2000 chars; the raw_payload itself MAY contain transient API responses. Acceptable because `dead_letter` is internal-only — no external read path in Phase 1. Phase 5 redaction TODO recorded in CONTEXT.md `<deferred>`. |
| T-1-AVL-01 | Denial of Service | tenacity infinite-retry | accept | `stop_after_attempt(N)` bounded per source (mexc=6, coinglass=5, coingecko=4); aiolimiter prevents burst that would amplify a remote 429 storm. |
| T-1-DOM-01 | Tampering | Source literal widening invalidating Phase 0 fixtures | mitigate | Task 1 sweeps every fixture in the same commit; pyright strict mode at task verify time catches stale Literal references. |
| T-1-DEP-01 | Tampering (supply chain) | New deps `apscheduler`, `boto3`, `moto` | mitigate | Phase 1 plan 01-09 (scheduler) and 01-10 (backup) add these; this plan installs none. Legitimacy verification deferred to those plans' Wave checkpoints per RESEARCH.md §19. |
</threat_model>

<verification>
After both tasks complete:

1. `uv run pytest tests/unit -x -q` exits 0 (all Phase 0 + new Phase 1 unit tests green; coverage gate now sees `src/shortfire/ingest/*` per D-91)
2. `uv run pyright` exits 0 in strict mode against `src/` + `tests/`
3. `uv run ruff check src/shortfire/ingest src/shortfire/domain` exits 0
4. `uv run python -c "from shortfire.ingest.storage.copy import copy_into_hypertable; from shortfire.ingest.retry import mexc_retry; from shortfire.ingest.rate_limit import MEXC_LIMITER; from shortfire.ingest.dead_letter.writer import write_to_dead_letter; from shortfire.ingest.context import get_engine, get_settings, get_metrics; print('infra ok')"` prints `infra ok`
5. `uv run pre-commit run --all-files` exits 0 (D-32 grep guards still green — Phase 1 added no naive `TIMESTAMP` literal, no `ON DELETE CASCADE`, no `: float` in domain)
</verification>

<success_criteria>
- Domain `Source` literal widened to the 4 CHECK-constraint-aligned values (D-59); every Phase 0 fixture using `"mexc"`/`"coinglass"` updated; pyright strict green
- `copy_into_hypertable`, `write_to_dead_letter`, three per-source retry decorators, three aiolimiters, three context accessors exported from `src/shortfire/ingest/`
- `[tool.coverage.run] omit` no longer hides `src/shortfire/ingest/*` (D-91)
- `uv run pytest tests/unit -x -q` and `uv run pyright` both exit 0
- No grep-guard regressions (`pre-commit run --all-files` green)
</success_criteria>

<output>
Create `.planning/phases/01-data-platform/01-01-SUMMARY.md` when done. Record:
- Domain widening rippled to N fixtures (list)
- Exact tenacity attempt counts (mexc=6, coinglass=5, coingecko=4) for downstream tuning visibility
- Whether `await conn.get_raw_connection()` worked on the actually-installed SQLAlchemy 2.0.x; if it required a fallback path (separate `asyncpg.create_pool()`), record so 01-05..01-10 know
</output>
</content>
</invoke>